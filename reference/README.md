# `reference/` — distilled hardware references

These `.md` files are **ours**: text-only emulation references, each written *from* a period
scan (a vendor data sheet or a MITS manual) and each citing that scan by filename in a `Source:`
line at its top. They carry what a developer reopens the scan for — register maps, port
addresses, status/control bit tables, disk geometry and timing, boot sequences, and the quirks
that bite an implementer.

The **scans themselves are not redistributed** (they are large and not ours; see
`.gitignore`). `docs/sources.md` is the manifest — what each original source is, where it came
from, and the traps paid for once. Read it before trusting any single number here; several of
these sources contradict themselves, and `sources.md` records which reading won and why.

The `Source:` links point at `#` — a placeholder to be filled in with a real URL later.

## CPU, front panel, and system

| Reference | What it covers |
|---|---|
| [Altair 8800 Operators Manual](Altair%208800%20Operators%20Manual.md) | Front-panel switches and LEDs, the 8080 register/flag layout, the full 78-instruction set with binary+octal opcodes, deposit/examine/run/step procedures, toggle-in sample programs. |
| [Zilog Z80 CPU](Zilog%20Z80.md) | Zilog Z80 CPU User Manual (UM0080): the full register/alternate/special-register set, the S Z F5 H F3 P/V N C flag layout, all instruction groups with hex opcodes/T-states/flags including the CB/ED/DD/FD/DDCB/FDCB prefix maps, interrupt modes 0/1/2 with NMI and IFF1/IFF2, and reset behavior. |
| [Intel 8080/8085 Assembly Language Programming](Intel%208080-8085%20Assembly%20Language%20Programming.md) | Intel's 8080/8085 programmer model (manual 9800301D): the A/B/C/D/E/H/L + SP/PC register set and pairs, the **S Z AC P CY** condition flags with exact semantics (AC is DAA-only and untestable; `IN` sets no flags), the **PSW flag-byte layout** (8080 filler bits 5=0/3=0/1=1 — **undefined on the 8085 ⚠**), the instruction-group taxonomy and the Appendix-A per-CPU T-state timing model (8080 480 ns–2 µs vs 8085 320 ns–2 µs clock period). Its reason to exist is the **8085-vs-8080 behavioral differences**: `ANA`/`ANI` always set AC on the 8085 (8080 = OR of bit 3), conditional branches **skip the third byte when not taken** on the 8085, and the new **`RIM`/`SIM`** instructions with full bit layouts. Full 8085 **interrupt** model: `TRAP` (24H, non-maskable, fires under `DI`), edge-latched `RST7.5` (3CH), `RST6.5` (34H), `RST5.5` (2CH), `INTR`, their priority, and the `RST n` → n×8 vectors. Assembler directive/macro/relocation language summarized. |
| [Intel 8085 undocumented instructions and flags](Intel%208085%20undocumented%20instructions%20and%20flags.md) | What Intel left out of the 8085 manual: the two extra condition bits **V** (overflow, PSW bit 1) and **K/X5** (PSW bit 5), with the exact per-instruction rules from Ken Shirriff's **silicon die** reverse-engineering (`V = carry-in⊕carry-out of bit 7`; `K = V ⊕ sign`, except `INX`/`DCX` where K is the 16-bit carry) — authoritative; and the ten undocumented opcodes (`DSUB ARHL RDEL LDHI LDSI RSTV SHLX LHLX JK JNK`) with opcode/octal/length solid but **operation and flag effects second-hand and flagged ⚠** (`LDHI`/`LDSI` flag behavior is a documented conflict). Why `8085EXM`'s `0D5h` mask cannot gate V/K, and the flags-first sequencing for issue #347. |
| [Altair 8800 Theory of Operation](Altair%208800%20Theory%20of%20Operation.md) | CPU-board gating and 2 MHz clock phases, the 8080 status byte and its data-bus bits, machine-cycle/interrupt/hold/reset timing, and the complete 100-pin S-100 bus pinout. |
| [Altair 8800 front panel schematic](Altair%208800%20front%20panel%20schematic.md) | Schematic 880-106: the `IN 0FFH` sense-switch decode (sINP + A8–A15 NAND), the A0–A15/D0–D7 LED buffers, and the RUN/STOP/STEP/EXAMINE/DEPOSIT one-shot sequencing. |
| [MITS Turn Key Board](MITS%20Turn%20Key%20Board.md) | 8800b Turnkey Module: the `FC00`–`FFFF` boot-PROM window (HDBL/monitor/MBL/floppy-loader sockets) and the **Phantom** latch it snoops out on the first `IN` from port `FE`/`FF` (the same event as reading the sense switches — why MBL won't run and boot loaders never read them), the 2SIO-Port-A-compatible 6850 SIO at `10h`, the Auto-Start `C3 00 xx` JMP synthesis (FC00 hard disk / FD00 monitor / FE00 tape / FF00 floppy), the Turnkey Monitor `M`/`L`/`J` commands, the two board revisions, and SB #007 / 88-SYS-CLG. |
| [Processor Technology Sol-20](Sol-20.md) | The Sol-PC integrated machine: the `F8h`–`FFh` onboard I/O map (serial `F8`/`F9`, the shared mixed-polarity status register `FA` + tape motor/baud write, tape `FB`, keyboard `FC`, parallel `FD`, VDM display `FE`, sense `FF`), every status bit and its active-high/low polarity from the SOLOS drivers, the **keyboard's key codes** (Table 7-4 — the eight bit-7 special keys `MODE SELECT`/`CLEAR`/`LOAD`/`HOME CURSOR`/arrows, and the five keys that send no code at all), and the `C000`–`CFFF` ROM/scratch/video memory map. |
| [Digicomp Research Pascal-100](Digicomp%20Research%20Pascal-100.md) | Dual-processor 16-bit S-100 CPU boardset (two cabled cards): a **Western Digital Pascal Microengine (WD9000)** that executes **UCSD Pascal III.0 P-code natively** + a stock **Z80** (I/O, interrupts, CP/M); only one runs at a time, the other in HOLD, switched by one byte to a **write-only System Control Port** (even, default `D0`; odd Map Output Port `D1`). Documents the port byte table (`80`/`00` processor swap; `C0`/`40`, `C1`/`41` Map enable/disable per processor; `C2`/`42`, `C3`/`43`, `C4`+`44`, `45`/`C5`), the **dynamic address-translation Memory Map** (Standard 16×8K→128K, Extended 64×2K→1M/512K, loaded Z80-only with **inverted** contents), **Microengine memory-mapped I/O** (word `FFpp`=8-bit port `pp`, `FEpp`=16-bit), the **Z80 Power-On-Jump** (`C3 00 X0`, DIP U242, J202), the interrupt model (pINT/NMI→Z80; Microengine int → semaphore SIGNAL, scratch `1F880/1`, vector `1F8C0/1`), the **byte=word×2** rule + reset init vector `1F8D0`, the boot sequence, the **ZMCOM** interprocessor area at byte `80h`, and the IEEE-696 bus behavior. **⚠ Not emulated** — reference for possible future S-100 CPU-board support. |
| [SD Systems SBC-100 & SBC-200](SD%20Systems%20SBC-100%20%26%20SBC-200.md) | SD Systems Z80 single-board computers (bus master): the shared `78h`–`7Fh` I/O map (CTC `78`–`7B`, 8251 USART console `7C`/`7D`, parallel `7E`/`7F`), the 8251 status polarity (TxRDY=D0, RxRDY=D1, active-high), the CTC baud tables and the `05h`/`45h` init difference, the parallel handshake (input "ready" = `7F` D1 active-low; **SBC-200 `OUT 7F` D1 switches onboard memory**), the X1/X2/X3 memory-mapping headers, and the reset **auto-start** (4K boundary via X16/X17/X18, released by a **read of port `7F`**) with the monitor PROM at `E000` / disk BIOS at `F000`. Documents both boards with the 2.4576-vs-4 MHz, 8251-vs-8251A, current-loop and memory-switch differences called out. |
| [MITS 88-VI / 88-RTC](88-VI-RTC.md) | MITS 88-VI Vector Interrupt + 88-RTC Real Time Clock — one S-100 card (the `virtc` board, MITS Feb 1976): the eight active-low bus lines **VI0 … VI7** with **VI0 = highest priority**, vectoring **VI level *n* → RST *n*** (CPU jumps to octal *n*×10), the **write-only** control/status port at **0xFE** (254 decimal / 376 octal), and the 88-RTC section (60 Hz-line or 10 kHz clock, ÷1/÷10/÷100/÷1000 jumper rates, one interrupt pulse per interval on the jumpered VI level). Its VI0-highest order is authoritative over the conflicting interrupt-pad labels in the 88-PIO/88-4PIO manuals. |
| [S100Computers V2 Z80 CPU Board](v2-z80-cpu-board.md) | The onboard monitor EEPROM + Power-On-Jump of the S100Computers (John Monahan) V2 Z80 board — the `v2z80` board (the Z80 processor itself is the separate `mits-z80cpu`): the **8K 28C64 EEPROM** at `F000`–`FFFF` as two 4K pages (low = `master0`, high = `master1`), **`OUT D3H`** bit 1 = EEPROM A12 / bit 0 = ROM-inactivate (write-only, reset default `00` = EEPROM on/low page), and the Power-On-Jump NOP-injection slide (U15 comparator vs jumper `P3`, default `F000`). The memory-manager ports `D2H`/`D3H` are **not** modeled. The `master0`/`master1` images boot CP/M 3 off the Dual SD card. |

## Memory and banking

See also [`docs/devguide/banked-ram.md`](../docs/devguide/banked-ram.md), which audits these
five real cards against how `altairsim` currently models bank switching (and finds most of the
model wrong).

| Reference | What it covers |
|---|---|
| [SD Systems ExpandoRAM](SD%20Systems%20ExpandoRAM.md) | The original ExpandoRAM ("I"), an S-100 DRAM board (MK4115/MK4116). **⚠ It has no I/O port at all** — a static, memory-mapped board whose "banks" are four fixed groups of 8 chips strapped to 8K/16K address slices by an 8-position DIP switch (U2 → 74LS138 → RAS0–3); per-bank write-protect + PHANTOM/manual output-disable. **Not** a runtime bank switcher — do not confuse with the ExpandoRAM-II. |
| [SD Systems ExpandoRAM II](SD%20Systems%20ExpandoRAM%20II.md) | The second-generation board, a different design: **port FF** takes a **page number** (0–9) that an on-board 82S130 PROM decodes, together with octal board-select + bank-enable DIP switches (S3), into a 32K or 48K **partition**. 4 banks of 8 chips/board (16K or 64K DRAMs → 64K or 256K/board). ⚠ The per-cell PROM page map (Fig. 2-4) is not reliably transcribable from the scan. |
| [Vector Graphic 64K Dynamic RAM](Vector%20Graphic%2064K%20Dynamic%20RAM.md) | Z-80-only S-100 DRAM board: **port 40H, one-hot** bank select (`1<<bank`, `0x01`→0 … `0x80`→7), 8 banks / ≤8 boards / 512K, bank 0 force-enabled at power-on/RESET (POC clears the port-40H latch and DO0 is pre-inverted so the cleared state = `0x01`). Jumper "area D" trims the top in 8K steps (ships at 56K, not full 64K). The one banked card whose encoding `altairsim` models correctly (`vram`). |
| [North Star HRAM](North%20Star%20HRAM.md) | HORIZON DRAM board (HRAM-32/48/64): **port C0H**, and the byte is **not** a bank number — **bit 0 = on(0)/off(1) command, bits 1–7 = one-hot** address of which bank the command toggles (per-board JP1). Only **≤6 banks** usable (one of bits 5/6/7 is spent on parity); software must switch the old bank off before the new one on. Two independently-switchable 32K sections (JP2); JP1 also sets the reset state (default: always on). |
| [Cromemco 64KZ / 64KZ-II RAM](Cromemco%2064KZ%20RAM.md) | Two S-100 DRAM boards as one family: **port 40H**, and the BANK SELECT byte is an **8-bit mask of active banks** (bit N ⇒ bank N on/off, several at once — `OUT 40H,28H` = banks 3 **and** 5), 8 banks. Each board is two 32K blocks A/B with per-block bank-membership switches (SW2/SW3) + a RESET-enable switch; 64KZ ≤512K with DMA/OVERRIDE, 64KZ-II ≤448K with per-16K-half enables and no documented DMA. |

## PROM / EPROM

| Reference | What it covers |
|---|---|
| [SSM IO-2 Parallel I/O Board](SSM%20IO-2%20Parallel%20IO%20Board.md) | SSM (ex-Solid State Music) S-100 **prototyping card**, the earlier/simpler sibling of the IO-4: committed circuitry for **one parallel input port (8212 U8) + one parallel output port (8212 U10)** and an **8-position DIP switch (U7)** decoder answering a **block of 8 consecutive addresses** (U1 7485 compare → U3 7442 1-of-8 select; pin 9 = first … pin 1 = last). Five poles decode **A7–A3** as an I/O card or **A15–A11** as a 2 K **1702-PROM** window. Committed-port jumpers: **SM** → I/O (disable memory read), **SOUT**/**SINP** status qualify (edge pins 45/46), **OUT STB**/**INP STB** → 8212 DS2, MD → GND. Three build personalities — parallel I/O; **serial** (AY-5-1013 UART, 74163/7493 baud divider for 110–1200 incl. 458.7 "Suding", 20 mA TTY loop or EIA RS-232, MITS Rev 0/1 layout: status even / data odd, DAV = D0−/D5+); and **1702 PROM** (≤8 = 2 K memory-mapped, added −9 V LM320T-8, optional 74L03+74L74 wait states 0/1/2). §4 host-clone recipes route the DAV bit per host (Altair Rev 0 → DI5, Rev 1 → DI0, ProcTech/IMSAI → DI6). Credited to Malcolm Wright + Lynn Cochran. **Not emulated** (issue #402) — a prototyping card; its serial build personality informed the strap-serial engine, but the generic serial board shipped is **Generic SIO** (`gsio`) (a fuller SSM IO-4 remains a future board). |
| [SSM IO-4 (2P + 2S) I/O Board](SSM%20IO-4%202P%2B2S%20IO%20Board.md) | SSM (ex-Solid State Music) combined S-100 I/O card: **two full-duplex serial UART channels** (Serial A = U9, Serial B = U8) + **four latched 8212 parallel ports** (2 in J4/J6, 2 out J3/J5). UART control on **S2 (A) / S1 (B)** — parity/word/stop and **PR** status-vs-data port reversal (OFF = MITS/ProcTech, ON = IMSAI); **W3** baud straps (10 rates **55–9600**, RX/TX per channel independent); **W1/W2** status-word straps (6 status bits → any data bit, polarity by **74367 positive / 74368 negative** at U18/U16); **20/60 mA opto current loop** (≤4800 baud) or **EIA/RS-232** (DS1488); **S3** serial 4-port block (A7–A2) and **S4** parallel 2-port block (A7–A1) with **mutual exclusion on overlap**; **W4** interrupts (serial-RX + parallel-in) to an 8-level card or polled; §4 host-emulation recipes (8251, Altair Rev 0/1, ProcTech, IMSAI, Selectric, PolyMorphic). **Serial half emulated** as the `io4` board (two strap-configurable channels); the parallel ports and current-loop/EIA electrical options are out of scope. |
| [SSM PB1 EPROM Programmer](SSM%20PB1%20EPROM%20Programmer.md) | SSM (ex-Solid State Music) **2708/2716 programmer + 4K/8K EPROM board**: two personalities on one S-100 card. The **programmer** reserves a **4K memory window** (any 4K boundary, **SW2-1..4** = A15–A12) for the two sockets (**U22 = 2708, U23 = 2716**) plus one **I/O control port** at any `x0H` address (**SW2-5..8** = A7–A4; only A4–A7 decoded, so low digit must be 0) — an `OUT` arms the board and latches **D0=1 → 2708 / D1=1 → 2716** timing, then each **memory write to the socket window becomes a programming pulse** (2708 ~0.6 ms @ +26 V; 2716 ~50 ms @ +5 V) held by the S-100 **READY** line, so **wait states are mandatory**; a **read of the socket window resets** the flip-flop (LED off). The **read-only** area (U11–U14) maps 4K 2708 / 8K 2716 to any boundary **above `8000H`** via **SW3** (SW3-1 = type), with unused sockets auto-disabling the data bus; jumper-selectable **0–4 read wait states**; +26.5 V from a TL497 (U32). Ships four 8080 driver routines (`ORG 0100H`, exit to the SSM monitor at `F021H`). **⚠ Not emulated** — reference for possible future support. |

## Altair 680 (Motorola 6800)

MITS's *second* machine — a **6800** computer, not an 8080/S-100 system, so its boards are
**memory mapped** (no `IN`/`OUT`) and generally **active-low**. Grouped here so the 6800 parts
are not mistaken for S-100 hardware.

| Reference | What it covers |
|---|---|
| [Altair 680b KCACR](Altair%20680b%20KCACR.md) | The 680b's Kansas City Standard audio-cassette interface — a **memory-mapped** board (not `IN`/`OUT`): status/control at **`F010`** (61456), read/write data at **`F011`**, read-vs-write of each address being different registers. Documents the **⚠ active-low ("True = Logic 0")** status/control convention (status D0 Read-Data-Available / D7 Transmit-Buffer-Empty; control D0/D1 read/write interrupt enable, D6/D7 remote motor off/on), **software motor control** (store `7F` on / `BF` off; `POKE 61456,127`/`191`), the interrupt model (enable via `FE`/`FD`; `IRQ` vector `FFF8/FFF9` → `0100` from the monitor PROM; any register access or Motor-Off clears the latch), the **SMC COM2017/H** UART (same 1602 family as [com2502](com2502.md), 8N2, 4800 Hz 16× clock), the **Kansas City Standard** modulation (300 baud; mark = 8×2400 Hz, space = 4×1200 Hz; LSB first) and the loader/punch PROM (socket V, **`FD00`**; punch `.J FD74`, load `.J FD00`; Motorola S-records; `C`/`M` error characters). |
| [Altair 680b Universal I/O Board](Altair%20680b%20Universal%20IO%20Board.md) | The 680b's general-purpose parallel+serial expansion board — **memory-mapped**, one slot, built from a **6820 PIA** (parallel) and a **6850 ACIA** (serial). Documents the `F0XX` address model (A15–A8 = `F0`, A7–A4 via switch **S9**, A3 parallel/serial, A2 parallel-port select) and the **default map** (PIA-C parallel `F008`–`F00B`, PIA-B `F00C`–`F00F`, ACIA-D serial `F006`/`F007`); the **fixed** switch-input port at **`F003`** and the 8-bit non-latched output at **`F010`–`F013`** (⚠ **collides with the KCACR** — period fix is "remove UI/O IC A1"); the standard 6820 PIA model (A/B sections, Control bit 2 = DDR/Data select, CA1/CA2/CB1/CB2 handshake, power-on reset → all inputs); the standard 6850 ACIA control/status bits (master-reset `11`, ÷16/÷64, RDRF/TDRE/DCD/CTS/FE/OVRN/PE/IRQ); baud via switch **S10** + clock-divide (75–9600, or 5 extra ÷64 rates); RS-232/TTL/20 mA-TTY levels; and both chips' `IRQ` wiring (active-low → `FFF8/FFF9` → `0100`). **⚠ active-HIGH** (raw Motorola bits — *not* the KCACR's inverted-true rule). |
| [Altair 680b Operator's Manual](Altair%20680b%20Operators%20Manual.md) | The operator-facing guide to the **680b machine itself** (not a plug-in board) — the three user-wired areas of the **main PC board** and, most useful for emulation, **front-panel operation** and the **power-up/deposit sequence**. Documents the baud-rate generator jumper truth table (4 holes → 50–9600 baud at IC Z pin 10; ⚠ **2400 listed twice**, reproduce verbatim); the **1 K on-board RAM** base-address scheme (jumpers 1–6 / pads 10–15 = NAND of A10–A15 or complements; worked bases `0000`, `2000`/8 K, `4000`/16 K, `5000`/20 K, `6000`/24 K, `8000`/32 K); the three **hardware-programmable bits** (1 = Baudot yes/no, 2 = 1/2 stop bits, 5 = No-Terminal/Terminal; 3,4 reserved, tie LOW); the three serial **interface builds** (ASR33/KSR33 20 mA loop, RS-232, Baudot-needs-external-isolation); the optional **PTRC** paper-tape-reader-control block; the **front panel** (4-hex-digit address A0–A15, 2-hex-digit data D0–D7, down=0/up=1, HALT continuously addresses the switches, DEPOSIT, RESET-before-RUN, "AC" power LED); and the **power-up sequence** (HALT→POWER→RESET→ADDRESS/DATA→DEPOSIT; the **No-Terminal bit** makes the PC start at `0000` on RUN; with a terminal, defer to the System Monitor manual). |
| [Altair 680b Programming Manual](Altair%20680b%20Programming%20Manual.md) | The programmer's guide to the **680b** (Motorola **6800**) — mostly the standard Motorola M6800 reprint (CPU model, 7 addressing modes, 72-mnemonic/197-opcode set, arithmetic tutorials, **all deliberately out of scope** here), distilled down to what is **680b-specific**. Documents the **programming model** (ACCA/ACCB/IX/PC/SP, 6-bit CCR `H I N Z V C`); the **addressing-mode summary** (byte counts; ⚠ CPX/LDS/LDX take 3-byte immediates; ⚠ **direct-vs-extended chosen by value** — ≤255 direct, >255 extended; branch range `(PC+2)−128…+127`, 8-bit two's-comp offset); the **interrupt latency** (⚠ **12 cycles** from end of instruction, **4** after `WAI`); the **680b assembler source language** (number formats — default decimal, `$`/`H` hex, `@`/`O`/`Q` octal, `%`/`B` binary, `#'C` ASCII; ⚠ `$…B` = hex; one-level forward reference; no operator precedence) and directives (incl. the 680b-specific **`MON`** = return to PROM Monitor); and — the emulation payload, from **Appendix C** — the full **6850 ACIA** register model (TDR/RDR + write-only Control + read-only Status): Control CR1,0 divide (`11`=Master Reset), CR4–2 word/parity/stop (unbuffered), CR6,5 Tx-ctrl+RTS+break, CR7 Rx-int-enable; Status RDRF/TDRE/DCD/CTS/FE/OVRN/PE/IRQ with ⚠ **CTS-high inhibits TDRE** and **DCD-high forces RDRF empty**; ⚠ **ACIA is not auto-reset on power-on** (software must Master-Reset then set divide); the **paper-tape reader** control (RTS high, CR6=1/CR5=0, = reader ON); and the **interrupt vectors** at `FFF8`–`FFFF` in the PROM Monitor (IRQ `FFF8/9`→`0100`). ⚠ **The ACIA's memory address is not given here** — it comes from the machine wiring (KCACR `F010`/`F011`, UI/O S9 window); do not invent one. |
| [Altair 680b Theory of Operation](Altair%20680b%20Theory%20of%20Operation.md) | The circuit-level companion to the Operator's and Programming manuals — and the one that **names the on-board addresses the Programming Manual withheld**. Documents the **680b clock** (500 KHz two-phase non-overlapping, 2 MHz crystal ÷4; Φ1 hi 600 ns/lo 1.4 µs, Φ2 hi 1 µs; ⚠ **all transfers on Φ2**, and the 6800 runs at **½ speed vs stock**); the full **6800 bus-control signal set** (`RESET`→PC from `FFFE/FFFF`; `HALT` hi=RUN, lo=halted→`BA` hi→buses tri-stated→front panel; `R/W` hi=READ; `VMA`; `DBE`=Φ2; `R/W-P`=R/W∧Φ2; `BA`; `TSC`=DMA); the memory/PROM map (**1 K** 2102A RAM jumper-placed; four 256×8 PROMs, **PROM 1 at `FF00`** → monitor+vectors in `FC00`–`FFFF`); and — the emulation payload — the **on-board address decode**: the **serial 6850 ACIA at `F000`/`F001`** (`F000`=Control(W)/Status(R), `F001`=TxData(W)/RxData(R); A0→RS; Enable=Φ2; `IRQ`→MPU `IRQ`; `RTS` low = paper-tape reader ON), the **34702 baud generator** (2.4576 MHz, 4 jumper holes, ⚠ output **16×** baud), and the **Baudot interface at `F002`** (ACIA unused, MPU bit-bangs 5-level via `BD0`; also the config-bit read port). ⚠ **This resolves the Programming Manual's "address not given" caveat** — the base machine's own serial port *is* `F000`/`F001`; the KCACR `F010`/`F011` and UI/O S9 window are the plug-in boards. Also records the manual's own **page-15 erratum** (the R/W vs R/W-P scope photos are cross-labelled). |
| [Altair 680b Turnkey Front Panel](Altair%20680b%20Turnkey%20Front%20Panel%20Schematic.md) | The single-sheet schematic (MITS DWG **SH 1-11**) for the 680b's **turnkey** front panel — the *reduced* panel a turnkey machine ships with **instead of** the Operator's-Manual hex panel. **No address/data toggles, no DEPOSIT/EXAMINE, no A0–A15 / D0–D7 LEDs** — it only **RESET**s the 6800 and puts it in **RUN or HALT**, with three indicator LEDs (**PWR ON / RUN / HALT**). The emulation payload is the two bus-visible signals, both **debounced and Φ2-synchronized** (a 7474 flip-flop chain clocked by Φ2 on pin 57): **`RES̄`** (connector pin **54**, **active-low** → 6800 RESET → PC from `FFFE/FFFF`) and **RUN/HALT** (pin **63** → 6800 HALT: hi = RUN, lo = halted → `BA` → buses tri-stated). Documents the **reset-scope jumper** (**JA–JC** = reset in Run *or* Halt; **JB–JC** = reset in Halt *only*) as a power-on build constant, the switch/LED complement, the debounce/latch build detail (7474×2 + 7400; R5 20 K / R6 10 Ω / C1 0.1 µF reset debounce; cross-coupled NAND RUN/HALT latch), and power/ground pins (VCC 1/51, GRD 50/100). ⚠ **No boot PROM** — unlike the 8800b Turnkey Module, the reset vector still points into the **main-board** monitor PROM at `FF00`; and ⚠ this panel has **no deposit path**, so HALT here only stops the CPU (the full hex panel's HALT-enables-deposit behavior does not apply). |
| [Altair 680b Software](Altair%20680b%20Software.md) | **Not a hardware manual** — the provenance anchor for the 680b *software* that ships as built-in ROMs. Records the four deramp.com `altair_680/` software directories and what each holds: `PROM Monitor/` (**MON680**, **SWIMON** → `builtin:mon680`/`swimon`), `PROM for KCACR/` (**KCACR** loader/punch → `builtin:kcacr`), `Cassette Binary Loader/` and `BASIC/` (runtime cassette software, not embedded). Notes which images are tracked verbatim under `roms/<NAME>/` (with `.ASM`/`.LST`) and CRC-checked at build time, and which are loaded at run time as on real hardware. Cross-references `docs/roms.md` for the per-image size/range/CRC. |

## Serial and UART

| Reference | What it covers |
|---|---|
| [88-SIO Rev 0 & 1](88-SIO%20Rev%200%20%26%201.md) | MITS Serial I/O board: the two-port model, both original and errata status-word bit layouts (with the active-low ready polarity), interrupt-enable bits, the 12-bit baud preset, and the address jumpers. |
| [Altair 2SIO User's Manual](Altair%202SIO%20User%27s%20Manual.md) | 88-2SIO dual-6850 board: per-port register map, control/status bits and their polarity quirks, the ÷16/÷64 baud jumpers, and the three interrupt modes. |
| [Altair 88-ACR Cassette Interface](Altair%2088-ACR%20Cassette%20Interface.md) | 88-ACR cassette: ports 006/007, status-word polarity, 8N1 framing, the 300-baud FSK tone format, the tape leader format, and the load procedure (no motor control). |
| [MITS 88-UIO](88-UIO.md) | 88-UIO Universal I/O: a 6850 serial port (IC K, 020/021 octal, 7-position baud jumper 110–19200, TTY/RS-232) **and** an 88-ACR-style cassette section (AY-5-1013A UART, IC L, 006/007 octal, active-low status) on one board. Documents the two things the plain ACR lacks — **motor control** (`OUT 6,127` on / `OUT 6,191` off, relay normally closed) and a **SW-1-selectable modulation standard** (MITS 2400/1850 vs Kansas City 2400/1200) — plus the 4-position config switch and the defaults that reproduce the standard 0x10 + 0x06 layout. |
| [PMMI MM-103 Modem and Comm Adapter](PMMI%20MM-103%20Modem%20and%20Comm%20Adapter.md) | PMMI MM-103 Bell 103 modem on an S-100 board (MC6860L + an unnamed 1602-family UART): the four-address block where **read and write are different registers** (default base `C0`, North Star `E0`), all six register bit tables with the **four active-low status bits** (Dial Tone, Ringing, CTS, Ans Phone) and **four active-low control bits** (ESD, Tx Brk, Brk Rel, ST), the `250,000/(N×16)` rate generator that also sets the dial rate and the 40%/60% timer pulses, `IN BA+3` as a side-effect strobe that loads the interrupt mask *and destroys the baud divisor*, the pulse-dial and auto-answer procedures, 6860 handshake timing, the non-queueing four-source interrupt system (factory E9–E10 jumper → `RST 7`), and the INTA/pin-96 field modification. **Records ten errata**, including the manual's **backwards overbar convention** and its **two dialer programs that dial opposite phases**. |
| [IMSAI SIO-2](IMSAI%20SIO-2.md) | IMSAI SIO-2 serial/parallel board: two independent serial channels (each an **Intel 8251 USART**) plus a parallel port, on DIP-strapped 4-port groups. The 8251's read-vs-write register shape — **data at the even offset, status/command at the odd offset** — and its all-active-high status byte (**TxRDY=D0, RxRDY=D1**, cross-confirmed by the polled console drivers); the mode/command-word programming sequence after reset; and the baud/handshake straps. The values behind the strap-serial `imsai-sio2` built-in profile (on `gsio`/`propio`). |
| [Cromemco TU-ART](Cromemco%20TU-ART.md) | Cromemco TU-ART Digital Interface: a twin-channel S-100 board, each channel a **TMS 5501** (UART + 5 timers + priority interrupt controller). The 16-port-per-device map (DIP-strapped base on A7–A4, register on A3–A0) with the trap that **status-read and baud-write share offset 0** while the **command register is its own write-only port at offset 2** (not the 8251/6850 read-vs-write shape); the **all-active-high** status byte with the Cromemco trace-strapped convention **TBE=D7 / RDA=D6** (cross-confirmed by the manual's own polled `AND 80H`/`AND 40H` drivers); the one-hot baud+stop register (HBD ×8); the command register; and the two-device Z-80-mode-2 / 8080-mode-0 interrupt chain (summarized — the serial console only needs to poll RDA/TBE). Records the manual's baud-port EQU erratum (p.24 says `base+3`; the register map says offset 0). |
| [CompuPro System Support 1](CompuPro%20System%20Support%201.md) | CompuPro/Godbout multifunction S-100 board (sometimes miscalled "Cromemco"): a 16-port block (standard base `50H`) packing a **2651 UART** serial console (TxRDY=D0/RxRDY=D1 active-high; the DCD/DSR status bits and DTR/RTS command bits are **inverting** — a set bit drives the RS-232 line low/asserted), dual **8259A** master/slave interrupt controllers (15 sources, the 8080/Z80 **PHANTOM\*** CALL-fetch trick for interrupt-acknowledge), an **8253** interval timer, an **MSM5832** BCD real-time clock (seconds are write-ignored, not read-as-0; chip model in [OKI MSM5832](OKI%20MSM5832.md)), and a **9511A/9512** math-chip socket (stack misalignment is reset-only). No erratum found. |
| [CompuPro Interface II](CompuPro%20Interface%20II.md) | CompuPro/Godbout **Interfacer II**: three full-duplex latched-TTL parallel ports **plus** one **1602/1863 UART** serial channel, each relocatable anywhere in the 256-port space (serial 2-port block, parallel 4-port block). Serial status all active-high (TBMT=D0, DAV=D1); UART framing set by eight power-up-jumpered latch bits the control port can flip at runtime; parallel status interleaved DAV0/TKN0/DAV1/TKN1/DAV2/TKN2 (D0–D5); a shared programmable timer-interrupt; all interrupts on the S-100 `VIx` lines (`sINTA` unused). Records the jumper-header-vs-control-port **reversed bit-order** trap. |
| [CompuPro CPU 8085/88](CompuPro%20CPU%208085-88.md) | CompuPro/Godbout **dual-processor** IEEE 696 / S-100 CPU board: an **8085A-2** (8-bit, 2/6 MHz via paddle switch S4) and an **8088** (8-bit bus, fixed 8 MHz) on one card, only one on line at a time. **One shared I/O port (standard `FDH`, DIP S3, `ON`=0) is both the swap trigger and the Memory Manager:** an `IN` toggles which CPU runs (destroys A, returns `FFH`); an `OUT` latches the **16 MB extended-address** byte onto A16–A23 (with the 8088 in control only the **top 4 bits** are used → A20–A23, since the 8088 natively drives A0–A19). On power-up the **8085 is in control at `0000H`** (or the S2-selected **power-on-jump** page — a forced `C3 00 pp`), the 8088 is held; its first entry is `FFFF0H`. Reset-On-Swap (S1-4/S1-5, **both OFF for DMA**) chooses RESET vs. resume-in-place per CPU when it comes back on line; one-wait-state-per-I/O-cycle (S1-3); MWRITE gen (S1-7); IMSAI front panel via J2/J6/S1-7; reset coupling via J7. **⚠ Records the manual's crystal contradiction** (Circuit Desc. X2=24/X3=12 MHz vs. Parts List X2=15/X3=10 MHz). |
| [Processor Technology 3P+S](Processor%20Technology%203P%2BS.md) | The 3P+S ("3 Ports + Serial"): one **AMI S1883 / TMS6011** UART serial channel + two fixed-direction 8-bit parallel ports in a 4-address block (channels A/B parallel, C status/control, D UART data), placed anywhere by jumper. Its defining quirk is that the **status/control byte has no fixed bit layout** — the builder wire-jumpers UART flags (PE/FE/OE/RDA/TBE), parallel flags (FA/FB/XA/XB) and EIA lines onto whichever bits the software expects; TBE=D7 / RDA=D6 **active-high** confirmed from the Appendix V test-program code (`ANI 80H`/`ANI 40H`). A 12-bit preset baud divider (35–9600), the CRL-strobe framing trap, and **no onboard interrupt logic** (88-VI is an off-board option). |
| [Console IO Board](Console%20IO%20Board.md) | S100Computers **Console IO Board** — a Parallax-Propeller console (PS/2 keyboard in, VGA out) presenting to the S-100 bus as a plain **two-port polled "status + data" console** with no UART register map. The port pair (monitor default `00`/`01`, test software `14`/`15`, both SW-selectable), and its jumper-configurable (P74–P77) status-bit convention — the worked SD-Systems-8024 example: **keyboard/RX ready = status bit 1 active-high, output/TX ready = bit 2 active-high**, DATA read clears the ready flag, DATA write sets the output-busy flag. Modeled as the `propio` single-channel subtype of the strap-serial engine (shared with `gsio`). |
| [6850](6850.md) | Motorola MC6850 ACIA: register map, control/status bit layouts, divide/word-select/transmitter-control tables, master reset, and chip-select logic. |
| [com2502](com2502.md) | SMC COM2502/COM2017 UART (the 88-SIO's UART): the control-word framing fields, the SWE#-gated status flags, and double-buffered TX/RX holding-register behavior. |
| [Intel 8251 / 8251A USART](Intel%208251%20USART.md) | Intel 8251 (and pin-compatible **8251A**) programmable serial interface: the programmer-visible register model, the mode/command-word programming sequence after reset, and the framing state machine — emulated behind the [SD Systems SBC-100/200](SD%20Systems%20SBC-100%20%26%20SBC-200.md) console (`7C`/`7D`) and the [IMSAI SIO-2](IMSAI%20SIO-2.md). **Data at the even offset, status/command at the odd offset**; all-active-high status (TxRDY=D0, RxRDY=D1), with the 8251A `TxRDY = TxEN·C̄TS·empty` caveat noted. |
| [Signetics 2651 USART](Signetics%202651%20USART.md) | Signetics/National **2651** (2661) programmable communications interface — the serial channel on the [CompuPro System Support 1](CompuPro%20System%20Support%201.md). **Four separate ports** (data/status/mode/command decoded from A1/A0, no data-vs-control write ambiguity); the **on-chip baud-rate generator** programmed by Mode Register 2's low nibble; the MR1-then-MR2 mode pointer; all-active-high TxRDY=D0/RxRDY=D1 with **inverting** DCD/DSR status and DTR/RTS command bits (a set bit drives the RS-232 line low). The chip behind `src/chips/sig2651.{h,cpp}`. |
| [Processor Technology CUTS](CUTS%20Assembly%20and%20Test.md) | The Processor Technology CUTS ("Computer Users Tape System") S-100 cassette module — the same modem that sits on the Sol-20 motherboard: two program-selected rates (**1200 bps default**, 300 bps Kansas-City/Byte-compatible) chosen through the status latch, and the per-bit modulation (mark/space/idle tones and cycle counts) from the 1977 *Assembly and Test* manual, corroborated by Holden's 2018 scope teardown. **Supersedes** the "MEASURED, not from a manual" table `reference/Sol-20.md` used to carry. |
| [OKI MSM5832](OKI%20MSM5832.md) | OKI MSM5832 microprocessor real-time clock/calendar: the 13 BCD digit registers (Seconds…Years + day-of-week), the command port (Hold/Write/Read/digit-select) and data port, and the special encodings — **Hours-10** carries the tens digit in bits 1:0 with PM in bit2 and 12/24-hour in bit3; **Days-10** carries the leap-year flag in bit2; **seconds are write-ignored (forced to 0) but read normally**. The clock chip behind the [CompuPro System Support 1](CompuPro%20System%20Support%201.md). |
| [Intel 8253](Intel%208253.md) | Intel 8253 / 8253-5 Programmable Interval Timer: three independent 16-bit down-counters + a control-word register in a four-address block (counters `+0/+1/+2`, control `+3` **write-only**). The control-word layout (SC select, RL read/load format incl. the **Counter Latch Command**, mode 0–5, BCD), what each mode does to OUT (mode 0 interrupt-on-terminal-count, mode 2 rate generator, mode 3 square wave, the gate-triggered 1/5), and the emulation stance of **deriving the count from elapsed clock ticks** rather than stepping it. The interval timer on the [CompuPro System Support 1](CompuPro%20System%20Support%201.md); the chip behind `src/chips/intel8253.{h,cpp}`. |
| [Intel 8259A](Intel%208259A.md) | Intel 8259A Programmable Interrupt Controller: eight prioritized IR inputs, a mask, an in-service register, and the INT/INTA vector logic, in a two-address block (A0 lo/hi). The initialization words (ICW1–4), the operation words (OCW1 mask, OCW2 the EOI/rotate family, OCW3 read-select/special-mask), **fully-nested priority with rotation**, the **master/slave cascade**, and the acknowledge vector — the **MCS-80/85 3-byte CALL** vs the 8086 single vector byte. Includes the emulation stance of **computing INT from the live IR levels** (not caching them) and why the `PHANTOM*` INTA trick can be unnecessary. The dual controllers on the [CompuPro System Support 1](CompuPro%20System%20Support%201.md); the chip behind `src/chips/intel8259a.{h,cpp}`. |

## Parallel I/O

| Reference | What it covers |
|---|---|
| [MITS 88-PIO](MITS%2088-PIO.md) | MITS 88-PIO parallel board (discrete TTL, two 8212 latches — *not* a PIA): the two consecutive ports (even Control/Status with A0 split, odd Data), the fixed-direction 8-bit in/out latches, the status bits (DI0 output-ready, DI1 input-has-data), the interrupt-enable bits (DO0/DO1, "BOTH" jumper), the `SBO`/`SBI` strobe + `BO`/`BIN` handshake, and both interrupt paths (88-VI vectored — with the priority-order conflict vs the 88-VI board — or single-level RST 7 → 0x38). |
| [MITS 88-4PIO](MITS%2088-4PIO.md) | MITS 88-4 parallel board (up to four Motorola 6820 PIAs, ports J/K/L/M): the 16-address-per-board decode (A4–A7 board, A3–A2 port, A1–A0 section/register), the read/write control/status register bit map (bit 7/6 IRQ status, 5–3 C2, 2 DDR-select, 1–0 C1), the C1/C2 edge+enable tables, the CA2/CB2-as-output read/write-strobe modes, DDR direction control, the power-on all-inputs reset, the init/handshake protocol, and both interrupt paths (88-VI vectored or single-level `PINT` → RST → 0x38). |

## Analog and game I/O

| Reference | What it covers |
|---|---|
| [Cromemco D+7A](D%2B7A.md) | Cromemco D+7A I/O board: the block of **8 consecutive ports** (default base 030 octal / 0x18), one parallel port at BASE+0 and **seven analog channels** at BASE+1..7 that are A/D on read and D/A on write, the 8-bit **two's-complement** 20 mV/LSB scale (−2.56 V … +2.54 V), the 5 µs A/D and the 5.5 µs / 11-wait-state READY hold, and the Dazzler REV B flash mod. The input+sound end of a Dazzler game console. |
| [Cromemco JS-1](JS-1.md) | Cromemco JS-1 joystick console — a **peripheral**, not a board, that plugs into the D+7A (1 or 2 per board): the two-axis pots → analog inputs, four buttons → parallel-input bits, and speaker → analog D/A output, with Cromemco's recommended console-1/console-2 port map (X/Y → 0x19/0x1A and 0x1B/0x1C; buttons → D0–D3 / D4–D7 of 0x18; speakers → 0x19 / 0x1B). Notes the button-polarity assumption. |
| [PAiA SS-100 Speech Synthesizer](PAiA%20SS-100%20Speech%20Synthesizer.md) | PAiA SS-100 S-100 board carrying a **Votrax SC-01** single-chip phoneme speech synthesizer: the two write-only OUT ports (data `30H` = 6-bit phoneme code + 2 inflection bits, parameter `31H` = the control word) at a DIP-selected base, the one jumper-selectable vectored interrupt (VI0–VI7 / PINT, default RST 7 / 0038H; CP/M variant VI6 / 30H) raised per phoneme, POC auto-disable, the control-word bit layout (ENABLE / VOLUME / MODE speech-effects / master-clock 16 steps), the SC-01 chip model (22-pin pinout, signals, 720 kHz clock, 47–250 ms phoneme duration, amp and stand-alone configs), the full 64-entry phoneme chart + dipthong table, and the software command set. **⚠ Not emulated** — reference for a possible future `ss100` board. |

## Printer

| Reference | What it covers |
|---|---|
| [88-C700 Centronics Printer Controller](88-C700%20Centronics%20Printer%20Controller.md) | MITS 88-C700 S-100 interface for the Altair C700 line printer: the two consecutive ports (even Control/Status, odd Data) with the A0 split and the MITS default base 002, the status byte (ACKNOWLEDGE/BUSY/PAPER EMPTY/SELECT/FAULT/INTERRUPT ENABLE/REQUEST), the control byte (D0 PRIME, D1 INTERRUPT CONTROL), the data-write strobe that clears the ACK latch and interrupt, the single-level interrupt switched per-character or per-CR/LF (SW2 #4), and the C700's own ASCII control codes (LF/CR/DC1/DC3/DEL/SO). |
| [88-LPC Printer Interface](88-LPC%20Printer%20Interface.md) | MITS 88-LPC S-100 interface for the 88-LP line printer (Okidata, 64-char, 80-col): the two consecutive ports (even Control, odd Data) with the A0 split and the MITS default base 002 (128 jumpered bases E1–E7 → A1–A7), the **active-high command** control byte (D0 PRINT, D1 LINE FEED, D2 CLEAR, D3 interrupt enable), the status byte (D0 buffer-empty, D1 not-printing, D2 paper-ok, D3 line-feed-ok), the **6-bit data code** and its ASCII packing (bit 6 = complement of bit 5, space=0x20 — *inferred*, the Okidata glyph chart is not in the manual), the 80-char line buffer that prints on PRINT or auto-fills at 80, and the full hardware interrupt (88-VI vectored or single-level PINT). |
| [Cromemco PRI Printer Interface](Cromemco%20PRI%20Printer%20Interface.md) | Cromemco PRI: one S-100 board driving **two printers at once** — a 3703/3779 dot-matrix channel (ports `53`/`54`, 7-bit ASCII + active-low DATA STROBE bit 7, status BUSY=bit 5 active-high / ACKNLG=bit 7 active-low) and a 3355A daisy-wheel channel (ports `5A`–`5D`, **all connector signals active-low**: a 12-bit carriage/paper-feed position + direction spread across `5A`/`5B`, a six-command control byte at `5C` (RESTORE/CHAR-STROBE/CARR-STROBE/PAPER-FEED/TOP-OF-FORM/PRINTER-SELECT), and a five-bit status at `5A` IN). Default base `05H` (fixed low nibble; relocatable only by a trace-cut DIP mod), interrupt-enable at bit 2 of `53`/`5D`, the board's own ~1 s **automatic ribbon-lift** timer, and TU-ART-style Z-80/8080 vector-gating. Internally consistent — no erratum. |

## Video / display

| Reference | What it covers |
|---|---|
| [Processor Technology VDM-1](Processor%20Technology%20VDM-1.md) | VDM-1 Video Display Module: the 1 KB memory-mapped screen RAM (16×64, default `0xCC00`), the single I/O port (default `0xCC`) with `OUT` scroll-latch + 0.25–0.5 s one-shot and `IN` status (D0 timer, D1 SCAN ADVANCE), the character/cursor byte split (D0–D6 → MCM6574 glyph, D7 → cursor), the CR→EOL / VT→EOScreen control blanking, and the SW1–SW6 video/cursor/blanking options. |
| [SD Systems VDB-8024](SD%20Systems%20VDB-8024.md) | SD Systems VDB-8024 video display board — an **intelligent terminal on one S-100 board** (onboard Z80 + SMC CRT 5027 + firmware PROM + private screen RAM), **not memory-mapped**: to the host it is just **two I/O ports** — status `IN 00H` (**D1** keyboard-ready, **D2** display-ready, both active-high) and data `IN`/`OUT 01H` (keyboard in / display out). 80×24, 7×8 cell in a 7×10 field (CGEN-bit-0 descenders), 128/256-glyph character-generator PROM. Documents the `OUT 01H` control-word set (scroll, cursor `ESC = Y X`, display speed, reverse/underline/blink enhancement fields, protect) with the honest caveat that control-code semantics are **firmware-dependent**, and the CGEN/cursor/interrupt jumpers. This is the "video console" the **SDMONV21** monitor build drives (ports 00/01) — cross-confirmed by that monitor's own `IN 00H`/`AND 02H`/`IN 01H` console routine. |
| [Cromemco Dazzler](Cromemco%20Dazzler.md) | Dazzler color graphics: the DMA bus-master framebuffer scan, `OUT 0x0E` (D7 on/off, D6–D0 = 512-byte-aligned base), `OUT 0x0F` (resolution X4/size/color bits), `IN 0x0E` (odd-even line, end-of-frame), the 512-byte/2 KB quadrant memory map, and the byte→pixel/color encoding for all four modes (normal 4-bit RGBI two-per-byte; X4 eight on/off bits). |

## Floppy and disk

| Reference | What it covers |
|---|---|
| [Altair Floppy (88-DCDD) Manual](Altair%20Floppy%20%2888-DCDD%29%20Manual.md) | 88-DCDD 8″ floppy: ports 010/011/012, drive-select/status/control register bits (active-low convention), the 32-sector hard-sector geometry, and the head/step/read/write sequences. |
| [88-MDS Minidisk Manual](88-MDS%20Minidisk%20Manual.md) | 88-MDS minidisk: ports 08/09/0A with full bit tables, 35-track/16-sector geometry, 300 RPM / 64 µs-byte / 12.5 ms-sector timing, and the motor/disable timers. |
| [88-MDS Minidisk Schematics](88-MDS%20Minidisk%20Schematics.md) | 88-MDS 6-sheet schematics: the hard-wired port decode (no address jumper), the S-100-2 MHz-derived byte clock, the non-inverting 8T97 status buffers, and the 4020 motor-off counter. |
| [Minidisk Info from MITS](Minidisk%20Info%20from%20MITS.md) | One-page MITS info sheet: capacity/transfer/access figures, the MDBL and DRWT PROMs, software hard-sectoring. Flags its own wrong 5-second motor timer. |
| [FDC+ Manual](FDC%2B%20Manual.md) | The modern FDC+ drop-in: the port map, the nine latched drive types and their geometries (including the 8 MB medium the period manuals cannot describe), and sector-start interrupt routing. |
| [88-HDSK](88-HDSK.md) | 88-HDSK "Datakeeper" hard disk: the Pertec D3422 geometry, the 4PIO port map, all seven commands with bit layouts, the errata-corrected error/status byte, and the IV-byte register map. |
| [Tarbell Floppy Disk Interface Manual](Tarbell_Floppy_Disk_Interface_Manual.md) | Tarbell SD interface (FD1771): the F8–FC port map with DRQ/INTRQ polarity, the DIP config, the FD1771 command/status layouts, the 32-byte bootstrap, and IBM 3740 geometry. |
| [Western Digital FD1771 - Datasheet](Western%20Digital%20FD1771%20-%20Datasheet.md) | WD FD1771 FDC: the five-register model, all 11 commands (Types I–IV) with opcode/flag bits, per-command-type status bits, and the Write-Track control bytes. |
| [Western Digital WD177X-00 - Datasheet](Western%20Digital%20WD177X-00%20-%20Datasheet.md) | WD1770/72/73 FDC: register set, Type I–IV commands with hex opcodes, per-type status bits, FM/MFM track formats. **Not the Tarbell's chip** — see the trap in `docs/sources.md`. |
| [Cromemco 4FDC / 16FDC / 64FDC](Cromemco%204FDC%2016FDC%2064FDC%20Floppy%20Controllers.md) | The Cromemco floppy-disk-controller family documented together with an up-front **differences table**: a WD **FD177x** core (4FDC = FD1771, single-density FM only; 16FDC/64FDC = FD1793, single **and** double density + PLL data separator; 64FDC adds write precompensation) wrapped with an onboard **TMS 5501** serial console (fixed ports `00`–`09`, the same register model as the [TU-ART](Cromemco%20TU-ART.md)) and an **RDOS** boot PROM at `C000`. The **fixed, non-relocatable** port block (`00`–`09` serial/timers, `30`–`34` disk, `40` bank-select), the port-`34` disk-flags/control register (the **Auto-Wait** CPU-stall transfer path; Double-Density bit on 16/64 only), the board-specific port-`04` PerSci drive-mechanics register (which shrinks across the three boards), the `OUT 40H` ROM-disable, and the **16FDC-only** 512 ms RTC and Z80-Mode-2 interrupt jumpers. |
| [CompuPro Disk 1 / Disk 1A](CompuPro%20Disk%201%20%26%201A%20Floppy%20Controllers.md) | The two CompuPro/Godbout floppy controllers documented together with an up-front **differences table**: a common **NEC µPD765A / Intel 8272** FDC + **24-bit arbitrated DMA** (crosses 64 K) + digital data separator + a `PHANTOM*` **boot EPROM** at the CPU reset address, running 8″ **and** 5.25″, single **and** double density. Four ports at a multiple-of-four base (default `C0H`): port 0 IN = FDC status, port 1 = FDC data, port 2 IN = board status / OUT = 24-bit DMA address (three writes, MSB first). The boards diverge at port 0 OUT and port 3: **Disk 1** (1981) has a **software bit-banged serial startup port** (port 3, D7, MARKING/SPACING inverted) and 8 boot routines (Z80/8085, 8086/87); **Disk 1A** (1984) drops the serial port for a **Drive Select Register** (port 0 OUT: unit / 8″-5.25″ data-rate / Force-Two-Sided / head-load) and a **Motor Control Register** (port 3: 4×8″ + 1×5.25″ motor bits, ~15 s auto-off), with 16 boot routines across four CPU families (adds 68000, 32016). Boot-disable = write D0=0 to port 3 on both. Records the inverted base-address switch, the per-board switch-identity shifts, and the WD-1791-`00`-byte cross-controller format warning. FDC internals defer to the µPD765A/8272 data sheet (reprinted in the Disk 1 manual). |
| [North Star MDS Floppy Controllers](North%20Star%20MDS%20Floppy%20Controllers.md) | The North Star Micro-Disk System — **MDS-A** (single density, FM) and its **MDS-A-D** double-density successor (adds MFM), documented together with an up-front **differences table**. A 5¼″ hard-sectored (10-sector) Shugart SA-400, 35 tracks, onboard **256-byte bootstrap PROM**, 4 MHz÷2 = 2 MHz clock — and the trait that makes it unlike every other floppy controller here: a **memory-mapped command interface**. The board is not an `IN`/`OUT` device; it occupies a **1 K memory window** (standard origin **`E800H`**) and **every command is a memory *read*** whose address bits carry the command/data, with transfer by **CPU wait-state (`PRDY`)** — never DMA, never interrupts. Authoritative for: the address-**case** decode (which **differs between the two boards** — 0/1 PROM · 2 write · 3 command on the SD; 0 PROM · 1 write · 2 "Controller Orders" · 3 command on the DD), the Case-3 command bytes and command codes, the **two** (SD: A/B) vs **three** (DD: A/B/C) status bytes with full bit layouts, the drive-select difference (binary `M1M0` vs one-hot `DS`), the sector format (SD 16+`FB`+256+chk = 274 B; DD 32+`FB FB`+512+chk = 547 B), the **rotate-left-XOR check byte (not a CRC)**, and the 300 RPM / 20 ms-sector timing. **⚠ Records the SD manual's "1 second (i.e. 5 sector times)" spin-up slip** (5 sector-times = 100 ms; 1 s ≈ 5 *revolutions*) and the SD errata folding Case 0 into Case 1. |
| [SD Systems VersaFloppy](SD%20Systems%20VersaFloppy.md) | SD Systems VersaFloppy I (FD1771, single density) and VersaFloppy II (FD1791, single+double density) floppy controllers, documented together: the 8-port block at `60`–`67h` (decoded on A0–A7; functional ports `63`–`67`), the board control/status register at `63h` (**different bit layout on VF-I vs VF-II** — drive-select/side/restore/density/wait-enable), the FD177x registers at `64`–`67h`, the **CPU wait-state (PRDY) data transfer on port `67h`** (no DRQ polling), the FD1771-vs-FD1791 command codes and the driver error-status byte, and the 8″/5″ single/double geometries (from the DDBIOS density tables). No onboard boot PROM — DDBIOS lives at `F000h`. |
| [iCOM FD3712 & FD3812 Floppy Disk Systems](iCOM%20FD3712%20%26%20FD3812%20Floppy%20Disk%20Systems.md) | The two iCOM (Pertec Microsystems) 8″ floppy subsystems documented together with an up-front **differences table**: the single-density **FD3700/FF37** family (two-board **CF3700** controller/formatter, general-µP OEM — 8080/6800/RCA-1800 hookups, Pertec FD511 drives) and its double-density successor the **FD3812/FF38-XX** (single **S-100 board for the MITS 8800**, Pertec FD514 drives). One shared **programmed-I/O** controller model: an eight-line **command word CPU 0–7** (**negative/zero-true**, CPU 0 = strobe), **CDO 0–7** data-out, **DI 0–7** data-in that is **multiplexed by CPU 6** (high → status byte, low → read-buffer byte), two 128-byte sector buffers (read + recirculating write), and a `DONE` pulse. The **14 shared commands** with hex codes (`00`/`03`/`05`/`07`/`09`/`0B`/`0D`/`0F`/`11`/`21`/`31`/`40`/`41`/`81`) + the FD3812-only **Load Configuration `15`** (density + format mode), the shared **status byte** (7 DDAM · 6 media/always-1 · 5 drive-fail · 4 write-protect · 3 CRC · 2–1 unit · 0 busy), the seek/read/write/read-CRC/DDAM/clear sequences, and the SD (128 B/sector) vs DD (256 B/sector) IBM 3740/2D geometry — 77 tracks × 26 sectors, CRC x¹⁶+x¹²+x⁵+1. **⚠ On a DD diskette track 0 stays single density** (why Load Configuration exists); **⚠ the FD3812 command table misprints Read (`0B`) and Write (`06`)** — the bit columns and the FD3700 both give `03`/`05`. **Not emulated** — reference for possible future S-100 double-density floppy support. |
| [Dual SD card Board](dual-sd-card.md) | The S100Computers **ESP32-S3 microSD** block device (two SD sockets A:/B:), the `dualsd` board. A **two-port programmed-I/O command engine** — status/command `80H`, data `81H` (DIP-relocatable) — driven by a **`33H`-lead two-byte protocol** with 8 commands (`80`/`81` INIT A/B, `82`/`83` SEL A/B, `84` SET_TRK_SEC, `85` READ, `86` WRITE, `87` FORMAT-with-`E5`, `88` RESET). Authoritative for: the **byte handshake** (verbatim `GET_DATA`/`SEND_DATA` — **DI7** high = data ready, cleared by the data read; **status bit 0** high = the last written byte not yet taken), **512-byte sectors**, and the CP/M 3 hard-disk **DPB** (512 B/sector, 61 sectors/track, 256 tracks, 2 KB blocks, 1024 dir entries, 1 reserved track → ~7.63 MB/volume; `LBA = track·256 + sector`, so the `OFF=1` reserved track puts the directory at **LBA 256**, confirmed against the shipped image). Boot is the **MASTER Z80 monitor `P`/`I` commands** (read 12 sectors from track 0 → RAM `100H` → jump); the board carries **no boot PROM**. **Resolved (from obtained source):** the `SET_TRK_SEC` byte order and `track·256 + sector` LBA formula, the `0xFF` erased-sector fill (`0xE5` is only what FORMAT writes), the MASTER V6.6 `I`-command direct-SD boot, and that `.imgc` is an HDD-Raw-Copy container — all pinned in `dual-sd-card.md` from the ESP32 firmware, `SD3.ASM`/`HBOOT3.ASM`, and the shipped `examples/dualidesd` card. |
| [S100Computers IDE-AB (CompactFlash) Board](dual-ide-card.md) | The **8255-based IDE/CompactFlash half** of the S100Computers "IDE-AB CF+ESP32" combination board (CP/M drives **A:/B:**), the `dualide` board; the SD half is the [Dual SD card Board](dual-sd-card.md) `dualsd` (drives C:/D:). Five 8255 ports (data A/B `30`/`31`, control C `32`, mode config `33`, drive-select `34`), the IDE control-line encoding (`A0=01 A1=02 A2=04 CS0=08 CS1=10 WR=20 RD=40 RST=80`) and the ATA register file (`REGdata=08 … REGcommand/REGstatus=0F`), the strobe-based register read/write (`IDEwr8D`/`IDErd8D`) and 16-bit sector loops (low byte on port A, high on B), the status polls (`IDEwaitnotbusy` `(st&0xC0)==0x40`, `IDEwaitdrq` `(st&0x88)==0x08`, ERR bit 0), the commands (`READ=20 WRITE=30 RECAL=10 INIT=91 SPINDOWN=E0 SPINUP=E1 IDENTIFY=EC`), and the LBA math (`REGsector | REGcylLSB<<8 | REGcylMSB<<16 | (REGshd&0x0F)<<24`, byte offset `LBA·512`). **Proves a card is portable between the two halves:** the CF `IDEHD$DPB` and SD `SD$DPB` are the identical `DPB 512,61,256,2048,1024,1,8000H` with the same LBA and byte order, so a `.img`/`.geo` card boots on either. Boot is the **MASTER Z80 monitor `P` command** (`HBOOTCPM`, 12 sectors from LBA 1 → `100H` → jump); the board has **no boot PROM**. From `HIDE3.ASM`/`HBOOT3.ASM`/`HDRVTBL3.ASM` (John Monahan, 1/24/2026). |

## File formats

| Reference | What it covers |
|---|---|
| [Intel Hexadecimal Object File Format](hexfrmt.md) | The Intel HEX specification (Rev A, 1988): the general record layout, all six record types, and the checksum rule (a record sums to zero). Plus what an 8-bit machine actually needs — types 00 and 01 — the modulo-64K address wrap that `LOAD ... AT` relies on, and the traps: RECLEN counts data bytes not characters, a file is sparse and its gaps must not be filled, and its records need not ascend. |

## Software toolchain

Not hardware — Microsoft's CP/M assembler and linker, kept for the **symbol files the
debugger loads**. The toolchain is `M80 → .REL → L80 → .COM/.HEX (+ optional .SYM)`; see the
Symbols section of `docs/manual/debugging.md` and `src/core/symbols.{h,cpp}`.

| Reference | What it covers |
|---|---|
| [Microsoft M80 Assembler](Microsoft%20M80%20Assembler.md) | MACRO-80: command string and default extensions (`.MAC`/`.REL`/`.PRN`/`.CRF`), the `.PRN` listing format the symbol loader reads — the per-line relocation markers (`'`/`"`/`!`) and the symbol-table section that flags every symbol's type (`U`/`C`/`*`/`'`/`"`/`!` and Public `I`) — plus the symbol rules (6 significant chars, `##`/`::`), the **decimal** default radix, and the switch/pseudo-op set. **M80 emits `.PRN`, not `.SYM`.** |
| [Microsoft L80 Linker](Microsoft%20L80%20Linker.md) | LINK-80: the command string and `.REL` default, the switches that decide the output — `/N` (name, `.COM`), `/E`/`/G` (exit/go), `/X` (`.HEX`), and **`/Y` (write `filename.SYM`)** — the 100H startup-`JMP` trap, and the key fact that a Microsoft `.SYM` holds **globals only**, which is why it is a flat name=value list with no EQU/label split. |
| [SD Systems Editor / Assembler / Linker](SD%20Systems%20Editor%20Assembler%20Linker.md) | The SD Systems Z80 development toolchain — the tools that built the SD ROMs: the **Editor** (`EDIT`) command set; the **assembler** `ZASM` ("Z80 Global Assembler") invocation, options (`/C`, `/S`, …) and two-pass listing format; the **Z80 assembly language** it accepts (radix suffixes `H`/`Q`/`B`/`D`, the expression-hierarchy and `.AND.`/`.SHR.`-style operators, and the pseudo-ops — `DEFB`/`DEFW`/`DEFS`/`DEFM`, `EQU`/`DEFL`, `ORG`, `IF`/`ENDIF`, `INCLUDE`, `PSECT REL`/`ABS`, and the single `GLOBAL` that plays both `PUBLIC` and `EXTRN`); the relocatable `.OBJ` module model; and the **linker** `LINK` (`/A=hhhh` origin, load map) that emits the Intel-`.HEX` an SD ROM is burned from — the same hex `loadHex` reads. |
| [SOLOS / CUTER](SOLOS.md) | The Sol-20's personality-module monitor (and its non-Sol VDM sibling CUTER): the five console commands (incl. **`ENTR`, which is the `.ENT` file loader** — `.ENT` software is console text, not tape), the four tape commands and ten `SET` commands, the input/output **pseudo-ports** (0 keyboard/VDM … 3 user), the machine-language interface — register conventions on entry (`SP` in the SOLOS stack, `HL` at the jump-table base), the **`C000` jump table** (`START`/`INIT`/`RETRN`/`FOPEN`…`SINP`/`AINP` with the zero-flag status convention), the **VDM display driver** control codes + `1B` escape sequences (cursor addressing, speed), and the cassette **file-header** layout with its auto-execute type bit. The software companion to `Sol-20.md`. |
| [CP/M 2.2 Manual](CPM%202.2%20Manual.md) | The DRI compilation altairsim runs — distilled to the parts we touch: the **page-zero contract** (0000 warm-boot, 0005 BDOS, 005C FCB, 0080 DMA, 0100 TPA), the **CCP** built-ins + line-editing control chars, **`ASM`** (directives, the **decimal** default radix, 16-significant-char identifiers, and the 16-column `.PRN` geometry with its `=` EQU marker that the symbol loader reads), **`DDT`** (the period debugger our own mirrors — `X`/`T`/`G`/`L`/`D`/`A`/`S`, the `CfZfMfEfIf A=…` register line, and RST-7 breakpoints), and the **BDOS** call convention + function numbers. |
| [Digital Research SID / ZSID](Digital%20Research%20SID%20and%20ZSID.md) | DRI's **Symbolic Instruction Debugger** — **SID** (8080) and the identical **ZSID** (Z80) — the successors to `DDT` that our own monitor/debugger deliberately mirrors, so this is the period statement of what that command family means. The full command set (`A C D F G H I L M P R S T U X`), the symbolic-expression grammar (`.s`/`@s`/`=s`, qualified `s1/s2`, `#`decimal / `'c'` character / `^` stack literals), the two breakpoint kinds — a temporary `G` breakpoint that stops **before** the instruction vs a **pass point** (count 1–FF) that stops **after** — and RST-7 as the external break, the monitored `T`/`U` single-step (with `TW` trace-without-call, and the stop-at-BDOS/run-ROM-in-real-time rules), the `CZMEI A=… B=… D=… H=… S=… P=…` register line and `X` state-editor altairsim's `REGS` descends from, the `MAC`/`RMAC` `.SYM` format, the `.UTL` utilities (`HIST` histogram, `TRACE` backtrace), and the SID-vs-ZSID differences (Intel vs Zilog mnemonics; the `-A` size trade). |
| [iCOM Macro Assembler](iCOM%20Macro%20Assembler.md) | iCOM's two-pass **8080 Macro Assembler** (Aug 1976) under FDOS-II: the `ASMB,source,object,PASS` invocation (with the PASS-value table), statement/field/label rules, the nine operand data types and built-in register names, the expression operator set with precedence, the `DB`/`DW`/`DS` + `ORG`/`EQU`/`SET`/`END`/`IF`/`MACRO` directives, the macro facility (local/global labels, parameter substitution, delayed evaluation), the listing/object output format, and the single-letter error-code table. Cover-sheet errata folded in. |
| [iCOM Text Editor (Rev A)](iCOM%20Text%20Editor.md) | The iCOM 8080 **Text Editor**, Revision A (Jan 1977) — a TECO-family character/line editor run under FDOS-II as `T,INPUT,OUTPUT`: the in-memory workspace and buffer-pointer model, the single-letter command set (`A`/`I`/`B`/`L`/`M`/`Z`/`S`/`C`/`D`/`K`/`P`/`T`/`E`) with the `$$` ESCAPE terminator and `n`-argument rules, command strings/iteration/tabs, and the three error signals (`CANNOT FIND`, BELL, `ITERATION STACK FAULT`). |
| [iCOM Text Editor (Rev B)](iCOM%20Text%20Editor%20Rev.B.md) | The later **Revision B** (June 1977) of the same editor — now invoked as `EDIT,INPUT,OUTPUT` (vs Rev A's `T,…`): the same `@`-prompt buffer model and 13 single-letter commands, command chaining/iteration/tabs, and the three status messages. The manual carries no changelog, so the A→B delta beyond the invocation keyword is not stated. |
| [FDOS-III Relocating Assembler](FDOS-III%20Relocating%20Assembler.md) | iCOM's **relocatable** 8080/Z80 assembly system under FDOS-III — `ASMB`/`LINK`/`LIB`/`RDBFL`: the option switches, the relocation/structuring/data/conditional/macro/listing directives, the external/global symbol model, the binary-relocatable + library + Intel-Hex-ASCII object formats (record types, relocation-mode bits, checksums), the linker/library directive set, and the 0–43 error-code table. Opcode reference and appendices noted but not transcribed. |
| [iCOM BASIC-M (DEBBI)](iCOM%20BASIC-M%20%28DEBBI%29.md) | iCOM's disk-resident **BASIC-M** interpreter (informally "DEBBI", Disk Extended BASIC by iCOM; Dec 1976) for FDOS-M/FDOS-II: the `RUNGO,BASIC[,"in"][,"out"]` invocation (and the `DEBBI,PROGA,PROGB` shorthand), the `DLOAD`/`DSAVE` disk-file mechanism, the BCD 6-digit numeric model, and tabulated command/statement/function/operator/error-code sets. Two source discrepancies flagged (GOSUB nest 5 vs 6; `TAN` only in the appendix index). |

## System software and monitors

| Reference | What it covers |
|---|---|
| [SD Systems SD / MS Monitor](SD%20Systems%20Monitor.md) | The SD Systems boot/debug PROM monitor (v2.10) for the SBC-100/200 at `E000h`: the `.` prompt (no banner, hex-only operands), the full command set (`D`/`E`/`F`/`M`/`L`/`T`/`V` memory, `I`/`O`/`P` port, `B`/`G`/`S`/`X`/`H` program-control, `C`/`R`/`W`/`Z`/`Q` disk with the format-type codes), the Z80 register-save map at `FFE6`–`FFFFh`, the 3-byte-`JMP` breakpoint model, and the console/auto-baud difference between the two ROM builds (**MSMONR21** → 8251 at `7C`/`7D`; **SDMONV21** → console at `01`/`00`). Boots [SDOS](SD%20Systems%20SDOS.md) via the [VersaFloppy](SD%20Systems%20VersaFloppy.md) DDBIOS. |
| [SD Systems SDOS](SD%20Systems%20SDOS.md) | SD Systems' CP/M-compatible disk OS (and its COSMOS sibling) for the SBC-100/200 + VersaFloppy: the page-zero contract (warm boot `0000`, `CALL 5` system-call vector, FCB `005C`, DMA buffer `0080`, illegal-address trap `0038`), the BDOS-numbered system-call table (0–27 as CP/M plus SD extensions 128/129/151), the resident-in-top-7 KB memory map with the overlayable CONPROC, the 128-byte-sector 8″/5″ geometries, and the cold-boot flow (BIOS at `F000` reads T0/S1 → `0080`, auto-identifies format). |
| [SD Systems COSMOS](SD%20Systems%20COSMOS.md) | SD Systems' CP/M-2.x-compatible **multi-user** OS (up to 8 users) for the SBC-200 family — software the Z-80 boots, not itself emulated. The guest-side story behind the **ExpandoRAM II banking**: **page-mode**, writing a page number to **port FF** to select a 32K/48K partition; **page 0 = 64K with resident COSMOS in the top 16K/32K** (re-entrant single copy) and the user-0/console overlay below; the two memory systems (3 boards→192K / 4 boards→256K, each board two stacked partitions). Also the ExpandoRAM II config COSMOS requires (jumper E9→E10, PROM EX-32/EX-48 per partition size, S3 board-select per Table 6-1 — none yet modeled by `bankmem card=expandoram2`), the CP/M-2.x page-zero map, the COSMOS syscall extensions (Table 5-6: 128–149 SD calls, 192–200 multi-user), the boot/`MCOSMOS.CMD` flow, and the SD disk-config table. ⚠ CP/M **2.x**, not CP/M 3. |
| [Cromemco CDOS](Cromemco%20CDOS.md) | Cromemco's own CP/M-1.3-work-alike disk OS for the 4FDC/16FDC/64FDC family: the **RDOS→CDOS boot handoff** (disk-controller switch 3 ON = silent auto-boot, OFF = RDOS's `;` prompt needing `b`+RETURN; up to 4 RETURNs to train console baud), the CP/M-standard page-zero map (`0000` warm boot, `0005` `CALL 5` vector, `0008`=`FFH` under CDOS/`C3H` under Cromix), the self-describing 5-way disk-geometry/type-specifier scheme (track 0/side 0 always forced single-density so RDOS can boot it), the FCB/directory-entry/disk-label byte layouts, and the full system-call table (0–27 CP/M-identical + 128–159 CDOS-only extensions: disk labels, multiply/divide, program chaining, a real-time clock). |
| [iCOM FDOS-II](FDOS-II%20Manual.md) | iCOM's second-generation **8080 floppy disk operating system** (Rev. Oct 1976) for the FD3712/FD3812 (FD360) hardware: the module/memory map (both the Altair `C000H` and MDS `E800H` builds), resident entry points and the user-supplied device-vector `JMP` table, the mini-monitor commands, the disk/file format (77 tracks × 26 sectors × 128 bytes, track-0 directory, 11-byte FCB, ≤253 files), the `RI`/`WRT` byte-I/O syscalls with the 3:1 logical→physical sector interleave, the 22 directives (incl. `RUNGO`/`RUN`), and the error codes. Sits between EDOS and FDOS-III. |
| [iCOM FDOS-III](FDOS-III%20Manual.md) | The most advanced iCOM **8080 disk OS** (Sept 1977): the per-host boot/load table, the `-58` I/O-vector init, the RAM map and entry vectors, the disk/file format (26 × 128 sectors, track-0 directory, 11-byte FCB, 253-file limit), the full command set (24 commands + `RUNGO`), the SYSGN field table, the `RI`/`WRT` byte-I/O plus Executive/`DKHN` syscall interface, the logical→physical sector-skew table, and all error codes (Executive, resident, DKHN, diagnostic). |
| [EDOS — Motorola EXORdisk DOS (M6800)](EDOS-I%20Manual.md) | **⚠ Not iCOM's 8080 "EDOS-I" the filename implies** — this is **Motorola's EDOS** (EXORciser Floppy Disk Operating System, internally "FDOS" v2.0, 4/9/75) for the **M6800** under EXbug, driving the EXORdisk. The iCOM link is the drive: the bundled diagnostic is headed *iCOM FD360-X-68*, an iCOM FD360 rebadged with a 6800 (`-68`) interface. Useful mainly for the shared iCOM **FD360 disk format** (77 × 26 × 128) and the logical→physical sector skew. Also covers the EXbug boot (`E800;G` → `!`), the directive set, the 7-file/9-track layout, error codes E1–E3, the zero-page driver interface, and the `E800`–`E824` entry-vector table. |
