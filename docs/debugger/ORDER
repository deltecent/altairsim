# The Debugger, in order.
#
# This is a SEPARATE document from the User Manual (docs/manual/), built by the same
# tools/build-docs.sh into altairsim-debugger.pdf and shipped beside the manual in the
# package. It was a manual chapter once (docs/manual/debugging.md); it moved out with the
# monitor because looking at the machine -- breakpoints, stepping, disassembly, the bus
# itself -- is most of why altairsim exists, and it reads better as its own document than as
# the longest chapter in a book about the emulated hardware.
#
# One chapter, so the sequence is trivial; ORDER still declares it, because build-docs.sh
# reads the chapter list from here and from nowhere else. Add a file here only if this
# document genuinely grows past the one page it opens with.

debugging.md
