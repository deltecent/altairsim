# Built-in ROMs

The common ROMs are **compiled into the simulator**. There is no portable place to keep ROM images across macOS, Linux, and Windows — `/usr/share`, `~/Library`, `%APPDATA%`, and "next to the binary" are four different answers and every one of them becomes a support question. ROMs are small, so they are embedded and the question disappears: a fresh checkout boots on any OS with nothing to download.

Use one from a `memory` board region:

```toml
  [[board.region]]
  type  = "rom"
  at    = 0xFF00
  mount = "builtin:dbl"
```

`SHOW ROMS` lists what is compiled in. A bare path (`mount = "roms/mine.bin"`) loads a host file instead — **built-ins are a convenience, never a lock-in.**

## The provenance rule

`DESIGN.md` §0.1 says hardware facts come from period manuals and first-hand artifacts, never from another emulator. **A ROM image is a hardware fact**, and an embedded blob with no recorded source is exactly the second-hand fact that rule exists to prevent — worse than a wrong bit in a document, because every piece of software above it would then be debugged against the wrong ground truth, and it would look like a software bug for a very long time.

So **every built-in ROM has a row in the table below**, and no ROM is embedded without one:

| Field | Why |
|---|---|
| **Source** | A specific dump, part, or listing. "From the internet" is not a source. |
| **Size** | Exact bytes. A ROM padded to the next power of two is a *different artifact*. |
| **CRC32** | Verified by a unit test at build time, so a corrupted embed **fails the build**, not a user. |
| **Verified against** | The listing, manual, or hardware the dump was checked against — if it was. Say so honestly when it wasn't. |

## The ROMs

| `builtin:` name | Part | Size | CRC32 | Source | Verified against |
|---|---|---|---|---|---|
| `dbl` | Altair Disk Boot Loader 4.1 | **256 B** (`FF00`–`FFFF`) | **`8E658905`** | `roms/DBL/` — `DBL.ASM` / `DBL.HEX` / `DBL.PRN`. Disassembled by **Martin Eberhard, 4 March 2012**, from an EPROM labeled "DBL 4.1" found socketed in a **MITS Turnkey board**. | `DBL.PRN` listing, **all 256 bytes**, byte-for-byte. |
| `mdbl` | Altair **Mini**disk Boot Loader | **256 B** (`FF00`–`FFFF`) | **`3BC20ADD`** | `roms/MDBL/` — `MDBL.ASM` / `MDBL.HEX` / `MDBL.PRN`. Disassembled by **M. Eberhard, 5 May 2014**, from MITS EPROMs. Fetched 2026-07-13 from deramp.com (`.../altair/software/roms/orginal_roms/`). | Its own `MDBL.PRN` listing. **And it boots**: `altairsim minidisk` loads CP/M 2.2b off a real period `.DSK` through this PROM and reaches `A>`. |
| `mbl` | Altair **Multi Boot Loader** — paper/cassette tape bootstrap | **256 B** (`FE00`–`FEFF`) | **`5E21410E`** | `roms/MBL/` — `MBL.ASM` / `MBL.HEX` / `MBL.PRN`. Reverse-engineered and commented by **Geoff Harrison** from a hex dump provided by **Grant Stockly**. A ROM tape loader that reads *any* MITS-designed paper/cassette tape format: it builds a byte-read routine in RAM for the reader board selected on the front-panel switches (A11–A8: 2SIO/SIO/ACR/4PIO/PIO/HSR), skips the tape's own stage-2 loader, then loads and checksums the payload and jumps to its entry point. Fetched 2026-07-24 from deramp.com (`.../altair/software/roms/orginal_roms/`). Not to be confused with `mdbl`, the **mini**disk loader. | Its own `MBL.PRN` listing, byte-for-byte. |
| `altmon` | ALTMON 1.3 — 1K Altair monitor | **1013 B** (`F800`–`FBF4`) | **`705203BE`** | `roms/ALTMON/` — `ALTMON.ASM` / `ALTMON.HEX` / `ALTMON.PRN`. **Mike Douglas**, 2016–2024, based on the Vector Graphic 2.0C monitor, reworked to drive an **88-2SIO**. | Its own `ALTMON.ASM` source, which is in the repository. **And it runs**: `altairsim altmon` prints its banner, takes commands, and dumps memory. |
| `cdbl` | CDBL 3.00 — Combo Disk Boot Loader | **245 B** (`FF00`–`FFF4`) | **`0558293E`** | `roms/CDBL/` — `CDBL.ASM` / `CDBL.HEX` / `CDBL.PRN` + manual. **Martin Eberhard & Mike Douglas**, v3.00, 16 Jan 2016. Boots an 88-DCDD 8″ floppy *or* an 88-MDS minidisk, auto-detecting the drive; the single PROM that supersedes `dbl` and `mdbl`. Fetched from deramp.com (*M. Eberhard Improved ROMs*). | Its own `CDBL.PRN` listing, byte-for-byte, and the `CDBL Manual.pdf` retained beside it. |
| `hdbl` | HDBL 2.00 — Hard Disk Boot Loader | **255 B** (`FC00`–`FCFE`) | **`796FCA9B`** | `roms/HDBL/` — `HDBL.ASM` / `HDBL.HEX` / `HDBL.PRN` + manual. **Martin Eberhard**, v2.00, 13 Aug 2014. Boots an 88-HDSK Pertec hard disk; boot platter chosen by Sense Switch 3. Fetched from deramp.com (*M. Eberhard Improved ROMs*). | Its own `HDBL.PRN` listing and `HDBL Manual.pdf`. |
| `amon` | AMON 3.0 — full-featured ROM monitor | **F000–FFFE** (4 KB span; **3807 B** programmed, gaps read `FF`) | **`C00DC413`** (over the `FF`-filled span) | `roms/AMON/` — `AMON.ASM` / `AMON.HEX` / `AMON.PRN` + manual. **Martin Eberhard**, v3.0, 12 Feb 2023 (later fixes by **D. Hansel**). Memory/transfer/EPROM-programming monitor with built-in floppy/minidisk/HDSK/tape boot entry points; console on an 88-2SIO port 0, cold start `F800h`. Fetched from deramp.com (Eberhard firmware archive). | Its own `AMON.PRN` listing and `AMON Users Manual.pdf`. `AMON.HEX` ends in Ctrl-Z (CP/M soft-EOF) padding, which `loadHex` treats as end-of-file. |
| `acuter` | ACUTER 1.0 — CUTER for the Altair | **2048 B** (`F000`–`F7FF`) | **`4A4E608D`** | `roms/ACUTER/` — `ACUTER.ASM` / `ACUTER.HEX` / `ACUTER.PRN` + manual. **Mike Douglas**, 25 Apr 2015, from Processor Technology's **CUTER 1.3** (© 1977 Software Technology Corp.); console on the 1st 2SIO port, tape via the ACR, `HGET`/`HSAVE` Intel-HEX commands added. Fetched from deramp.com (*CUTER for Altair*). | Its own `ACUTER.PRN` listing and the retained `CUTER Manual.pdf`. |
| `cuter` | CUTER 1.3 — the original Processor Technology monitor | **2043 B** (`C000`–`C7FA`) | **`B0106ED2`** | `roms/CUTER/` — `CUTER13.ASM` / `CUTER13.HEX` / `CUTER13.PRN` + manual. **Software Technology Corp., 1977** (version 1.3, 77-03-27; source restored by J. Bowman from an *Access* newsletter listing). The stand-alone CUTS monitor whose **built-in VDM-1 driver** writes the screen at `CC00h` through control port `C8h` — console = VDM out / keyboard in on cold start. Fetched from deramp.com (*Sol-20 software*). | Its own `CUTER13.PRN` listing, byte-for-byte, and the retained `CUTER-SOLOS Manual.pdf`. **And it runs**: `altairsim cuter` cold-starts it and its `>` prompt appears on the VDM-1 ([`machines/cuter.toml`](../machines/cuter.toml)). |
| `solos` | SOLOS 1.3 — the Sol-20 stand-alone operating system | **2048 B** (`C000`–`C7FF`) | **`4D0AF383`** | `roms/SOLOS/` — `SOLOS13.ASM` / `SOLOS13.HEX` / `SOLOS13.PRN` + manual. **Processor Technology Corp., 1977** (version 1.3, 77-03-27; the same revision as the "SOLOS 4-1" EPROM set). The Sol-PC's own OS: keyboard, VDM-1 video, serial, parallel and CUTS cassette at the fixed hardware ports `F8h`–`FFh` (see `roms/SOLOS/README.md` and `docs/boards/proctech-sol.md`). Fetched from deramp.com (*Sol-20 software*). | Its own `SOLOS13.PRN` listing, byte-for-byte, and the retained `SOLOS-CUTER Manual.pdf`. **And it runs**: `altairsim sol20` cold-starts it and its sign-on and `>` prompt appear on the VDM-1 ([`machines/sol20.toml`](../machines/sol20.toml)). |
| `ddb200` | SD Systems VersaFloppy disk BIOS (DDBIOS 3.3) | **2042 B** (`F000`–`F7F9`) | **`D94C68DC`** | `roms/DDB200/` — `DDB200.ASM` / `DDB200.HEX` / `DDB200.PRN`. **Rex Brown**, DDBIOS v3.3, 24 Jun 1982 — the SDOS-COSMOS floppy driver for an SBC-100/200 with a VersaFloppy II controller; loads at `F000h`, reads T0/S1 → `0080h`, auto-IDs the format and boots the OS resident. From the SD Systems archive (VersaFloppy manuals on deramp.com); hardware detail distilled in `reference/SD Systems VersaFloppy.md`. | Its own `DDB200.PRN` listing, byte-for-byte. **Verified by listing only — not yet executed**: the SBC/VersaFloppy boards are still to be built. |
| `msmonr21` | SD Systems SBC MS Monitor 2.10 | **2048 B** (`E000`–`E7FF`) | **`64428921`** | `roms/MSMONR21/` — `MSMONR21.Z80` / `MSMONR21.HEX` / `MSMONR21.LST`. **SD Systems**, MS monitor v2.10 — the SBC-100/200 boot/debug PROM with an **8251 console at `7C/7D`** and Z80-CTC auto-baud (`78`–`7B`); memory/port/program commands plus a disk boot (`C`) that runs SDOS/COSMOS via the VersaFloppy DDBIOS. From the SD Systems archive (the *SD Systems Monitor* manual on deramp.com); command set distilled in `reference/SD Systems Monitor.md`. | Its own `MSMONR21.LST` listing, byte-for-byte. **Verified by listing only — not yet executed**: the SBC-100/200 board is still to be built. |
| `sdmonv21` | SD Systems SBC SD Monitor 2.10 | **2048 B** (`E000`–`E7FF`) | **`4B101B4B`** | `roms/SDMONV21/` — `SDMONV21.Z80` / `SDMONV21.HEX` / `SDMONV21.LST`. **SD Systems**, SD monitor v2.10 — the same SBC-100/200 boot/debug PROM as `msmonr21`, but built for the **SD Systems VDB-8024 video console** (an intelligent terminal presented as two I/O ports: `00h` status, `01h` keyboard-in/display-out) rather than the 8251 serial port. Identical command set. From the SD Systems archive (the *SD Systems Monitor* and *VDB-8024* manuals on deramp.com); console distilled in `reference/SD Systems VDB-8024.md`. | Its own `SDMONV21.LST` listing, byte-for-byte. **Verified by listing only — not yet executed**: the SBC-100/200 board and the VDB-8024 console are still to be built. |
| `tarbell-sd` | Tarbell floppy controller boot PROM | **32 B** (`0000`–`001F`) | **`55E3446D`** | `roms/TARBELL-SD/` — `TARPROM.ASM` / `TARPROM.HEX`. The **Tarbell Electronics** single-density floppy interface (#1011, 1977) shipped a 32-byte bootstrap PROM that shadows low memory over PHANTOM\*: it homes the head, reads **track 0, sector 1 into `0000h`** through the FD1771 with a wait-state (PRDY) transfer, then jumps to `07Dh` — an address with A5 set, which combinationally releases the shadow and drops into the cold loader it just read. The same PROM boots the double-density card (#2022). Distilled in `reference/Tarbell_Floppy_Disk_Interface_Manual.md`. | Its own `TARPROM.ASM` source, which is in the repository — the 32 bytes are the whole part. **And it boots**: `altairsim tarbell` / `altairsim tarbelldd` load CP/M 2.2 off the tracked disks and reach `A>` through this PROM. |
| `icom-fd3712-cpm` | iCOM FD3712 CP/M 2.2 boot PROM | **1024 B** (`F000`–`F3FF`) | **`82361A3B`** | `roms/ICOM-FD3712-CPM/` — `FD3712-CPM.HEX`. The **iCOM FD3712** 8″ single-density S-100 interface's CP/M loader at `F000` (`JMP F073`): it drives the C0/C1 command engine, reads the cold loader off track 0, and drops into CP/M 2.2's CBIOS (console on an 88-2SIO at port `10`). **Mike Douglas** disassembly, from deramp.com (*iCOM Floppy Systems*); hardware distilled in `reference/iCOM FD3712 & FD3812 Floppy Disk Systems.md`. | Its own `FD3712-CPM.HEX`, byte-for-byte. **And it boots**: `altairsim icom` loads `48K CP/M 2.2 v1.0 for iCOM FD3712 and Altair` off the tracked disk and reaches `A>` through this PROM ([`machines/icom.toml`](../machines/icom.toml)). |
| `icom-fd3712-fdos` | iCOM FDOS/EDOS bootstrap + mini-monitor | **1024 B** (`C000`–`C3FF`) | **`47140DCC`** | `roms/ICOM-FD3712-FDOS/` — `FD3712-FDOS.HEX`. The FD3712's other boot PROM, at `C000` (`JMP C015`): iCOM's own **FDOS** bootstrap and mini-monitor, which loads the RAM-resident FDOS Executive off the system tracks. Carries the resident disk driver's C0/C1 handshake and the console/disk I/O vectors (scratch RAM at `C400`). **Mike Douglas** disassembly, from deramp.com; distilled in `reference/FDOS-II Manual.md` / `reference/FDOS-III Manual.md`. | Its own `FD3712-FDOS.HEX`, byte-for-byte. **And it boots**: `altairsim icom examples/icom/fdos-iii.toml`-style config cold-starts at `C000` and FDOS-III signs on to its `!` prompt, `LIST` reads the directory. |
| `icom-fd3812-cpm` | iCOM FD3812 double-density CP/M 2.2 boot PROM | **1010 B** (`F000`–`F3F1`) | **`7FCFCA76`** | `roms/ICOM-FD3812-CPM/` — `FD3812-CPM.HEX`. The **FD3812** double-density card's Lifeboat CP/M loader at `F000` (`JMP F046`): same C0/C1 engine as the FD3712, but the disk is mixed density — a single-density track 0 then double-density tracks 1–76 (256-byte sectors). Not a full 1K part. **Mike Douglas** disassembly, from deramp.com; distilled in `reference/iCOM FD3712 & FD3812 Floppy Disk Systems.md`. | Its own `FD3812-CPM.HEX`, byte-for-byte. **And it boots**: `altairsim icom examples/icom/cpm22-3812.toml` loads `48K CP/M 2.23 / iCOM 3812 Double Density Floppy / Copyright 1980, Lifeboat Associates` and reaches `A>` — exercising the double-density read path. |
| `rombasic` | MITS Extended ROM BASIC 16K | **16384 B** (`C000`–`FFFF`) | **`643FE88B`** | `roms/ROMBASIC/` — `ROMBASIC.HEX` (the full C000–FFFF image) + the vendor distribution retained in `roms/ROMBASIC/dist/` (`EBROM1`–`EBROM8` / `EBROM-C0`–`EBROM-F8`, the eight 2K PROMs, and their combined `EBROM-ALL.hex`) + `ROM BASIC ReadMe.pdf`. **MITS, ALTAIR ROM BASIC Ver 4.1, © 1977 MITS Inc.** — Extended BASIC that executes in place from eight 2K PROMs, leaving 48K of RAM; the last PROM holds the disk bootloader (`RUN FF00`). | Its own `ROM BASIC ReadMe.pdf` (load map, sense-switch table). **And it runs**: `altairsim rombasic` cold-starts at `C000`, prints `ALTAIR ROM BASIC VER 4.1` / `48101 BYTES FREE` / `OK` on the 88-2SIO console, and evaluates BASIC ([`machines/rombasic.toml`](../machines/rombasic.toml)). |
| `vdm1demo` | VDM-1 banner demo — **ours, not a period ROM** | **52 B** (`F800`–`F833`) | **`CF2C4986`** | `roms/VDM1DEMO/` — `VDM1DEMO.ASM` / `VDM1DEMO.HEX`. **Written for this project**, in the same spirit as the Host Bridge utilities: a 52-byte 8080 program that writes `PROCESSOR TECHNOLOGY VDM-1  READY` into the VDM-1's screen RAM at `CC00` and halts. No console, no monitor — the text appears because the CPU *writes memory*, which is the whole claim of a memory-mapped display. | Its own `VDM1DEMO.ASM`, which is in the repository — the source *is* the artifact here, so there is no dump to reconcile. **And it runs**: `altairsim vdm1` executes it at `F800` and the banner appears on the VDM-1 ([`machines/vdm1.toml`](../machines/vdm1.toml)). |
| `rdos252` | Cromemco RDOS 2.52 — 16FDC boot/monitor PROM | **4097 B** (`C000`–`D000`); the board maps the **4 KB** `C000`–`CFFF` 2732 — the final `00` at `D000` is an assembler pad one byte past the EPROM, and the FDC's ROM window is 4 KB, so it is never mapped | **`A006878E`** | `roms/RDOS252/` — `rdos0252.hex` + `rdos252.lis`. **Cromemco, RDOS v2.52, 1982** (the "Resident Disk Operating System" boot/monitor PROM of the 16FDC — the ROM in **Joe's** machine). From the Cromemco members' archive `github.com/cromemcos/m` (`cromemco/code/operating systems/RDOS/02.52/`), files believed public domain per that archive. | Its own `rdos252.lis` assembler listing, retained beside it — which states the ROM "resides at C000H to CFFFH" and matches the image byte-for-byte over that 4 KB. **Not yet executed**: the guest-boot acceptance test is a Phase-1 follow-up. |
| `rdos312` | Cromemco RDOS 3.12 — 64FDC boot/monitor PROM | **8192 B** (`C000`–`DFFF`) — a full **8 KB** part; RDOS 3.12 grew past the 16FDC's 4 KB into the second 4 KB, so the 64FDC's ROM window is 8 KB | **`59EBD5EC`** | `roms/RDOS312/` — `rdos0312.bin` + `rdos0312.lst`. **Cromemco, RDOS v3.12, 1985** — the 64FDC's boot/monitor PROM (the successor to the 16FDC's 2.52). From the Cromemco members' archive `github.com/cromemcos/m` (`cromemco/code/operating systems/RDOS/03.12/`), files believed public domain per that archive; the `.bin` and the repo's `rdos0312.hex` agree byte-for-byte. | Its own `rdos0312.lst` assembler listing, retained beside it (`ORG 0C000H`, first byte `F3 DI`), matching the image byte-for-byte. **Not yet executed**: the guest-boot acceptance test is a Phase-1 follow-up. |
| `mon680` | Altair **680b** PROM Monitor (ACIA v1.0) | **256 B** (`FF00`–`FFFF`) | **`397E717F`** | `roms/MON680/` — `MON680.S19` / `MON680.ASM` / `MON680.LST`. **MITS**, the 680b System Monitor for the onboard 6850 ACIA console — a single 256-byte PROM (PROM 1, the highest) holding the monitor and the reset/interrupt vectors (`reference/Altair 680b Theory of Operation.md` §3). Motorola **6800** code in Motorola **S-record** form, decoded by `loadSrec`. Fetched 2026-08-08 from deramp.com (`.../altair/software/altair_680/PROM Monitor/`). | Its own `MON680.LST` listing, byte-for-byte (the CRC is over the decoded `FF00`–`FFFF` image). **And it boots**: `altairsim altair680` reaches the `.` prompt and examines its own PROM over the onboard ACIA ([`machines/altair680.toml`](../machines/altair680.toml); `tests/acceptance/altair680.exp`). |
| `swimon` | Altair **680b** PROM Monitor (SWI-breakpoint variant) | **256 B** (`FF00`–`FFFF`) | **`2ABE348F`** | `roms/SWIMON/` — `SWIMON.S19` / `SWIMON.ASM` / `SWIMON.LST`. **MITS**, the same 680b monitor as `mon680` customized to vector `SWI` through `$0010` (so the user can install a `JMP $FFEE` there and use SWI breakpoints); same `FF00`–`FFFF` window, a different image, a different CRC. Fetched 2026-08-08 from deramp.com (`.../altair/software/altair_680/PROM Monitor/`). | Its own `SWIMON.LST` listing, byte-for-byte (CRC over the decoded image). Console path identical to `mon680`, which boots. |
| `kcacr` | Altair **680b** KCACR cassette loader/punch PROM | **256 B** (`FD00`–`FDFF`) | **`A89ADB57`** | `roms/KCACR/` — `KCACR.S19` / `KCACR.ASM` / `KCACR.LST`. The optional 680b PROM (socket V, one below the monitor) that loads and dumps memory over the **KCACR** audio-cassette interface in Motorola S-record form: `.J FD00` loads, `.J FD74` punches. Motorola **6800** code (`.S19`, decoded by `loadSrec`) that calls the MON680 console routines, so it runs with `mon680` present; its listing's equates name the `680kcacr` board's registers — `SIOSR $F010`, `SIODR $F011`. Fetched 2026-08-08 from deramp.com (`.../altair/software/altair_680/PROM for KCACR/`). | Its own `KCACR.LST` (Mike Douglas's July-2022 disassembly), byte-for-byte (CRC over the decoded `FD00`–`FDFF` image). Registers match the `680kcacr` board's unit tests (`tests/test_680kcacr.cpp`). |
| `master0` | S100Computers **MASTER V6.6** monitor — V2 Z80 CPU board **LOW** page | **3865 B** programmed (`F000`–`FF18`, contiguous); the board maps it into the full **4 KB** `F000`–`FFFF` EEPROM window, the unprogrammed tail reading `FF` | **`E2276961`** | `roms/MASTER0/` — `MASTER0.HEX` / `MASTER0.z80` / `MASTER0.LST`. **John Monahan (S100Computers)**, MASTER Z80 Monitor **V6.6**, `ORG 0F000H` — the everyday monitor menu of the V2 Z80 CPU board's onboard 28C64 EEPROM (chip offset `0000`–`0FFF`, the "low page"). Supplied by Patrick 2026-08-16; hardware distilled in `reference/v2-z80-cpu-board.md`. | Its own `MASTER0.LST` assembler listing, byte-for-byte over the programmed span. **Not yet executed** as a guest boot — the `v2z80` board's unit tests (`tests/test_v2z80.cpp`) check the decode/paging; the CP/M 3 boot is Task 3a-2 ([`machines/dualsd.toml`](../machines/dualsd.toml)). |
| `master1` | S100Computers **MASTER V6.6** monitor — V2 Z80 CPU board **HIGH** page | **3507 B** programmed (`F000`–`FDB2`, contiguous); mapped into the same **4 KB** `F000`–`FFFF` window, unprogrammed tail reads `FF` | **`70A572ED`** | `roms/MASTER1/` — `MASTER1.HEX` / `MASTER1.z80` / `MASTER1.LST`. The **high page** of the same EEPROM (chip offset `1000`–`1FFF`), selected by `OUT D3H` bit 1 = 1: XModem file transfer, clock/date, and the **`I` command** that boots CP/M 3 from the Dual SD card (`SDBOOTCPM`→`SDCPM`). Supplied by Patrick 2026-08-16; distilled in `reference/v2-z80-cpu-board.md`. | Its own `MASTER1.LST` listing, byte-for-byte over the programmed span. Boot path is [[dual-sd-card]]; guest boot is Task 3a-2. |

> ### `dbl` and `mdbl` are more interchangeable than they look, and finding that out was the point
>
> DBL is the 8″ floppy's bootstrap and MDBL is the minidisk's. Both live at `FF00`, both are 256
> bytes, and both open by copying themselves into RAM — a 1702A EPROM was too slow to execute
> from — differing in where they land and how much they move:
>
> ```
> DBL   FF00  21 13 FF   LXI H,FF13     MDBL  FF00  21 13 FF   LXI H,FF13
>       FF03  11 00 2C   LXI D,2C00           FF03  11 00 4C   LXI D,4C00
>       FF06  0E EB      MVI C,0EBH           FF06  0E E3      MVI C,0E3H
> ```
>
> MDBL relocates to `4C00` rather than `2C00` because **Minidisk BASIC loads low** and would land
> on top of it.
>
> **I expected DBL to be unable to read a minidisk. It reads one perfectly well.** It went into
> `machines/minidisk.toml` as a negative control — an 8″ PROM on a 5.25″ machine, which surely
> hangs — and CP/M booted and listed both drives. Mike Douglas's own `BOOT.ASM`, the loader on
> that very disk, says why in its header: *"This code is loaded from sectors 0 and 2 into RAM by
> the disk boot loader PROM (DBL)."* DBL interleaves 2:1, so it reads sector 0 and then sector 2 —
> exactly where the loader sits — and DBL's `cHDLOAD` arrives at an 88-MDS as `TIMER RESET`, which
> is harmless.
>
> A bootstrap written for one controller runs on the other **because MITS built the 88-MDS to
> present the 88-DCDD's programming model** (`docs/boards/mits-88mds.md`). That is precisely the
> compatibility that let a minidisk hide inside the 8″ controller in this tree for months. So the
> minidisk acceptance test's negative control swaps the **card**, not the PROM — that is the
> variable that actually matters.
>
> Use `mdbl` on a minidisk machine anyway: it is the PROM MITS shipped for that subsystem
> (88-MDS manual §1-3.B), and it is the one that will boot Minidisk BASIC when that lands.

**`altmon` is the one ROM here that is verified by *execution*, not just by listing.** It is not a data structure we assert things about — it is a program, written for real hardware by someone who owns one, and it either works on our 8080 and our 2SIO or it does not. It does:

```
$ altairsim altmon
ALTMON 1.3
*DUMP F800 F80F
F800 3E 03 D3 10 D3 12 3E 11 D3 10 D3 12 31 00 C0 CD  >.....>.....1...
```

Those sixteen bytes are ALTMON's own first sixteen — `MVI A,3 / OUT 10 / OUT 12 / MVI A,11 / OUT 10 / OUT 12` — so this is the ROM reading its own 2SIO initialization back to us *through the card it initializes*, and it agrees with `ALTMON.HEX` byte for byte.

It expects **itself at F800**, **RAM below C000** (it sets its stack there and pushes down), and an **88-2SIO at port 10** with the console on channel A. Those are not choices; they are what the listing requires, and `machines/altmon.toml` is the machine that satisfies it.

> **A trap, from ALTMON's own listing.** Its `ahex` routine reads exactly four hex digits and **aborts to the prompt on any byte below `'0'`** — so a space is not a separator, it is a *cancel*. Type `DF800F80F`, not `D F800 F80F`. The spaces in ALTMON's own printed command summary are typography, not grammar.

**How `dbl` was verified.** All 16 Intel HEX records checksum clean and cover `FF00`–`FFFF` contiguously with no gaps and no overlap. Every one of the 256 bytes in `DBL.HEX` matches the address/opcode columns of the `DBL.PRN` assembler listing exactly. The image ends `... C9 00 00` — the trailing zeros are the listing's own `DW 00H` ("FILLS THE EPROM OUT WITH 00'S"), so this is a **complete 256-byte part, not a truncated one**.

**One caveat, stated plainly.** The chain is *EPROM → Eberhard's disassembly → reassembly*, not a raw dump. The bytes are therefore only as faithful as the disassembly, and I cannot close that last link without the physical part. This is still far better than a bare `.bin`, because the listing makes every byte auditable — but it is not the same as a dump, and the table should not pretend it is.

**What DBL actually does, since it keeps getting mis-cited.** DBL copies *itself* from `FF13` into RAM at `2C00` (`EB` bytes) and jumps there, because — its own header says so — "BECAUSE OF THE SLOW EPROM ACCESS TIME." **It never writes to `FFxx`.** It therefore has *no* opinion about PHANTOM\* write-through, and any claim that DBL "needs shadow RAM" is false. Earlier drafts of `DESIGN.md` §4.2, `docs/config.md`, and `docs/boards/s100-memory.md` all said exactly that; it was fabricated and has been removed. It reads the sense switches at `FF22` (`IN 0FFH`, bit 4) to pick the 2SIO stop-bit setting — *that* part is real, and it is why the sense switches had to be modeled at all. They live on the **front-panel card** (`SET fp0 sense=...`), not on `[machine]`: a `[machine] sense` key existed briefly, put a byte nowhere on the bus, and is now refused with a message that says where the switches went.

## Licensing

Most of these parts are from companies that no longer exist (MITS folded in 1979), and vintage ROM images circulate freely — but "freely circulating" is not the same as "licensed to redistribute," and **embedding a ROM in the binary is redistribution** in a way that asking a user to supply a file is not.

This is Patrick's call, not the simulator's, and it is recorded here so it is a decision rather than an accident. If any ROM turns out to be one we should not ship, the fallback costs nothing: it stays a path (`mount = "roms/x.bin"`), the user supplies it once, and `docs/roms.md` says where to get it.

`rombasic` is an original **MITS** part — ALTAIR ROM BASIC 4.1, © 1977 MITS Inc. — on the same footing as `dbl`, `mdbl` and `mbl`: MITS folded in 1979 and its BASIC images circulate freely in the vintage archives. It ships here as a decision, made here, reversible to a path if it should not have been.

`mbl` is an original **MITS** ROM, on the same footing as `dbl` and `mdbl` above: what ships here is Geoff Harrison's reverse-engineered listing of a hex dump, and it circulates as freely as the rest of that vanished catalog. `cdbl`, `hdbl`, and `amon` are Martin Eberhard's and Mike Douglas's own work, published on deramp.com for exactly this kind of use. `acuter` and `cuter` are more delicate: both derive from Processor Technology's **CUTER 1.3**, which carries a 1977 Software Technology Corp. copyright — `acuter` is Mike Douglas's 2015 Altair port of it, and `cuter` is the original part itself (its source restored from an *Access* newsletter listing). They circulate as freely as the rest of that vanished catalog, and they ship here on the same basis as the MITS parts above — a decision, made here, reversible to a path if it should not have been.

`tarbell-sd` is a **Tarbell Electronics** part — the 32-byte boot PROM from the #1011/#2022 floppy interfaces. Tarbell Electronics is long defunct, the bootstrap is 32 bytes reproduced from the interface manual's own listing, and it ships on the same footing as the MITS and SD Systems parts above — reversible to a path if it should not have been.

`ddb200`, `msmonr21` and `sdmonv21` are **SD Systems** parts. SD Systems is long defunct, and all circulate freely in the vintage archives (deramp.com): `ddb200` is Rex Brown's DDBIOS floppy driver, and `msmonr21` / `sdmonv21` are the two console builds of the SBC monitor PROM (8251 serial vs VDB-8024 video). They ship on the same footing as the MITS parts above — reversible to a path if any should not have been.

`icom-fd3712-cpm`, `icom-fd3712-fdos` and `icom-fd3812-cpm` are **iCOM** interface-board boot PROMs. iCOM (Pertec) is long defunct, and all three circulate freely in the vintage archives (deramp.com) as Mike Douglas disassemblies: the two FD3712 PROMs (the CP/M loader at `F000` and the FDOS/EDOS bootstrap at `C000`) and the FD3812 double-density CP/M loader. They ship on the same footing as the MITS and Tarbell parts above — reversible to a path if any should not have been.

**Each ROM directory carries its manual and a `README.md`.** The vendor manual PDFs are retained in `roms/<NAME>/` as downloaded, and each `README.md` is distilled from that manual (plus the ROM's own `.ASM` header) — what the ROM is, its load address and entry points, its I/O and RAM needs, and the `mount =` line to use it. These are the one exception to the repo's "no vendor PDFs" rule (`.gitignore`): they are small, they are the primary-source manuals for parts we embed, and keeping them beside the image is the same provenance discipline the table above exists to enforce.

## How it works

At build time, CMake turns each file in `roms/` into a byte array in a generated translation unit, and a registry maps `builtin:<name>` → `{span<const uint8_t>, crc32, description}`. Consequences worth stating:

- **No filesystem access at runtime** for a built-in. It is `.rodata`. Nothing to find, nothing to permission, nothing to ship alongside.
- **The board does not care.** A region takes a `span<const uint8_t>`; whether that came from `.rodata` or a file the host service read is not its business (§7). The `builtin:` scheme is resolved by the config loader, above the board.
- **`CONFIG SAVE` round-trips the name, not the bytes** — `mount = "builtin:dbl"` in, `mount = "builtin:dbl"` out.
- **A CRC test per ROM.** Cheap, and it turns "someone's editor mangled a binary" from a mystery into a build failure.
- **The `SHOW ROMS` description is one hand-written line per ROM**, in `roms/<NAME>/DESC`. Every built-in has one, so the column is uniform and each is visible and editable in the tree — not derived by a heuristic no one can see. Editing a `DESC` re-runs the embed; a ROM with no `DESC` still builds (blank column) but warns.

## References

- `DESIGN.md` §0.1 (where hardware facts come from), §7 (host services), §10.2 (`RAW` — how you get bytes into a ROM at runtime).
- `docs/boards/s100-memory.md` — the board that holds them.
