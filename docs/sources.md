# Sources — the manuals and data sheets the hardware was built from

`reference/` holds the period documentation this simulator's boards were modeled from.
The **scanned manuals and data sheets themselves are deliberately not in git** (see
`.gitignore`): 83 MB, none of them ours to redistribute, and none needed to *build* — only
to *understand*. What **is** tracked is a **distilled, text-only `.md` for each one** — register
maps, port addresses, bit tables, geometry and timing — written from the scan and citing it by
filename. `reference/README.md` indexes them; this file is the manifest that says what each
original source is and where it came from, so a fresh checkout knows what the `.md` was made
from and what scan it is missing.

Nothing here is a build dependency. If `reference/` held only its `.md` files, everything still
compiles and every test still passes. The manifest below names each source scan by its `.pdf`
filename; the tracked distillation is the same name with a `.md` extension (indexed in
`reference/README.md`).

## The rule

**Period manuals and first-hand artifacts only. Never read another emulator's source to
learn how hardware works** — that explicitly includes SIMH / AltairZ80. Second-hand facts
inherit second-hand mistakes, and the value of this project rests on the hardware model
being *right*, not on matching someone else's. If a spec is missing, ask Patrick; he sources
the manual. Do not guess, do not reconstruct from memory.

Authorized web sources: **deramp.com** and **altairclone.com** (Mike Douglas), and
**s100computers.com** (John Monahan's S-100 hardware-manual archive). These carry period
artifacts and test programs written for real silicon — not somebody's emulator.

**righto.com** (Ken Shirriff) is authorized specifically for **silicon die
reverse-engineering** — reading the behavior off the physical chip's transistors, which is
first-hand hardware analysis, not another emulator. Patrick authorized it 2026-08-19 for the
8085's undocumented flags (`reference/Intel 8085 undocumented instructions and flags.md`).
The undocumented-opcode operations and flag masks are settled by two **primary** artifacts
Patrick provided 2026-08-20 — the **Tundra CA80C85B data sheet** and the 1979 *Electronics*
article (both in the manifest below), which agree with each other and with Shirriff on the
flags. `electronicerror.blogspot.com`, once the only record of those operations, now stands
only as **corroboration** — the same standing the JS-1 corroborating sources have below.

## The manifest

| File | What it is | Authoritative for |
|---|---|---|
| `Western Digital FD1771 - Datasheet.pdf` | WD FD1771 FDC data sheet, 20 pp, **real text layer** | `src/chips/wd17xx.{h,cpp}`. The Tarbell's actual chip. Also reprinted as §7-2 of the Tarbell manual, so there is a free cross-check. |
| `Tarbell_Floppy_Disk_Interface_Manual.pdf` | Tarbell SD FDC manual, 78 pp, **real text layer** | `docs/boards/tarbell-sd.md`. Settles the port base (DIP switch, upper 5 bits), the wait port, and the control port's 3-to-8 decoder + latch. |
| `Altair Floppy (88-DCDD) Manual.pdf` | MITS 88-DCDD manual (52 MB — the big one) | `docs/boards/mits-dcdd.md`, as a cross-check. The `BOOT.ASM` equates in `disks/` are the better spec. |
| `Altair 88-ACR Cassette Interface.pdf` | MITS 88-ACR manual, **has a text layer** | `docs/boards/mits-88acr.md`. An 88-SIO "B" board plus an analog modem board. **There is no motor control** — the operator pressed PLAY. |
| `88-UIO.pdf` | MITS "Using the 88-UIO Board" + the 4-sheet Attaché schematic set (drawing 250272A). **Has a text layer** (the prose pages); the schematic sheets are page images. Fetched 2026-07-25 from deramp.com (`.../altair/hardware/88-UIO/88-UIO.pdf`). | `src/boards/mits-88uio.{h,cpp}`, `docs/boards/mits-88uio.md`, `reference/88-UIO.md`. Two independent sections on one board: a **6850 serial port** (schematic IC K — one 2SIO channel; 020/021 octal, SW-2 → 030/031; 7-position baud jumper SK1 110–19200; SK2 TTY/RS-232) and an **88-ACR-style cassette section** (AY-5-1013A UART, IC L; 006/007 octal, SW-3 → 016/017; active-low status; 300 baud). Authoritative for the two things the plain 88-ACR **lacks**: **motor control** (`OUT 6,127` D7-low = ON / `OUT 6,191` D6-low = OFF; **relay contacts normally closed at power-up**) and a **SW-1-selectable modulation standard** — MITS 2400/1850 (OFF) or Kansas City 2400/1200 (ON), the KCS principle reprinted from BYTE Feb 1976. Register model, status polarity, framing and tape format are the 88-ACR's (shared with `mits-88acr.md`); the cassette UART is the same 1602-family part `com2502.pdf` documents. Defaults (all switches OFF) reproduce the standard 0x10-serial + 0x06-cassette layout, so MITS BASIC boots unchanged. |
| `88-SIO Rev 0 & 1.pdf` | MITS Serial I/O Board Documentation, © 1975 | `docs/boards/mits-88sio.md`. Status word, interrupt-enable bits, address decode, UART pads. |
| `CUTS Assembly and Test.pdf` | Processor Technology *CUTS, Computer Users Tape System — Assembly and Test Instructions*, © 1977, 39 pp. Corroborated by H. Holden, "The SOL-20 Computer's Cassette interface," Dec 2018 (worldphaco.com) — a scope-and-counter teardown of the same modem design (web article, no scan). | `reference/CUTS Assembly and Test.md`. The Processor Technology S-100 cassette module and the identical modem on the Sol-20 motherboard: two program-selected rates (**1200 bps default** / 300 bps Kansas-City-compatible), the per-bit modulation (mark/space/idle tones and cycle counts), and the status-latch speed select. **Supersedes** the "MEASURED, not from a manual" table `reference/Sol-20.md` used to carry. |
| `88-VI-RTC.pdf` | MITS 88-VI Vector Interrupt / 88-RTC Real Time Clock, © 1976. **No text layer** — read as page images. Fetched 2026-07-13 from deramp.com (`.../00-MITS/10-MITS S100 Boards/88-VI-RTC-Vectored Ints and RTC modules/88_virtc.pdf`). | `docs/boards/mits-88virtc.md`, `src/boards/mits-88virtc.{h,cpp}`. The control register at **376Q (0xFE, write-only)**, the priority order (**VI0 highest, VI7 lowest**), level *n* → `RST n`, and the RTC's rates and its "RI" jumper. **⚠ IT CONTRADICTS ITSELF** on the encoding of bits 0–2 (prose says the level; its own table says the ones-complement) and on the sense of bit 3. **The table won, and the tiebreaker was the artifact:** the PS2 monitor's own ISR (`tapes/MitsPS2/PS2-MON.TAP`, disassembled at 08DB) writes exactly what the table predicts. Do not re-derive this from the prose. |
| `com2502.pdf` | SMC COM2502/COM2017 UART data sheet. **No text layer** — read as page images. | `src/chips/uart1602.{h,cpp}`. The 88-SIO's UART; the 88-ACR manual calls the same part an AY-5-1013/TR1602. |
| `Altair 2SIO User's Manual.pdf` | MITS 88-2SIO manual | `docs/boards/mits-2sio.md`. |
| `6850.pdf` | Motorola MC6850 ACIA data sheet, pp. 4-527…4-535. **No text layer** — read as page images. | `src/chips/mc6850.{h,cpp}`. |
| `8251.pdf` | Intel 8251/8251A USART data sheet. Fetched from deramp.com (`https://deramp.com/downloads/intel/8251.pdf`). | `reference/Intel 8251 USART.md`. The programmable serial interface behind the SD Systems SBC-100/200 console (`7C`/`7D`) and the IMSAI SIO-2: register model (**data even offset, status/command odd offset**), the mode/command programming sequence after reset, the framing state machine, and the all-active-high status byte (TxRDY=D0, RxRDY=D1) with the 8251A `TxRDY = TxEN·C̄TS·empty` caveat. |
| `IMSAI SIO-2.pdf` | IMSAI *SIO 2 Board* manual (Rev. 3 — Functional Description / Theory of Operation / User's Guide, Edition 2; © 2002 IMSAI Division, Fischer-Freitas Company — a reprint of the mid-1970s IMS Associates manual, assembly diagram dated 2/76). | `reference/IMSAI SIO-2.md`, and the USIO `imsai-sio2` built-in profile. Two-channel serial board built around a pair of **Intel 8251 USARTs** (`8251.pdf`): the DIP-strapped 4-port groups, the read-vs-write register shape (data even / status+command odd), the mode/command sequence, the baud/handshake straps, and the I/O-port-vs-memory-mapped response jumper. |
| `Western Digital WD177X-00 - Datasheet.pdf` | WD1770/72/73 data sheet | **Nothing yet.** See the trap below. Kept for a future double-density controller. |
| `Altair 8800 Theory of Operation.pdf` | MITS Altair 8800 Theory of Operation, **searchable — real text layer** | `docs/boards/mits-frontpanel.md`. The CPU board's gating logic (`SSW DSB`, and the sense switches at "device address 377o"), the D/C board's EXAMINE/DEPOSIT/SINGLE-STEP sequences, and the bus pin definitions. |
| `Altair 8800 front panel schematic.pdf` | Schematic **880-106**, "Computer Front Panel Control" | `docs/boards/mits-frontpanel.md`, and **authoritative for the port FF decode**: the `sINP` + A8–A15 8-input NAND, and the three banks of 7405 buffers. **No text layer** — read it as a page image. |
| `Altair 8800 Operators Manual.pdf` | MITS Altair 8800 Operator's Manual, **searchable** | The switch and LED inventory. Note it never uses the phrase "sense switch" — the Theory of Operation and the schematic are where that lives. |
| `Hexfrmt.pdf` | *Intel Hexadecimal Object File Format Specification*, Rev A, 6-Jan-1988, 10 pp, **real text layer**. Added by Patrick 2026-07-17. | `src/core/hex.{h,cpp}` and `reference/hexfrmt.md`. The record layout, all six record types, and the checksum rule (a record's bytes sum to zero, RECLEN through CHKSUM). **Authoritative for the modulo-64K wrap** that `LOAD ... AT` does: §4 defines a byte's address as `[DRLO + DRI] MOD 64K` and says wraparound "from 0FFFFH to 00000H results in wrapping around from the end to the beginning" -- so relocating a file whose first record is F000 to AT 0 puts its E000 record back up at F000. Note it contains **no worked example** of a file: the example in `reference/hexfrmt.md` and in the LOAD reference is our own `SAVE` output, checksum-verified. |
| `Zilog Z80.pdf` | Zilog Z80 CPU User Manual **UM0080** (UM008011-0816), 332 pp, **real text layer**. Added by Patrick 2026-07-15. | `src/cpu/cpuZ80.{h,cpp}` and `src/isa/isaZ80.cpp`. The register/alternate/special set, the flag layout, and the CB/ED/DD/FD/DDCB/FDCB opcode maps with byte lengths and addressing. **⚠ Two gaps an emulator author must fill from elsewhere:** (1) the manual labels flag bits **5 and 3 "not used"** and never describes the real-silicon **F5/F3 (YF/XF)** copy behavior — for that the authority is the artifact, **ZEXALL** (`tests/cpu/z80/`), which the core passes. (2) The compact op-code tables give opcodes and byte counts but **not** T-states or flag-effect columns; those are the canonical Zilog per-instruction values (the 240-page per-instruction section was not transcribed line by line). |
| `9800301D_8080_8085_Assembly_Language_Programming_Manual_May81.pdf` | Intel *8080/8085 Assembly Language Programming Manual*, document **9800301D**, May 1981, 222 pp, **real text layer** (scanned via Adobe Paper Capture — the text is OCR'd, so a stray character or two appears in prose; all mnemonics/bit tables verified). All mnemonics © Intel 1974–1977. Fetched 2026-08-08 from bitsavers.trailing-edge.com (`.../components/intel/MCS80/`). | `reference/Intel 8080-8085 Assembly Language Programming.md`; `src/cpu/cpu8080.{h,cpp}` and `src/isa/isa8080.cpp`. Intel's authoritative programmer model for both parts: the A/B/C/D/E/H/L + SP/PC register set and pairs, and the **S Z AC P CY** condition flags with exact semantics (AC is DAA-only and untestable; `IN` sets no flags). Its value over a generic 8080 doc is the **8085 differences**, all emulation-observable: (1) the **PSW flag-byte filler bits** are fixed on the 8080 (bit 5=0, 3=0, 1=1) but **undefined on the 8085**; (2) `ANA`/`ANI` **always set AC on the 8085**, whereas the 8080 sets AC to the OR of bit 3 of the operands (changes the following `DAA`); (3) 8085 conditional branches **skip the third opcode byte when the condition is not met** (source of the split T-state counts); (4) the two 8085-only instructions **`RIM`/`SIM`** (SID/SOD serial + interrupt mask — full bit layouts captured). Complete 8085 **interrupt** model: `TRAP` (vector 24H, **non-maskable, fires even under `DI`**), edge-latched `RST7.5` (3CH; flip-flop cleared by RESET / recognition / SIM bit 4), `RST6.5` (34H), `RST5.5` (2CH), `INTR`, their fixed priority, and the generic `RST n` → n×8 vectors. Appendix A gives per-CPU T-states (8080 clock period 480 ns–2 µs, 8085 320 ns–2 µs); the full alphabetical instruction descriptions and the assembler/macro/relocation language are summarized, not transcribed. **⚠ It is an OCR'd scan — WebFetch cannot parse it; use `pdftotext` (clean) or read the pages as images.** |
| *(web, no scan)* Ken Shirriff, ["Silicon reverse-engineering: The 8085's undocumented flags"](https://www.righto.com/2013/02/looking-at-silicon-to-understanding.html) + ["The 8085's instruction set: the octal table"](https://www.righto.com/2013/02/8085-instruction-set-octal-table.html); electronicerror, ["Undocumented flags and instructions"](https://electronicerror.blogspot.com/2007/08/undocumented-flags-and-instructions.html). Provided and authorized by Patrick 2026-08-19. **Web articles, not PDFs** — the tracked distillation *is* the source of record. | The 8085's **undocumented condition flags and opcodes**, which Intel omitted from the programmer's manual (`9800301D` above records only that the filler bits are "undefined"). Shirriff's flag article is **die-level reverse-engineering of real 8085 silicon** — the authority for **V** (overflow, PSW bit 1 = `carry-in ⊕ carry-out` of bit 7) and **K/X5** (PSW bit 5 = `V ⊕ sign`, except `INX`/`DCX` where it is the 16-bit incrementer carry), with the exact per-instruction rules; it also **corrects** the older published K formula from the silicon. The octal table fixes the ten undocumented **opcodes/octal/lengths** (`DSUB ARHL RDEL LDHI LDSI RSTV SHLX LHLX JK JNK`; `RSTV` → `0x0040`), which already match `src/isa/isa8085.cpp`. Their **operation/flag effects** — once only recorded second-hand here (electronicerror, machine-translated, self-contradictory on `LDHI`/`LDSI` and `ARHL`/`RDEL`) — are now **settled by the two primary sources below** (Tundra data sheet + *Electronics*), so electronicerror stands only as corroboration. ⚠ **`8085EXM`'s `0D5h` mask cannot validate V/K** — so a faithful-V/K core is gated by hand-derived vectors from the flag rules, not the exerciser. All distilled in `reference/Intel 8085 undocumented instructions and flags.md`; tracked for issue #347. |
| `tundra_80c85.pdf` | **Tundra CA80C85B data sheet**, *High Performance 8-bit CMOS Microprocessor*, Tundra Semiconductor Corp. (Calmos 8000 series), 16 pp, © 1997 — a second-source CMOS 8085 with "Full support of extended instruction set." **Real text layer** (the tables are page images). Provided and authorized by Patrick 2026-08-20. | The 8085's **undocumented (extended) instruction set — the primary reference for `reference/Intel 8085 undocumented instructions and flags.md` §2.** Table 2-10 (*Extended Instruction Set*) gives all ten opcodes' **bytes, flag masks, cycles, states and operation**; Figure 2-9 (*Extended Condition Codes*) gives the flag byte `S Z UI AC 0 P V C` (V = bit 1, **UI**/X5/K = bit 5) and the `UI = O1·O2 + O1·R + O2·R` formula. Settles every earlier ⚠: `LDHI`/`LDSI` set **no** flags, `RDEL` sets **CY and V**, `ARHL` sets **CY** only, `DSUB` sets **all seven**; confirms `RSTV` = byte **`CB`** and leaves INTE alone. Agrees with the *Electronics* article and with Shirriff (`righto.com`) on the V/K values. A vendor data sheet outranks both. |
| `UnDoc85.pdf` | **Wolfgang Dehnhardt & Villy M. Sorensen, "Unspecified 8085 op codes enhance programming,"** *Electronics* "Engineer's notebook", 1979 (GSI Darmstadt / Sorensen Software), 2 pp — the first published account of the ten opcodes and the two flag bits. **Real text layer.** Provided and authorized by Patrick 2026-08-20. | Corroborating primary source for `reference/Intel 8085 undocumented instructions and flags.md` §2. Its "NEW 8085 INSTRUCTIONS" table gives each opcode's operation and flag mask and **agrees with the Tundra data sheet** point-for-point (`DSUB` "all condition flags"; `ARHL` CY only; `RDEL` CY, V; `LDHI`/`LDSI` no flags), plus the `S Z X5 AC 0 P V C` condition-code layout and the same majority formula for X5. One print gotcha: its table misprints `RSTV`'s hex as "(C8)", but the binary `11001011` and Tundra both give **`CB`**. |
| `88-HDSK.pdf` | MITS 88-HDSK hard disk manual | Nothing yet — the board is M7. |
| `88-C700.pdf` | MITS "Altair C700 Printer User's Guide", first printing March 1977, 32 pp. **Has a text layer.** Fetched 2026-07-17 from deramp.com (`.../00-MITS/10-MITS S100 Boards/88-C700 Centronics Printer Controller/88-C700.pdf`). | `src/boards/mits-88c700.{h,cpp}`, `docs/boards/mits-88c700.md`, `reference/88-C700 Centronics Printer Controller.md`. The card is modeled **polled** (the single-level interrupt is not). The two consecutive ports (even Control/Status, odd Data; A0 split; MITS default **base 002**), the status/control bit tables, the data-write strobe (~1.5 µs, "widen to ≈2 µs for Anadex") that clears the ACK latch and interrupt, the single-level interrupt switched **per-character or per-CR/LF (SW2 #4)** — the board does **not** vector like the 88-VI — and the C700's printer-side ASCII control codes (LF/CR/DC1/DC3/DEL/SO). |
| `88-LPC Printer Interface.pdf` | MITS "88-LPC Board Documentation" (LINE PRINTER CONTROLLER), © MITS 1975. Controller for the **88-LP line printer** (Okidata mechanism, 64-char, 80-col). **No text layer — read as page images.** Fetched 2026-07-25 from deramp.com (`.../altair/hardware/88-LPC Printer Interface.pdf`). | `src/boards/mits-88lpc.{h,cpp}`, `docs/boards/mits-88lpc.md`, `reference/88-LPC Printer Interface.md`. The card is modeled **polled** and modeled as the **printer's line mechanism** (not a byte pipe like the C700 — its data channel carries 6-bit codes and its line boundaries are *commands*). The two consecutive ports (even Control, odd Data; A0 split; 128 jumpered bases E1–E7 → A1–A7; MITS software requires **base 002**), the **active-high command** control byte (**D0** PRINT / **D1** LINE FEED / **D2** CLEAR / **D3** interrupt enable), the status byte (**D0** buffer-empty / **D1** not-printing / **D2** paper-ok / **D3** line-feed-ok), the 80-char buffer that prints on PRINT or auto-fills at 80, and the full hardware interrupt (88-VI vectored or single-level `PINT`). **⚠ INFERENCE:** the data channel's **6-bit code → glyph** mapping (bit 6 = complement of bit 5, so `0x20`→space, `0x01`→`A`) is the standard 64-char-subset packing and matches this manual's own test program (`space = 100000 octal`), but the exact chart is in the **Okidata printer manual, which the 88-LPC document references and does not include** — verify against it if it surfaces. |
| `MITS 88-PIO.pdf` | MITS "Parallel I/O Board Documentation", © MITS 1975 (parts list dated June 1975), 33 pp. **No text layer — read as page images.** Added by Patrick 2026-07-23. | `reference/MITS 88-PIO.md`, and (planned) an `88-pio` board — see `TODO.md`. The 88-PIO is **discrete TTL**, a pair of 8212 latches, **not a PIA** — port direction is fixed in hardware. Authoritative for: the two consecutive ports (even Control/Status with the A0 split, odd Data), the status bits (**DI0** output-ready, **DI1** input-has-data), the interrupt-enable bits (**DO0**/**DO1**, "BOTH" jumper), the `SBO`/`SBI` strobe + `BO`/`BIN` handshake, and both interrupt paths (88-VI vectored, or single-level RST 7 → 0x38). **⚠ Its interrupt-pad priority labels ("0 lowest … 7 highest") conflict with the 88-VI board's own VI0-highest order** — trust `88-VI-RTC.md`. |
| `MITS 88-4PIO.pdf` | MITS "88-4 Parallel Input/Output Board", © MITS 1975, Third Printing March 1977, 36 pp. **No text layer — read as page images.** Added by Patrick 2026-07-23. | `reference/MITS 88-4PIO.md`, and (planned) an `88-4pio` board — see `TODO.md`. Built on up to four **Motorola 6820 PIAs** (ports J/K/L/M), so nearly everything is software-selectable. Authoritative for: the 16-address-per-board decode (A4–A7 board / A3–A2 port / A1–A0 section+register), the read/write control/status register bit map (**7/6** IRQ status, **5–3** C2 control, **2** DDR-select, **1–0** C1 control), the C1/C2 edge+enable tables, the section-A/B-specific CA2/CB2-as-output strobe modes, the DDR direction rule (0=in, 1=out), the power-on all-inputs reset, the init/handshake protocol, and both interrupt paths (88-VI vectored, or single-level `PINT` → RST → 0x38). This is the same 4PIO port model the **88-HDSK** uses. |
| `FDC+ Manual.pdf` | Altair **FDC+** User's Manual, 23 pp, **real text layer**. v2.0, 26-Jan-2026. Fetched 2026-07-13 from deramp.com (`.../altair/hardware/fdc+/FDC+ Manual.pdf`). | `docs/boards/mits-dcdd.md`. The FDC+ is a modern *"100% compatible drop-in replacement"* for the 88-DCDD, so it is not a source for the original card's registers — but it **is** the authority on the **8 MB medium**, which is the one thing the period manuals cannot describe. §3.7.4: drive type **7**, and *"the 8Mb drive looks like an Altair 8 inch drive with 2048 tracks instead of 77."* It also documents the arrangement period software expects — **8 MB on drives A/B, ordinary 77-track floppies on C/D**, mixed on one controller — which is the real justification for the per-drive `Spindle`. |
| `TurnKey Board.pdf` | MITS/Pertec *Systems Turnkey Module* Theory of Operation (drawing `200786A`, §4-4 + Boot Loader/Turnkey Monitor §9–10), Service Bulletin **#007** (10/9/79), assembly drawing `248067A`. **No text layer — read as page images.** PCBA `200372-01`, schematic `200374`. Paired with `8800b Turnkey Notes with Schematics.pdf` (Martin Eberhard, 13-Aug-2014, **real text layer + schematics**), which supplies the actual addresses the manual omits. | **Built as the `turnkey` board** — `src/boards/mits-turnkey.{h,cpp}`, `docs/boards/mits-turnkey.md`, `reference/MITS Turn Key Board.md`. The board asserts `PHANTOM*` over its PROM window and snoops its PROM **out** on the first `IN` from port `FE`/`FF` rather than answering — so it never contends with a panel for the port (in a `turnkey` machine it owns FF itself). **⚠ Traps the two sources settle:** the trigger is an *input* only after Service Bulletin #007 (as-built, *any* I/O disabled the PROM); the **sense-switch read is that same Phantom trigger**, which is why BASIC drops into full 64K RAM after init, why PROM boot loaders must **never** read the sense switches (so DBL et al. don't set the 2SIO stop bits), and why **MITS's own MBL — which reads them — will not run on a Turnkey Module**. PROM window `FC00`–`FFFF` (sockets `L1`=HDBL / `K1`=TURMON / `J1`=MBL / `H1`=floppy loader); onboard 1K RAM at `F800`–`FBFF` exists **only** on the REV 0 board (removed on `200372-01`; 88-SYS-CLG/CLG2 rework the older board toward the newer). The 6850 SIO is **2SIO-Port-A-compatible at `10h`**. |
| `88-MDS Minidisk Manual.pdf` | MITS 88-MDS Minidisk System manual, 110 pp. **No text layer** — read as page images. Fetched 2026-07-13 from deramp.com (`.../altair/hardware/minidisk/`). | `docs/boards/mits-88mds.md`, `src/boards/mits-88mds.{h,cpp}`. **Everything about the card**: the port map (p29), the status bits (pp. 30–31, and *"When a bit is True, it is a logic Ø"*), the control bits (pp. 31–32), the sector register (Table 3-B, p34), 300 RPM / 125 kbit/s / 64 µs a byte / 12.5 ms a sector (p4), the **6.4 s Disk Disable Timer** (p68 — a **4020** clocked by the sector pulse, so 12.5 ms × 512 = 6.4 s *exactly*), and the **1 second motor-on delay** (p58, p69). **⚠ IT CONTRADICTS ITSELF THREE TIMES** — see below. **And do not miss the unnumbered handwritten one-shot sheet bound in after p79** ("MINI DISK CONTROLLER BOARD TIMING", signed "Shep"): it is the complete 74123 timing table, with min/max, and it is not in the table of contents. |
| `88-MDS Minidisk Schematics.pdf` | MITS 88-MDS schematics, 6 sheets. **No text layer, and the scan is too coarse to read most R/C values** — say so rather than guess. | `docs/boards/mits-88mds.md`, for **structure, not numbers**: the port decode is hard-wired (`74L30`/`74L10` gates annotated *"= 1 WHEN ADDRESS = 010₈ / 011₈ / 012₈"*, i.e. 08/09/0A, with **no address jumper anywhere on the controller**); the byte clock is divided down from the **S-100 2 MHz bus clock** (pin 49 → ÷2 → ÷8 → 125 kHz), not a crystal; the status buffers are **non-inverting 8T97** (the *signals* are active-low instead — same byte on the bus, different mechanism from the 88-DCDD's 7405s); and the motor-off timer is a **4020B ripple counter (IC B2)**, not a one-shot. |
| `Minidisk Info from MITS.pdf` | MITS technical-information sheet, 1 p, **scanned**. | The system description: two controller boards, 71,680 formatted bytes, the **MDBL** and **DRWT** PROMs. **Wrong on the timer** — it says five seconds where the manual's engineering sections and two pieces of period software all say **6.4**. Marketing, not engineering. |
| `Microsoft M80 Assembler.pdf` | Microsoft MACRO-80 Assembler manual, CP/M version (Heath/Zenith 595-2666-02, © Microsoft 1979), 46 pp, **real text layer**. Fetched 2026-07-17 from deramp.com (`.../altair/software/manuals/Microsoft M80 Assembler.pdf`). | `src/core/symbols.{h,cpp}`, `docs/manual/debugging.md` (Symbols). The **`.PRN` listing format** the debugger loads: the per-line relocation markers and the symbol-table section that flags every symbol's type. Also the default extensions (`.MAC`/`.REL`/`.PRN`/`.CRF`) and the **decimal** default radix. **M80 writes `.PRN`, not `.SYM`.** |
| `Microsoft L80 Linker.pdf` | Microsoft LINK-80 Loader manual, CP/M version (Heath/Zenith, © Microsoft 1979), 11 pp, **real text layer**. Fetched 2026-07-17 from deramp.com (`.../altair/software/manuals/Microsoft L80 Linker.pdf`). | `src/core/symbols.{h,cpp}`, `docs/manual/debugging.md` (Symbols). The **`.SYM` file** the debugger loads: **L80 `/Y` writes `filename.SYM`** (with `/N`, on `/E`) — names+addresses of **globals only**, for DRI's SID/ZSID. Corrects the earlier belief that the Microsoft path emits no `.SYM`; M80 does not, but L80 `/Y` does. |
| `VDM-1 Rev E Newer Manual.pdf` | Processor Technology *VDM-1 Video Display Module User's Manual*, Manual Part No. 208003, Sixth Printing April 1978 (© 1976), 111 pp. **No text layer — read as page images** (71 MB high-res scan). Fetched 2026-07-18 from deramp.com (`.../00-Processor Technology/10-PTC S100 Boards/VDM-1/VDM-1 Rev E Newer Manual.pdf`). Note the scan is a **composite of revisions** — individual pages carry Rev A/B/C footers. | `reference/Processor Technology VDM-1.md`, and (planned) `src/boards/proctech-vdm1.{h,cpp}` / `docs/boards/proctech-vdm1.md`. Authoritative for: the **1 KB memory-mapped screen RAM** (16×64; page = jumper on ADR10–15; default **`0xCC00`**, §3.1.8), the **single I/O port** (6 jumper bits = ADR7–2, **ADR1–0 forced 0**; `port = (base>>8)&0xFC` → default `0xCC`) with `OUT` = scroll-row latch + a **0.25–0.5 s one-shot** and `IN` = status (**D0** one-shot timer, **D1** SCAN ADVANCE), the byte split (**D0–D6** → MCM6574 glyph, **D7** = cursor/blink, not in ROM; §3.1.4), CR→EOL / VT→EOScreen control blanking, and the **SW1–SW6** options (Table 3-1: video polarity / cursor / control-char blanking). Derive D0/D1 status from the `Clock`, not a poll counter. |
| `Cromemco_Dazzler_Instruction_Manual.pdf` | Cromemco *Dazzler Instruction Manual*, Part No. 023-0003, November 1978 (US Patent 4,121,283), 23 pp. **No text layer — read as page images.** Fetched 2026-07-18 from deramp.com (`.../00-Cromemco/10-Cromemco S100 Boards/Dazzler/Manual/Dazzler Rev D/Cromemco_Dazzler_Instruction_Manual.pdf`). | `reference/Cromemco Dazzler.md`, and (planned) `src/boards/cromemco-dazzler.{h,cpp}`. Authoritative for: the **DMA bus-master** framebuffer scan (~1 MB/s, ~15% slowdown — rides `requestsBus()`/`busMaster()`), `OUT 0x0E` (**D7** on/off, **D6–D0** = A15–A9 → **512-byte-aligned base** `(v&0x7F)<<9`), `OUT 0x0F` (**D6** X4, **D5** 512 B/2 KB, **D4** color/mono, **D3–D0** X4 global color/intensity), `IN 0x0E` (**D7** odd/even line, **D6** end-of-frame low ~4 ms), the **512 B (1 quadrant) / 2 KB (4 quadrants, 2×2 tile)** memory map, and the byte→pixel encoding (**normal**: two 4-bit RGBI elements/byte; **X4**: eight on/off bits as two 2×2 blocks). The p6 front-panel test program is a ready-made demo. Derive the status bits from the `Clock`. |
| `D+7A Rev C Manual.pdf` (+ Rev B/D manuals, Rev B schematic) | Cromemco *D+7AI/O* manuals, fetched 2026-07-23 from deramp.com (`.../00-Cromemco/10-Cromemco S100 Boards/D+7A/`). Rev C has the cleanest **Operating Instructions** (the two's-complement scale, the port block, the calibration loops) plus the addendum with the **wait-state timing** and the Dazzler flash note. **No text layer — read as page images.** | `reference/D+7A.md`, `src/boards/cromemco-d7a.{h,cpp}`, `docs/boards/cromemco-d7a.md`, `machines/d7a.toml`. Authoritative for: the **8 consecutive ports** (5 jumpers = A7..A3; recommended base **030 octal / 0x18**), the **parallel port** at BASE+0 (a byte in, a byte out) and the **seven analog channels** at BASE+1..7 (A/D on `IN`, D/A on `OUT`, read/write independent per port), the **8-bit two's-complement** 20 mV/LSB scale (0x80 = −2.56 V, 0x00 = 0 V, 0x7F = +2.54 V), and the **5.5 µs / 11-wait-state** READY hold on every analog `IN`/`OUT` (deliberately **not** modeled — see the board doc). |
| `Cromemco JS-1 Joystick Manual.pdf` | Cromemco *JS-1 Joystick Console* manual, fetched 2026-07-23 from manx-docs.org (`.../harte/Cromemco/Cromemco JS-1 Joystick Manual.pdf`). **No text layer — read as page images.** | `reference/JS-1.md`, `src/boards/cromemco-d7a.{h,cpp}` (the JS-1 is emulated *through* the D+7A, not as its own board). Authoritative for the JS-1→D+7A port map: X/Y pots → analog inputs **031/032** (console 1) and **033/034** (console 2), buttons SW1–4 → parallel-input bits **D0–D3** / **D4–D7** of **030**, and the speaker → analog **output** 031 / 033. **⚠ The button read polarity is NOT stated in the manual** — settled as **active-low** (pressed = 0), 2026-07-23 (all sources provided by Patrick, all agreeing): the s100computers.com **Dazzler II** board (a modern card implementing the D+7A joystick core circuit — port 18H *"Low if pressed", "+5V from 10K source"*, and the same two's-complement scale), the **ADCTEST** diagnostic (`adctest.hex`, disassembled — reads `IN 18H` buttons and `IN 19H/1AH/1BH/1CH` axes, confirming the port map), and David Hansel's Arduino Altair 8800 simulator firmware (builds the byte with `if (!joy->bN) ... |= bit`). None of these is a Cromemco manual, so they corroborate rather than join the manifest (`reference/JS-1.md` §3). **The three disagree on one output-only point** — console 2's speaker is `OUT 1BH` per the JS-1 manual but `OUT 1AH` on the Dazzler II — which affects only a future sound path, not joystick reads. |
| `Dazzler Games.pdf` | Cromemco *Dazzler Games* manual, 44 pp, **has a text layer**. Fetched 2026-08-03 from s100computers.com (`.../Hardware Manuals/Cromemco/Dazzler Games.pdf`). Prints the **full 8080 source listings** of the Dazzler games (**Dazzle-Doodle** pp. 5–7, plus Track/Chase). A genuine Cromemco artifact — the manufacturer's own program code, not a third-party disassembly. | `reference/JS-1.md` §4.1, `src/boards/cromemco-d7a.{h,cpp}`. Authoritative for the **JS-1's usable analog window as the games actually use it**: Doodle's per-axis `ADI 0x40; JP` range-check draws only for readings in **−64…+63 (`0xC0`…`0x3F`)** and discards anything of magnitude ≥64 on the positive side as "out of range"; its `CMA` on Y confirms the **Y-inversion**. This is why the emulator maps SDL full deflection to **+63 / −64** (a `>>9`), not the A/D rails ±127 — a fully-deflected host stick must land inside this window or Doodle won't draw. |
| `730000_Sol_Systems_Manual_Jan1978.pdf` + `roms/SOLOS/SOLOS13.ASM` | Processor Technology *Sol Systems Manual*, Jan 1978, and the **SOLOS 1.3 source** (© 1977 Processor Technology Corp., the "SOLOS 4-1" EPROM revision) it drives. Manual fetched from deramp.com (*Sol-20 hardware*); corroborated by the deramp.com *Sol-20 Restoration* notes (`.../processor_technology/sol-20/hardware/Sol-20 Restoration.pdf`). | `reference/Sol-20.md`, `src/boards/proctech-sol.{h,cpp}`, `docs/boards/proctech-sol.md`, `machines/sol20.toml`, `roms/SOLOS/`. Authoritative for the Sol-PC's **`F8h`–`FFh`** onboard I/O: serial status/data `F8`/`F9` (**active high**, D6 RX-rdy / D7 TX-empty), the **shared status register `FAh`** that multiplexes keyboard (D0), parallel (D1/D2) and tape (D3/D4/D6/D7) with **mixed polarity** (kbd/parallel active **low** — the drivers `CMA`-invert — tape active **high**) plus the `OUT FAh` tape motor (D7/D6) and 300-baud select (D5), tape data `FB`, keyboard data `FC`, parallel data `FD`, the **VDM display parameter port `FEh`** (low-4-bit scroll, fixed on the Sol vs the stand-alone VDM-1's page-tied port), sense `FF`, and the `C000`–`CFFF` ROM/scratch/video memory map. **Polarity was settled from the SOLOS driver code, not the `EQU` names.** The `HELIOS`-conditional disk ports `F0`–`F7` are **not** part of a stand-alone Sol-20. |
| `SOLOS.pdf` | Processor Technology / Software Technology Corp. *SOLOS/CUTER User's Manual*, document **M0100**, © 1977, 31 pp, **real text layer**. Fetched 2026-07-18 from deramp.com (`.../processor_technology/sol-20/software/manuals/SOLOS.pdf`). The *software* companion to the *Sol Systems Manual* — the monitor that lives in `builtin:solos`. | `reference/SOLOS.md`, `roms/SOLOS/`, `machines/sol20.toml`. The SOLOS/CUTER **command set** (5 console incl. `ENTR` = the `.ENT`-file loader, 4 tape, 10 `SET`), the input/output **pseudo-ports** (0 keyboard/VDM … 3 user), and the **machine-language interface**: entry register conventions (`SP` primed into the SOLOS stack for a `RET` to command mode, `HL` at the jump-table base), the **`C000` jump table** (`START` `00`/`7F` SOLOS-vs-CUTER discriminator, `INIT`/`RETRN`/`FOPEN`/`FCLOS`/`RDBYT`/`WRBYT`/`RDBLK`/`WRBLK`/`SOUT`/`AOUT`/`SINP`/`AINP` — the last two returning **status in the zero flag, char in `A`**), the **VDM display driver** control codes (`01`/`0B`/`0D`/`13`/`17`/`1A`) and `1B` cursor/speed escape sequences, and the cassette **file-header** layout (name/type/size/addr/xeq, TYPE bit 7 = non-executable). **`.ENT` files are console text loaded via `ENTR`, not a CUTS tape image.** |
| `CPM 2.2 Manual.pdf` | Digital Research *CP/M Operating System Manual*, the reformatted 2.2 compilation (© 1982), 317 pp, **real text layer**. Fetched 2026-07-17 from deramp.com (`.../altair/software/manuals/CPM 2.2 Manual.pdf`). Six sections: CCP, ED, ASM, DDT, BDOS interface, BIOS. | `src/core/symbols.{h,cpp}` and `docs/manual/debugging.md` (the **`ASM` `.PRN`/`.HEX`** the loader reads — 16-column geometry, `=` EQU marker, **decimal** default radix), plus the OS altairsim boots on the 88-DCDD/88-MDS: the **page-zero contract** (0000 warm-boot / 0005 BDOS / 005C FCB / 0080 DMA / 0100 TPA) and the **BDOS** call convention. **`DDT`** is the period analogue of altairsim's own debugger (`X`/`T`/`G`/`L`/`D`/`A`/`S`, the `CfZfMfEfIf A=…` register line, RST-7 breakpoints). Distilled to the toolchain-relevant parts; ED and the BDOS/BIOS internals are not modeled. |
| `SBC-100 Manual.pdf` | SD Systems *SBC-100 Operations Manual*, Rev A, May/July 1980, 50 pp. **No text layer — read as page images.** Fetched 2026-07-25 from deramp.com (`.../sd_systems/Manuals/SBC-100%20Manual.pdf`). | `reference/SD Systems SBC-100 & SBC-200.md`, and (planned) an `sbc100`/`sbc200` board. The Z80 single-board computer's port map (8251 USART console `7C`/`7D`, MK3882 CTC `78`–`7B`, parallel `7E`/`7F`), the auto-start-at-a-4K-boundary reset (released by a **read of port `7F`**), the X1/X2/X3 memory-mapping headers, and the 110–9600 baud table. Documented **together with the SBC-200** (they share the whole port map). |
| `SDS_SBC-200.pdf` | SD Systems *SBC-200 Operations Manual*, Rev C, Aug 1981 (SD #7140052), 41 pp. **No text layer — read as page images.** Fetched 2026-07-25 from deramp.com (`.../sd_systems/Manuals/SDS_SBC-200.pdf`). | `reference/SD Systems SBC-100 & SBC-200.md`, and (planned) an `sbc200` board. The 4 MHz Z80A revision of the SBC-100: same port map, plus the two things the SBC-100 lacks — **`OUT 7F` bit 1 switches the onboard memory out/in** (§2.4.4) and the X22 DMA driver-disable jumper — and the 150–9600 baud table with the USART ÷64/÷16 split and the `45h`/`4Fh` init bytes. The 8251 status polarity, CTC baud generation and auto-start are read out of the **MSMONR21.Z80** monitor's own init/poll code (the manuals give no bit numbers). |
| `VersaFloppy-I Manual.pdf` | SD Systems *VersaFloppy (I) Manual*, 84 pp. **No text layer — read as page images.** Fetched 2026-07-25 from deramp.com (`.../sd_systems/Manuals/VersaFloppy-I%20Manual.pdf`). | `reference/SD Systems VersaFloppy.md`, and the `versafloppy` board. The **single-density FD1771B-1** controller: the 5-port decode at `63`–`67h` (window X=6 / E), the board control register at `63h` (D0–3 drive / D4 side / D5 restore / **D6 wait-enable** / D7 int-enable) and its readback+INTRQ status, the **wait-state (PRDY) data transfer on port `67h`** (DRQ is not polled), the FD1771 command codes (Table 8-1), and the 8″/5″ single-density geometry. **⚠ Port `60h`** ("RESET" in the DDBIOS driver) is **not decoded by the VF-I board** — emulate as a no-op. |
| `VersaFloppy-II Manual.pdf` | SD Systems *VersaFloppy II Operations Manual*, incl. the 11-Sep-1979 addendum, 34 pp, **real text layer**. Fetched 2026-07-25 from deramp.com (`.../sd_systems/Manuals/VersaFloppy-II%20Manual.pdf`). | `reference/SD Systems VersaFloppy.md`, and the `versafloppy` board. The **single+double-density FD1791B-1** controller — same port layout as the VF-I but the `63h` control register differs (**D5 = 5″/8″, D6 = density, D7 = wait-enable**); Output/Input Port 63 §2-13/2-14, the command table (Table 8-1) and the error-status byte (Table 8-2). The FDC register order (`64`–`67`) and the runtime command bytes and per-density geometry are cross-checked against **DDB200.ASM** (the DDBIOS driver) and **VF2ROM.Z80** (the boot-ROM disassembly), which are the authoritative artifacts for the numbers the manual leaves to software. |
| `SDOS.pdf` | SD Systems *SDOS User's Guide* (Disk Operating System), 104 pp, **real text layer**. Fetched 2026-07-25 from deramp.com (`.../sd_systems/SDOS/SDOS.pdf`). | `reference/SD Systems SDOS.md`. SDOS is a **CP/M work-alike** — the page-zero contract (warm boot `0000`, `CALL 5` system-call vector, FCB `005C`, DMA buffer `0080`), the BDOS-numbered system-call table (0–27 as CP/M plus SD extensions 128/129/151), the resident-in-top-7 KB memory map, and the cold-boot flow. Something SDOS software runs *on*, not something the simulator emulates. The disk geometry/BIOS linkage is shared with the VersaFloppy reference (both from **DDB200.ASM**). |
| `SDS_Monitor.pdf` | SD Systems *SD Monitor Instructional Publication*, SD #7140011, Rev B, March/April 1981, 18 pp. **No text layer — read as page images.** Fetched 2026-07-25 from deramp.com (`.../sd_systems/Manuals/SDS_Monitor.pdf`). | `reference/SD Systems Monitor.md`. The SBC-100/200 boot/debug PROM monitor (v2.10) at `E000h`: the `.` prompt, the full command set (memory `D/E/F/M/L/T/V`, port `I/O/P`, program-control `B/G/S/X/H`, disk `C/R/W/Z/Q` with the format-type codes), the Z80 register-save map at `FFE6`–`FFFFh`, and the 3-byte-`JMP` breakpoint model. The console-port constants and the SD-vs-MS build difference come from the two ROM sources **SDMONV21.Z80** ("SD monitor", console `01`/`00`) and **MSMONR21.Z80** ("MS monitor", 8251 at `7C`/`7D` with auto-baud); the manual prints neither the port addresses nor a version banner. |
| `SD Systems Editor Assembler Linker.pdf` | SD Systems *Editor / Assembler / Linker Operations Manual* (Text Editor V1.0, `ZASM` "Z80 Global Assembler" V3.3, `LINK` V3.1), © 1978 with a 1979 addendum, 60 pp, **real text layer**. Local copies under `~/…/sd-systems/TOOLS/` and `.../SDOS/`; on deramp.com (`.../sd_systems/Manuals/SD%20Systems%20Editor%20Assembler%20Linker.pdf`). Companions `SD Systems Z80 Assembler.pdf` / `SD Systems Z80 Linker.pdf`. | `reference/SD Systems Editor Assembler Linker.md`. Not hardware — the **toolchain that built the SD ROMs**: the `ZASM` two-pass invocation/options, the Z80 assembly it accepts (radix suffixes `H`/`Q`/`B`/`D`, the expression hierarchy and `.AND.`/`.SHR.`-style operators, and the pseudo-ops `DEFB`/`DEFW`/`DEFS`/`DEFM`, `EQU`/`DEFL`, `ORG`, `IF`/`ENDIF`, `INCLUDE`, `PSECT REL`/`ABS`, and the single `GLOBAL` that serves as both `PUBLIC` and `EXTRN`), the relocatable `.OBJ` module model, and `LINK`'s `/A=hhhh` origin + Intel-`.HEX` output — i.e. how `SDMONV21.Z80` / `MSMONR21.Z80` / `DDB200.ASM` become the `builtin:` ROM images (`docs/roms.md`). |
| `pmmi-mm-103-modem-and-comm-adapter.pdf` | PMMI *MM-103 Modem and Communications Adapter Owner's Manual*, © 1982 PMMI Communications, Falls Church VA (warranty effective 1-Mar-1979), 32 pp / ~31 printed pages, 20 MB. **Has a good OCR text layer** for §7 (the register spec), §9 (interrupts) and §10 (the MC6860 reprint) — the whole load-bearing part; the §8 program listings and the figure pages OCR poorly. Fetched 2026-07-27 from s100computers.com (`.../Hardware%20Manuals/PMMI/`). **Over `WebFetch`'s 10 MB limit** — stream it with `curl -sL "<url>" \| pdftotext -q -f <first> -l <last> - -`. | `reference/PMMI MM-103 Modem and Comm Adapter.md`, `docs/boards/pmmi-mm103.md`, and (planned) a `pmmi` board. A **Bell 103 originate/answer modem on one S-100 card** — MC6860L modem chip, an unnamed 1602-family UART, rate generator, pulse dialer, dial-tone and ring detectors, maskable interrupts. Authoritative for: the four-address block in which **`IN` and `OUT` are different registers** (6-position DIP on A2–A7, **OPEN/OFF = 1**, default **`C0`**, North Star **`E0`** because North Star DOS uses `C0` for memory protect), all six register bit layouts (**Figure 7.0**), the `250,000/(N×16)` baud formula and the `250,000/(N×100)` **40%-high/60%-low** timer pulses off the *same* divisor, `IN BA+3` as a **side-effect strobe** that loads the interrupt mask **and destroys the baud divisor**, the pulse-dial and count-the-rings auto-answer procedures, 6860 handshake timing (51 ms SH/RI hold, 2 s billing delay, 17 s timeout, 150/450/750 ms originate handshake), the four non-queueing interrupt sources with **no vector** (factory jumper E9–E10 → floating `0xFF` → `RST 7`), and the **INTA / pin-96 field modification** (cut pin 96, jumper U12 pin 5 → P3 pin 13, then supply INTA in software via Aux Out 3). **⚠ ITS OVERBAR CONVENTION IS STATED BACKWARDS** and **its two dialer programs dial opposite phases of the timer bit** — see the traps below. **There is no schematic** (none was ever shipped), so §8's ten worked programs are the only cross-check on the prose. **The E-pad ↔ VI-level mapping is never stated.** |
| `Cromemco PRI Printer Interface Manual.pdf` | Cromemco *PRI Printer Interface* manual, © Cromemco 1978/1980, 13 pp. **No text layer — read as page images.** Fetched 2026-07-29 from s100computers.com (`.../Hardware Manuals/Cromemco/Cromemco PRI Printer Interface Manual .pdf`). | `reference/Cromemco PRI Printer Interface.md`, and (planned) a `pri` board. One S-100 board driving **two printers at once** — a 3703/3779 dot-matrix channel (ports `53`/`54`; 7-bit ASCII + active-low DATA STROBE bit 7; status BUSY=bit 5 active-high, ACKNLG=bit 7 active-low) and a 3355A daisy-wheel channel (ports `5A`–`5D`, **all connector signals active-low**: a 12-bit carriage/paper-feed position + direction across `5A`/`5B`, a six-command control byte at `5C`, a five-bit status at `5A` IN). Default base `05H` (fixed low nibble; relocatable only by a trace-cut DIP mod), interrupt-enable at bit 2 of `53`/`5D`, the ~1 s **automatic ribbon-lift** timer, TU-ART-style Z-80/8080 vector-gating. Internally consistent — no erratum. The printers' own glyph/character sets live in the printer manuals, **not** this interface manual. |
| `Cromemco 4FDC Manual.pdf` · `16FDC.pdf` · `Cromemco 64FDC Manual.pdf` | Cromemco *4FDC* (© 1977, 49 pp), *16FDC* (© 1979–81, 82 pp) and *64FDC* (© 1983, 88 pp) floppy-disk-controller manuals. **No text layer — read as page images.** Fetched 2026-07-29 from s100computers.com (`.../Hardware Manuals/Cromemco/{Cromemco 4FDC Manual,16FDC,Cromemco 64FDC Manual}.pdf`). | `reference/Cromemco 4FDC 16FDC 64FDC Floppy Controllers.md` — **one grouped distillation** with a per-board differences table. A WD **FD177x** core (4FDC = FD1771, single-density FM only; 16FDC/64FDC = FD1793, single **and** double density + PLL; 64FDC adds write precompensation) wrapped with an onboard **TMS 5501** serial console (fixed ports `00`–`09`, same register model as the TU-ART — see `Cromemco TU-ART.md`) and an **RDOS** boot PROM at `C000`. Authoritative for the board wrapper: the **fixed, non-relocatable** port block (`00`–`09` / `30`–`34` / `40`), the port-`34` disk-flags/control register (**Auto-Wait** CPU-stall transfer; Double-Density bit on 16/64 only), the board-specific port-`04` PerSci drive-mechanics register (which shrinks across the three boards), the `OUT 40H` ROM-disable, and the **16FDC-only** 512 ms RTC + Z80-Mode-2 interrupt jumpers. FD177x chip internals defer to `Western Digital FD1771 - Datasheet.md`. No erratum found. |
| `Cromemco CDOS 0236 023-0036 198106.pdf` | Cromemco *CDOS Operating System Instruction Manual*, Part No. 023-0036, © 1978, 1981, June 1981 printing, 254 pp. **Scanned, no text layer, but reads cleanly as page images** (RC4-encrypted against printing/editing, not against reading). Provided directly by Patrick, 2026-07-30. | `reference/Cromemco CDOS.md`. Cromemco's own Z-80 OS for the 4FDC/16FDC/64FDC family — a **CP/M 1.3 work-alike** (all 27 CP/M system calls, numbered identically) licensed from Digital Research for the data structures/UI, plus Cromemco-only extensions from 128 up (disk labels, multiply/divide, program chaining, a real-time clock). Not itself emulated — software the emulated Z-80 boots and runs, the same relationship `SD Systems SDOS.md` documents for SDOS. Authoritative for: the **RDOS→CDOS boot handoff** (disk-controller switch 3 ON = silent auto-boot, OFF = RDOS's `;` prompt needing `b`+RETURN; up to 4 RETURNs to train console baud), the CP/M-standard page-zero map (`0000` warm boot, `0005` `CALL 5` vector, `0008`=`FFH` under CDOS/`C3H` under Cromix), the 5-way disk-geometry/type-specifier table (self-describing via bytes 121–128 of sector 1) with track-0/side-0 always forced single-density so RDOS can boot it, the FCB/directory-entry/disk-label byte layouts, and the full system-call table (0–27 CP/M + 128–159 CDOS extensions) with entry/return registers. No erratum found beyond one internal terminology wrinkle (a System-Area/File-Area-start table reuses "DD" for double-*sided* where the rest of the manual uses "DS" — flagged in-file). |
| `CompuPro System Support 1 Manual.pdf` | CompuPro/Godbout *System Support 1 Technical Manual*, © 1981 (Doc #11620, Board 162 Rev G; printings Jul 1981–Dec 1983), 106 pp. **No text layer — read as page images.** Fetched 2026-07-29 from s100computers.com (`.../Hardware Manuals/CompuPro/CompuPro System Support 1 Manual.pdf`). | `reference/CompuPro System Support 1.md`. A **multifunction** S-100 board (**not** Cromemco — casually miscalled so): one 16-port block (standard base `50H`) packing a **2651 UART** serial console (TxRDY=D0/RxRDY=D1 active-high; the DCD/DSR status bits and DTR/RTS command bits are **inverting** — a set bit drives the RS-232 line low/asserted), dual **8259A** master/slave interrupt controllers (15 sources), an **8253** interval timer, an **MSM5832** BCD RTC (both seconds digits always read/write 0), and a **9511A/9512** math-chip socket (stack misalignment is reset-only). **⚠ The 8080/Z80 `PHANTOM*` trick:** the 8259A answers INTA with a 3-byte CALL, but an 8080/Z80 fetches the last two bytes as ordinary memory reads — so the board asserts `PHANTOM*` to blank all system RAM/ROM during them (jumper J13 selects 8085/8088/8086 vs Z80/8080; this is why every memory board must respond to `PHANTOM*`). No erratum found. |
| `CompuPro Interface II Manual.pdf` | CompuPro/Godbout *Interfacer II User's Manual*, © 1981 Godbout Electronics, 22 pp. **No text layer — read as page images.** Fetched 2026-07-29 from s100computers.com (`.../Hardware Manuals/CompuPro/CompuPro Interface II Manual.pdf`). | `reference/CompuPro Interface II.md`. **Three full-duplex latched-TTL parallel ports + one 1602/1863 UART serial channel**, each relocatable anywhere in the 256-port space (serial 2-port block, parallel 4-port block). Serial status all active-high (TBMT=D0, DAV=D1, cross-confirmed by the manual's own `ANI 02H`/`ANI 01H` test loops); UART framing set by eight power-up-jumpered latch bits the control port can flip at runtime; parallel status interleaved DAV0/TKN0/DAV1/TKN1/DAV2/TKN2 (D0–D5); a shared programmable timer-interrupt; all interrupts on the S-100 `VIx` lines (`sINTA` unused). **⚠ Reversed bit order:** the UART-programming jumper-header table (p.4) and the control-port bit-assignment table (p.5) list the same eight signals in opposite bit order without saying so — key off signal name, not position. |
| `CPU 8085-88 Manual.pdf` | CompuPro/Godbout *CPU 8085/88 Technical Manual* (Godbout Electronics; component layout © 1981, board 161 Rev F; manual dated 2/83), 31 pp, **real text layer** (the schematic/logic sheets pp. 24–29 are page images). Fetched 2026-08-08 from s100computers.com (`.../Hardware Manuals/CompuPro/CPU 8088-88 Manual.pdf`). | `reference/CompuPro CPU 8085-88.md`. A **dual-processor** IEEE 696 / S-100 CPU board — **8085A-2** (8-bit, 2/6 MHz via paddle S4) + **8088** (8-bit bus, fixed 8 MHz), only one on line at a time. **One shared I/O port (standard `FDH`; DIP S3, `ON`=0) is both the processor-swap trigger and the Memory Manager:** `IN` toggles which CPU runs (destroys A, returns `FFH`); `OUT` latches the **16 MB extended-address** byte onto A16–A23 (8088 in control ⇒ only top 4 bits used → A20–A23, as the 8088 natively drives A0–A19; 8085 ⇒ all 8 bits). Power-up: **8085 in control at `0000H`** or the S2-selected **power-on-jump** page (forced `C3 00 pp`); 8088 held, first entry `FFFF0H`. Reset-On-Swap S1-4/S1-5 (**both OFF for DMA**) picks RESET vs. resume-in-place per CPU; one-wait-state-per-I/O-cycle S1-3; MWRITE S1-7; IMSAI panel J2/J6; reset coupling J7 (A-B shipped = both reset together, B-C = 8088 power-on reset only). **⚠ Internal contradiction:** Circuit Description (p.13) says X2=24 MHz / X3=12 MHz (÷3 → 8088 @8 MHz, ÷2 → 8085 @6 MHz), but the Parts List (p.22) says X2=15 MHz / X3=10 MHz; X1=4 MHz agrees. Prose matches the cover's 8/6 MHz claim and is the likelier intent — flag if timing ever depends on it. |
| `Pascal 100.pdf` | Digicomp Research *Pascal-100 User's Manual*, Second edition March 1981, 270 pp. **OCR text layer** (ABBYY FineReader — occasional OCR slips in prose; all tables/addresses verified). Appendices B/F/G/H © 1979 The MICROENGINE Company (Western Digital), appendix C © 1977 Mostek — reprinted vendor material. Supplied by Patrick 2026-08-19. | `reference/Digicomp Research Pascal-100.md`. **Not emulated** — reference for possible future S-100 CPU-board support. A **dual-processor 16-bit S-100 CPU boardset** (two cards, cabled): a **Western Digital Pascal Microengine (WD9000)** that executes **UCSD Pascal III.0 P-code natively** + a stock **Z80** (I/O, interrupts, CP/M); only one runs at a time, the other in S-100 HOLD. Authoritative for: the **two-port interface** — even **System Control Port** + odd **Map Output Port** (DIP U225, top 7 bits, default `D0`/`D1`) — and its **write-only byte table** (`80`/`00` swap processor; `C0`/`40`, `C1`/`41` enable/disable the address-translation Map for Microengine/Z80; `C2`/`42` NMI-on-protection-violation; `C3`/`43` auto-transfer-to-Z80-on-interrupt; `C4`+`44` interrupt the Microengine; `45`/`C5` select Map half to load); the **Memory Map** (Standard: 16×4-bit 8K pages → 128K on A13–A16; Extended option: 64×9-bit 2K pages → 1M on A11–A19, or 512K + write-protect; loaded Z80-only via `OUT (C),A` with **contents INVERTED**); **Microengine memory-mapped I/O** (word `FFpp`=8-bit port `pp`, word `FEpp`=16-bit via sSIXTRQ/SIXTN; Pascal `PORTIO` uses `pp−256`); the **Z80 Power-On-Jump** (`C3 00 X0` synthesis, DIP U242 A12–A15, jumper J202, default `0000` — same trick as [[MITS Turn Key Board]]/[[CompuPro CPU 8085-88]]); the **interrupt model** (pINT→Z80 IRQ, NMI→Z80 `CALL 66h`, all handled by Z80; Microengine int response writes scratch to byte `1F880/1` and reads a vector from `1F8C0/1` then SIGNALs a semaphore); the **byte=word×2** addressing rule, the **reset init vector at byte `1F8D0`** (word `FC68`), the **boot sequence** (hard boot → SBQB+BIOS at `280h` → soft boot loads Pascal boot at `5000` and fires the Microengine → Pascal boot loads SYSTEM.Z80BIOS/Z80CODE/PASCAL), the **ZMCOM** interprocessor area at byte `80h`, and the IEEE-696 bus behavior (16-bit transfers, low-order-even byte sex, PHANTOM>64K jumper J203, jumper table III-1). **⚠ Manual erratum:** the interrupt vector's word address is printed "FC80" but byte `1F8C0`÷2 = **`FC60`** (the byte addresses are authoritative). The reprinted WD Microengine data sheet (app. B), Z80 sheet (app. C — see [[Zilog Z80]]) and P-machine description (app. F) are **not** transcribed. |
| `CompuPro Disk 1 Manual.pdf` | CompuPro/Godbout *Disk 1 Technical Manual*, © 1981 (board 171F; First–Fourth Printings Jul 1981–Mar 1983), 131 pp. **No text layer — read as page images.** Includes a reprint of Intel's *Single/Double Density Floppy Disk Controller* (8272) data sheet (© Intel 1980, pp. 31–46) and full CBIOS/boot source listings. Fetched 2026-07-29 from s100computers.com (`.../Hardware Manuals/CompuPro/CompuPro Disk 1 Manual.pdf`). | `reference/CompuPro Disk 1 & 1A Floppy Controllers.md` (grouped with Disk 1A). The original CompuPro floppy controller: **NEC µPD765A / Intel 8272** FDC + **24-bit arbitrated DMA** (three-deep push-down address stack, MSB first, crosses 64 K) + `PHANTOM*` **256 B boot EPROM** at the CPU reset address + a **software bit-banged serial startup port** (port 3, D7, MARKING/SPACING **inverted** onto D7, cleared to MARKING on reset). Four ports at a multiple-of-four base (std `C0H` 8″ / `CCH` 5.25″): port 0 IN FDC status, port 1 FDC data, port 2 IN board status (**D7** = FDC INT only) / OUT DMA address, port 3 serial + `D0=0` boot-disable. 8 boot routines (S2 pos 1–2 + J17), Z80/8085 and 8086/87 families. Base on **S2** pos 3–8 (`ON`=0), priority on **S1** pos 5–8, wait-enable **S1-1**, boot-enable **S1-4 OFF**. 5.25″ is a solder mod. FDC internals defer to the reprinted 8272 data sheet (no `8272.md` in-tree yet). No internal contradictions found. |
| `CompuPro Disk 1A Manual.pdf` | CompuPro *Disk 1A Technical Manual*, © 1984 (board 203 Rev D, EPROM Version 1.4, Part #290, Doc #12036; Second Edition November 1984), 38 pp. **No text layer — read as page images.** Fetched 2026-07-29 from s100computers.com (`.../Hardware Manuals/CompuPro/CompuPro Disk 1A Manual.pdf`). | `reference/CompuPro Disk 1 & 1A Floppy Controllers.md` (grouped with Disk 1). The 1984 refinement of the Disk 1: same **µPD765A/8272** FDC + **24-bit arbitrated DMA** + `PHANTOM*` boot EPROM (now **512 B** page), but the software serial port is **gone** — console is always a separate board (System Support 1 or Interfacer) chosen by the boot ROM. Port 0 OUT is now a **Drive Select Register** (unit / **8″-5.25″ data-rate** bit 5 / **Force-Two-Sided** bit 3 / alt head-load) and port 3 is a **Motor Control Register** (4× 8″ + 1× 5.25″ motor bits, **bit 7 shared** by 8″ #3 + all minifloppies, **~15 s auto-off** reset on any access, defeated by removing J11) with `D0=0` boot-disable. Port 2 IN board status gains **D0** ready / **D1** index / **D2** sense-switch S3-1 (INT still **D7**). 16 boot routines across **four** CPU families (adds 68000, 32016), S1 pos 2–5. Base on **S3** pos 2–7, priority **S2** pos 4–7, wait-enable **S2-8**, boot-enable **S3-8 ON** — **note every switch identity shifts vs the Disk 1**. Runs 8″ + 5.25″ simultaneously. No internal contradictions found. |
| `FD3700 Interfacing Guide.pdf` | iCOM (Pertec Microsystems Div.) *Interfacing Guide for iCOM Model FD3700 Series Floppy Disk Systems — iCOM Frugal Floppy Model FF37 and Controller/Formatter Model CF3700*, Rev B, April 1978, 28 pp. **No text layer — read as page images.** Fetched 2026-08-08 from deramp.com (`.../altair/hardware/icom_floppy/iCOM FD3712/FD3700 Interfacing Guide.pdf`). | `reference/iCOM FD3712 & FD3812 Floppy Disk Systems.md` (grouped with the FD3812). The **single-density** side of the family: the two-board **CF3700** controller/formatter (Z1 command/data-in P5, Z2 data/status-out P4), Pertec **FD511** drives, and general-µP OEM interfacing (worked 8080, alternate-8080-via-8255, 6800-via-6820, and RCA-1800-via-1852 hookups, for iCOM FDOS-II). Authoritative for the **shared programmed-I/O model**: the negative/zero-true **CPU 0–7 command word** (CPU 0 = strobe), **CDO 0–7** data-out, **DI 0–7** data-in-or-status **multiplexed by CPU 6**, two 128-byte buffers, the `DONE` pulse, the 14 commands with hex codes (`00`/`03`/`05`/`07`/`09`/`0B`/`0D`/`0F`/`11`/`21`/`31`/`40`/`41`/`81`), the status byte, the seek/read/write/read-CRC/DDAM sequences, IBM 3540/3740 SD geometry (77×26×128), and the P4/P5/P8/disk-drive cable pinouts. |
| `FD3812 and FF38-XX Frugal Floppy.pdf` | iCOM (Pertec Microsystems Div.) *FD3812 Double Density Flexible Disk System and FF38-XX "Frugal Floppy" Series User's Guide*, doc 250297A, Rev A (Initial Release), May 1978, 124 pp. **No text layer — read as page images.** ~13.4 MB — over `WebFetch`'s 10 MB limit; download with `curl` and OCR via the Read tool's `pages=`. Fetched 2026-08-08 from deramp.com (`.../altair/hardware/icom_floppy/iCOM FD3812/FD3812 and FF38-XX Frugal Floppy.pdf`). | `reference/iCOM FD3712 & FD3812 Floppy Disk Systems.md` (grouped with the FD3700). The **double-density** successor: a single **S-100 board that interfaces to a MITS 8800** (Multibus optional), Pertec **FD514** drives. Same controller model as the FD3700 **plus** the **Load Configuration command (`15`)** (bit 4 double density / bit 5 format mode), the **double-density track format** (256 B/sector MFM; **⚠ track 0 stays single density**, CRC x¹⁶+x¹²+x⁵+1), the DD geometry (509,184 B/diskette, 62,500 B/s), the J1 I/O / J2 drive / J3 power / J4 LED-status pinouts, the 74LS240 signal levels, the DMA option (`IRSTR`/`IWSTR`), and the SD/DD initialization byte tables. **⚠ Its command table misprints Read (`0B`) and Write (`06`)** — the bit columns and the FD3700 both give `03`/`05`; the reference uses `03`/`05` and flags the misprint. |
| `Single Density Controller (2nd version).pdf` | North Star *Micro-Disk System MDS-A* (single density), © 1977, **Revision 5** (for the MDS-A4 PC board), 33 pp incl. the July 31 1978 errata sheet, ~5 MB. **No text layer — read as page images** (`img2pdf`-wrapped scan; `pdftotext` returns nothing). Fetched 2026-08-08 from deramp.com (`.../north_star/hardware/Single Density Controller/Single Density Controller (2nd version).pdf`). | `reference/North Star MDS Floppy Controllers.md` (grouped with the MDS-A-D). The original North Star floppy controller — **single density (FM) only**, 5¼″ hard-sectored Shugart SA-400, **35 trk × 10 sec × 256 B** (≈89.6 KB/diskette, up to 4 drives). Authoritative for the SD board's **memory-mapped command interface** (not an I/O-port device: a **1 K memory window**, standard origin **`E800H`**, where every command is a memory *read* whose address bits carry the command/data; transfer by **CPU wait-state / `PRDY`**, never DMA/interrupts), the address-**case** map (**0/1 = PROM, 2 = write-data, 3 = command** — different from the DD board), the Case-3 command byte `MO RD BST CC(3) M1 M0` with its 8 command codes, the **two** status bytes (A: `SF WN 0 MO WRT BDY WP TR0`; B: `SF WN 0 MO | SP×4`), the drive-select via binary `M1M0`, the 256-B onboard bootstrap PROM (std PROMs `LE820-3`/`RE820-3`/`SE8-1`), and the 274-B sector format (16 zeros + `FB` + 256 data + rotate-left-XOR check). **⚠ Spin-up slip:** step 1 of the driver reads "wait 1 second (i.e. 5 sector times)" but 5 sector-times = 100 ms; 1 s ≈ **5 revolutions**. **Errata:** Case 0 is now identical to Case 1 (both read the standard PROM); interrupts during transfer corrupt data (disconnect the POLY-88 4.0 RTC interrupt). |
| `Console IO Board.htm` (web page, not a PDF) | S100Computers (John Monahan) *Console IO Board* project page. **HTML, not a scan** — fetched 2026-08-16 from s100computers.com (`My System Pages/Console IO Board/Console IO Board.htm`). No text-layer/OCR concern. | `reference/Console IO Board.md`, and the `propio` board (a `usio` subtype). A **Parallax-Propeller console** (PS/2 keyboard in, VGA out) that presents to the S-100 bus as an ordinary **two-port, polled "status + data" console** with **no UART register map** (the Propeller *is* the terminal). Authoritative for: the two SW2/SW3-selectable ports (**monitor default `00`/`01`**, the board's own test software **`14`/`15`**); the **jumper-configurable** status-bit assignment/polarity (P74–P77) so the board "splices into almost any S-100 system"; and its worked-example convention (the SD Systems 8024 monitor it ships with) — **keyboard/RX ready = status bit 1, active high** (`AND 02H`); **output/TX ready = status bit 2, active high** (`AND 04H`, 0 = busy); a **DATA read clears** the keyboard-ready flag (`INPUT_ENABLE*` → resets U44B), a **DATA write sets** the output-busy flag (`OUTPUT_ENABLE` → U44A). No interrupts. Because the real board is fully jumperable, `propio` presets that one documented convention with every strap still overridable — a faithful model, not invented hardware. |
| `Dual SD card Board.htm` (web pages, not a PDF) | S100Computers (John Monahan) *Dual SD card Board* project page + the `CPM3 HDISK BIOS Software.htm`, `MASTER (V4.53)` monitor listing, and `CPM Card Images Store.htm` pages. **HTML, not a scan** — fetched 2026-08-16 from s100computers.com (`My System Pages/Dual SD card Board/…`) and its Software Folder. No text-layer/OCR concern; the `SD_CARD.Z80` driver and ESP32 firmware source were **not** among the fetchable pages. | `reference/dual-sd-card.md`, and the `dualsd` board. A modern **ESP32-S3 microSD** block device presenting two SD sockets (A:/B:) to the S-100 bus as a **two-port programmed-I/O command engine** (status/command `80H`, data `81H`, DIP-relocatable). Authoritative for: the **`33H`-lead two-byte protocol** and its 8 commands (`80`/`81` INIT A/B, `82`/`83` SEL A/B, `84` SET_TRK_SEC, `85` READ, `86` WRITE, `87` FORMAT-with-`E5`, `88` RESET), the **DI7 = data-ready / status-bit-0 = write-busy** byte handshake (verbatim `GET_DATA`/`SEND_DATA` fragments), **512-byte sectors**, and the CP/M 3 hard-disk **DPB** (512 B/sector, 61 sectors/track, 256 tracks, 2 KB blocks, 1024 dir entries, 1 reserved track → ~7.63 MB/volume; `LBA = track·256 + sector`, so the `OFF=1` reserved track puts the directory at **LBA 256**, confirmed against the shipped image). Boot is the **MASTER Z80 monitor `P`/`I` commands** (12 sectors from track 0 → RAM 100H → jump); the board has **no boot PROM**. **Resolved since (from obtained source, not the web pages):** the `SET_TRK_SEC` byte order and `track·256 + sector` LBA formula, the `0xFF` erased-sector fill (`0xE5` is only what FORMAT writes), the MASTER V6.6 `I`-command direct-SD boot, and that `.imgc` is an HDD-Raw-Copy container (decode with `unimgc`) — all pinned in `reference/dual-sd-card.md` from `S100_ESP32_Firmware_v1.5.ino`, `SD3.ASM`/`HBOOT3.ASM`, and the shipped `examples/dualidesd` card. |
| `HIDE3.ASM` combination CP/M 3 BIOS (+ `HBOOT3.ASM`, `HDRVTBL3.ASM`) | S100Computers (John Monahan) **"IDE-AB CF+ESP32-CD SD"** distribution — the combination CP/M 3 hard-disk BIOS source (IDE/CompactFlash **and** Dual SD in one driver), dated **1/24/2026**, supplied by Patrick 2026-08. Not a fetchable web page; the shipping combination `CPM3.SYS` source. Nothing redistributed. | `reference/dual-ide-card.md`, and the `dualide` board — the **8255-based IDE/CompactFlash half** of the combination board (drives **A:/B:**; the SD half is the [[dual-sd-card]] `dualsd` board, drives C:/D:). Authoritative for: the **five 8255 ports** (A/B data `30`/`31`, C control `32`, mode config `33`, drive select `34`), the **IDE control-line encoding** (`A0=01 A1=02 A2=04 CS0=08 CS1=10 WR=20 RD=40 RST=80`) and the ATA register file (`REGdata=08 … REGcmd/REGstatus=0F`), the strobe-based register read/write (`IDEwr8D`/`IDErd8D`) and 16-bit sector loops (`WRSEC1`/`MoreRD16`, low byte on A then high on B), the status polling (`IDEwaitnotbusy` `(st&0xC0)==0x40`, `IDEwaitdrq` `(st&0x88)==0x08`, ERR bit 0), the commands (`READ=20 WRITE=30 RECAL=10 INIT=91 SPINDOWN=E0 SPINUP=E1 IDENTIFY=EC`), and the LBA math (`REGsector | REGcylLSB<<8 | REGcylMSB<<16 | (REGshd&0x0F)<<24`, offset `LBA·512`). **Proves the card is portable between the halves:** the CF `IDEHD$DPB` and SD `SD$DPB` are the identical `DPB 512,61,256,2048,1024,1,8000H`, and the CF `wrlba` and SD `SD$wrlba` build the same LBA with the same byte order. Boot is the **MASTER Z80 monitor `P` command** (`HBOOTCPM`, 12 sectors from LBA 1 → RAM 100H → jump); no boot PROM. Ready images: **Image-1** (CF only) and **Image-14** (CF + SD). |
| `Z80 CPU Board.htm` (web page, not a PDF) + `MASTER` V6.6 source | S100Computers (John Monahan) *V2 Z80 CPU Board* project page (`My System Pages/Z80 Board/Z80 CPU Board.htm`, fetched 2026-08-16, HTML not a scan), plus the **MASTER Z80 Monitor V6.6** assembler source (`Master0.z80` / `Master1.z80` + assembled `MASTER0.HEX` / `MASTER1.HEX`, `ORG 0F000H`) supplied by Patrick 2026-08-16. | `reference/v2-z80-cpu-board.md`, and the `v2z80rom` board (the card's onboard monitor EEPROM + Power-On-Jump; the Z80 itself is `mits-z80cpu`). Authoritative for: the **8K 28C64 EEPROM** at `F000`–`FFFF` as **two 4K pages** (low = `master0`, high = `master1`); **`OUT D3H` bit 1 = EEPROM A12** (`0`=low, `2`=high) and **bit 0 = ROM inactivate**, `D3H` write-only, reset default `00` (EEPROM on, low page); the memory-manager ports `D2H`/`D3H` (jumper `P2 5-6`) which `altairsim` does **not** model (Dual SD target is non-banked flat-64K CP/M 3); and the **Power-On-Jump NOP-injection slide** (U15 comparator vs jumper `P3`, default `F000`; U18 forces `00`/NOP on the bus until A12–A15 match, one-shot armed by reset). The `master0`/`master1` page images are embedded as built-in ROMs (`docs/roms.md`); the `I` command boots CP/M 3 off the [[dual-sd-card]]. |
| `SD Systems ExpandoRAM I.pdf` | SD Sales / SD Systems *ExpandoRAM* (the original "I"), © 1977, 39 pp. **No text layer — read as page images.** Fetched 2026-08-17 from s100computers.com (`Hardware Manuals/SD Systems/SD Systems ExpandoRAM I.pdf`). | `reference/SD Systems ExpandoRAM.md`. The first-generation S-100 DRAM board (MK4115/MK4116). Authoritative for the fact that matters most to the banking audit: **it has no I/O port at all** — Table 1-2's J1 pinout carries only memory-bus signals, so it cannot decode an `OUT`. Its "banks" are four fixed groups of 8 chips strapped to 8K/16K address slices by an 8-position DIP switch (U2 → 74LS138 → RAS0–3), a static memory-mapped board; plus a 5-position write-protect/output-disable switch (U1) and a PHANTOM jumper. See `docs/devguide/banked-ram.md` — because it has no port, it is modelled by the plain `memory` board, not the `bankmem` bank switcher. |
| `SD Sales ExpandoRAM-II.pdf` | SD Systems *ExpandoRAM-II*, © 1979 (this printing "Revision C, March 1981"), 31 pp. **No text layer — read as page images.** Fetched 2026-08-17 from s100computers.com (`Hardware Manuals/SD Systems/SD Sales ExpandoRAM-II.pdf`). | `reference/SD Systems ExpandoRAM II.md`. The second-generation board — a **different design** from the I. Authoritative for: **port FF** takes a **page number** (0–9) that an on-board **82S130 PROM** (U8) decodes, with octal board-select + bank-enable DIP switches (S3), into a **32K or 48K partition** (6 boards with a 32K PROM / 8 with a 48K PROM); 4 banks of 8 chips/board (16K or 64K DRAMs → 64K or 256K). ⚠ The per-cell PROM page map (Fig. 2-4, bound out of order in the scan) is not reliably transcribable — get the exact truth table from the schematic or a PROM dump. Implemented as `bankmem card=expandoram2` — a documented binary page-select approximation, because that PROM map cannot be sourced (`docs/boards/bankmem.md`, `docs/devguide/banked-ram.md`). |
| `SDS COSMOS OS User Manual 198106.pdf` | SD Systems *Communication Oriented Multi-User Operating System (COSMOS) — Instructional Publication*, SD #7140075 Rev. B, Version 2.17, June 1981, 94 pp. **No text layer — read as page images.** Provided by Patrick, 2026-08-18. | `reference/SD Systems COSMOS.md`. SD Systems' CP/M-2.x-compatible **multi-user** OS (up to 8 users) — software the emulated Z-80 boots and runs, not itself emulated (the SDOS/CDOS relationship). Authoritative for the **guest-side story behind the ExpandoRAM II banking**: COSMOS is **page-mode**, writing a page number to **port FF** to select a **32K or 48K ExpandoRAM II partition** (§2.2, §6.2.1); **page 0 is 64K with resident COSMOS in the top 16K/32K** and the user-0/console-processor overlay below (Fig 2-1, re-entrant single copy); the two supported memory systems are **3 boards→192K (32K users)** and **4 boards→256K (48K users)**, each board carrying two stacked partitions (Figs 6-1/6-2). Also authoritative for the **ExpandoRAM II config COSMOS requires** — jumper E9→E10, PROM **EX-32 (7010392)** / **EX-48 (7010393)** per partition size, and **S3 board-select** per Table 6-1 (§6.2.3.3) — none of which `bankmem card=expandoram2` yet models (see the ⚠ in the `.md`). Plus the CP/M-2.x page-zero map (Table 2-1), the COSMOS system-call extensions (Table 5-6: 128–149 SD calls, 192–200 multi-user START/STOP/MOUNT/DISMOUNT/GET USER STATUS/DISPATCH MESSAGE/RECORD LOCKING/ATTACH/IDENTIFY), the I/O device mnemonics (Table 2-2), the boot/`MCOSMOS.CMD` config flow (§6.3), and the SD disk-config table (Appendix A; distribution disk is config C/D, 256-B-sector DD 8"). ⚠ CP/M **2.x**, not CP/M 3 — a generic CP/M 3 diskette will not exercise this board's paging. |
| `64K DRAM Manual.pdf` | Vector Graphic *64K Dynamic RAM Board* manual, © 1979, 30 pp. **No text layer — read as page images.** Fetched 2026-08-17 from deramp.com (`.../vector_graphic/hardware/64K DRAM Manual.pdf`). | `reference/Vector Graphic 64K Dynamic RAM.md`. Z-80-only S-100 DRAM board. Authoritative for: **port 40H, one-hot** bank select (`1<<bank`, `0x01`→0 … `0x80`→7), 8 banks / ≤8 boards / 512K; **bank 0 force-enabled at power-on/RESET** (theory of op p. 25: POC clears the port-40H latch and DO0 is pre-inverted so the cleared state is electrically `0x01`); jumper "area D" trims the top in 8K steps (ships at the 56K option, not full 64K). Implemented as `bankmem card=vector` — the OASIS `0x41`/`0x42` quirk is empirical, not in this manual (`docs/boards/bankmem.md`). |
| `North Star 64k HRAM.pdf` | North Star Computers *64K HRAM* (HORIZON RAM) user/technical manual, © 1981, 64 pp. **No text layer — read as page images.** Fetched 2026-08-17 from deramp.com (`.../north_star/hardware/North Star 64k HRAM.pdf`). | `reference/North Star HRAM.md`. HORIZON DRAM board (HRAM-32/48/64). Authoritative for: **port C0H**, where the byte is **not** a bank number — **bit 0 = on(0)/off(1) command, bits 1–7 = one-hot** address of which bank the command toggles (per-board JP1), so software must switch the old bank off before the new on; **≤6 banks** usable (one of bits 5/6/7 spent on parity). Two independently-switchable 32K sections (JP2); JP1 also sets the reset state (default: bank always on). Verified against the scan (`MVI A,08H`/`09H` = bank 3 on/off). Implemented as `bankmem card=northstar` (`docs/boards/bankmem.md`, `docs/devguide/banked-ram.md`). |
| `Cromemco 64KZ RAM Manual.pdf` | Cromemco *64KZ RAM* instruction manual, P/N 023-0008, © 1979, 1981 (this printing May 1981), 36 pp. **No text layer — read as page images.** Fetched 2026-08-17 from s100computers.com (`Hardware Manuals/Cromemco/Cromemco 64KZ RAM Manual.pdf`). | `reference/Cromemco 64KZ RAM.md` (grouped with the 64KZ-II). Authoritative for: **port 40H**, and the BANK SELECT byte is an **8-bit mask of active banks** (bit N ⇒ bank N on/off, several at once — verbatim p. 17; `OUT 40H,28H` = banks 3 **and** 5, p. 19), 8 banks / ≤512K. Two independent 32K blocks A/B with per-block bank-membership switches (SW2/SW3) and a RESET-enable switch (SW1); DMA/OVERRIDE switches + no-refresh-during-DMA; MEMDSBL phantom on pin 67; PROM-relocatable port (40H–4FH, C0H–CFH). Implemented as `bankmem card=cromemco64kz` (`docs/boards/bankmem.md`, `docs/devguide/banked-ram.md`). |
| `Cromemco 64KZ-II RAM Manual.pdf` | Cromemco *64KZ-II RAM* instruction manual, P/N 023-2020, © 1982 (November 1982), 21 pp. **No text layer — read as page images.** Fetched 2026-08-17 from s100computers.com (`Hardware Manuals/Cromemco/Cromemco 64KZ-II RAM Manual.pdf`). | `reference/Cromemco 64KZ RAM.md` (grouped with the 64KZ). The successor board: same **port 40H** 8-bit bank-mask model and two 32K blocks A/B, but adds independent **Lower-16K/Upper-16K** enables per block (SW3), ≤448K for Z-80 Cromix multiuser, PROM-relocatable port **41H–4FH** only (no C0H–CFH). ⚠ The -II manual documents **no DMA/OVERRIDE** and gives no explicit post-POC "no banks active" prose — flagged as unconfirmed vs the 64KZ. |
| `Double Density Controller.pdf` | North Star *Micro-Disk System MDS-A-D Double Density*, © 1978, **Revision 1**, 40 pp incl. the January 16 1979 errata sheet, ~230 KB, **real text layer** (Acrobat PDFWriter). Fetched 2026-08-08 from deramp.com (`.../north_star/hardware/Double Density Controller/Double Density Controller.pdf`). | `reference/North Star MDS Floppy Controllers.md` (grouped with the MDS-A). The double-density successor — a **superset** of the MDS-A that keeps single density and adds **double density (MFM)**, 512-B sectors, **179.2 KB/diskette** (4 drives → 716.8 KB) and a `SS` double-sided select. Same memory-mapped 1 K window / `E800H` / `PRDY`-wait personality, but the board **reorganized the interface**: the address-**case** map is now **0 = PROM, 1 = write-data, 2 = "Controller Orders" (`DD SS DP ST | DS×4`), 3 = command**, with drive-select as a **one-hot `DS`** field (1/2/4/8 = drive 1–4), the Case-3 byte reshaped to `DM(3) CC(3)` (`DM` picks A/B/C-status or read-data on the DI bus) with its own 8 command codes (motor-on and interrupt arm/disarm are now *codes*, not bits), and **three** status bytes (A: `SF IX DD MO WI RE SP BD`; B: `SF IX DD MO WR SP WP T0`; C: `SF IX DD MO | SC×4`). DD sector = 547 B (32 zeros + `FB FB` + 512 data + rotate-left-XOR check); SD sector unchanged. Auto-motor-off **9.6 s** default (jumper 3.2–38.4 s). Std PROMs `DWE-1`/`DSEL-E8-1`/`DPGM-E8-1`. Same interrupt-corrupts-transfer erratum as the MDS-A. |
| `680-KCACR Manual.pdf` | MITS *Altair 680b KCACR Documentation*, © MITS Inc. 1977 (reprinted June 1977), 136 pp (kit-assembly padded — the load-bearing content is Operator's Guide §2 and Theory §3), ~2 MB. **No text layer — read as page images** (`ilovepdf`-wrapped scan; `pdftotext` returns nothing). Fetched 2026-08-08 from deramp.com (`.../altair/hardware/altair_680/680-KCACR Manual.pdf`). | `reference/Altair 680b KCACR.md`. The Kansas City Standard audio-cassette interface for the **Altair 680b** (Motorola **6800**, so **memory-mapped** — no `IN`/`OUT`). Authoritative for the two-register map (status/control **`F010`** = 61456 / 360,020q; read/write data **`F011`**; read ≠ write of each), the **⚠ active-low "True = Logic 0"** status/control convention (status D0 Read-Data-Available / D7 Transmit-Buffer-Empty; control D0/D1 read/write interrupt enable, D6 motor-off / D7 motor-on), **software motor control** (store `7F` on / `BF` off; `POKE 61456,127`/`191`; via SPST-NO relay on connector pin 9 `MRD`), the interrupt model (enable writes `FE`/`FD`, motor-off+reset `BF`; 6800 `IRQ` vector **`FFF8/FFF9` → `0100`** from the 680b monitor PROM; reading either register or Motor-Off clears the enable latch), the UART = **SMC COM2017/H** (the COM2502/1602 family in `com2502.pdf`; 8N2, 16× clock = 4800 Hz), the **Kansas City Standard** modulation reprinted from BYTE Feb 1976 (300 baud; mark = 8 cycles 2400 Hz, space = 4 cycles 1200 Hz; character = space start + 8 data LSB-first + 2+ mark stops; tape 1.875 in/s), and the loader/punch PROM (socket V, begins **`FD00`**; punch `.J FD74` then `?`start `?`end → Motorola S-records + `S9` + 50 nulls; load `.J FD00`; errors `C` checksum/non-hex, `M` memory; **never punch/load `0000`–`00FF`**, the monitor/PROM workspace). The same KCS the 88-UIO selects at SW-1 ON, and the same UART family as the 88-SIO. |
| `680-UIO Manual.pdf` | MITS *Altair 680b Universal I/O Board Documentation*, © MITS Inc. 1976 (second printing March 1977), 65 pp, ~2.9 MB, **real text layer** (Nitro PDF Professional) — but a **garbled OCR-quality layer** (word-salad glyph runs); usable for locating sections and confirming addresses, still cross-check page images for tables. Fetched 2026-08-08 from deramp.com (`.../altair/hardware/altair_680/680-UIO Manual.pdf`). | `reference/Altair 680b Universal IO Board.md`. The general-purpose parallel+serial expansion board for the **Altair 680b** (Motorola **6800**, so **memory-mapped** — no `IN`/`OUT`; **active-HIGH**, raw Motorola bits — *not* the KCACR's inverted-true rule). Built from a **6820 PIA** (parallel, one/two 8-bit ports, 16/32 data lines) and a **6850 ACIA** (serial). Authoritative for the `F0XX` address model (256 I/O locations `F000`–`F0FF`; A15–A8 hard-wired `F0`; A7–A4 + complements select the 16-position board window via switch **S9**; A3 = parallel/serial; A2 = which parallel port; A1/A0 = register within a port) and the **default map** at S9-lowest (PIA-C parallel port 1 `F008`–`F00B`; PIA-B parallel port 2 `F00C`–`F00F`; ACIA-D serial `F006` Control/Status + `F007` Data); the **fixed, non-S9** decodes — switch inputs ("sense" bits) at **`F003`** (read-only tri-state) and the 8-bit **non-latched output** at **`F010`/`F011`** (Drive 1) + `F012`/`F013` (Drive 2), which **⚠ collide with the KCACR's `F010`/`F011`** (period fix: remove UI/O IC A1); the standard 6820 PIA model (sections A/B; each with Control/Status, Data, DDR; **Control bit 2** = DDR(0)/Data(1) select; DDR bit 1 = output/0 = input; CA1/CB1 handshake-in, CA2/CB2 strobe-out tied to the 6800 **E**/Φ2 pulse; power-on reset → all lines + C2 as inputs; status bits 7/6 unaffected by writes); the standard 6850 ACIA (Register Select on A0; **Control** bits 1,0 divide `01`=÷16/`10`=÷64/**`11`=Master Reset**, bits 4–2 word/parity/stop, bits 6,5 Tx control+RTS+break, bit 7 Rx-int-enable — reset then config, e.g. 8N2 TTY = `0x56`; **Status** RDRF/TDRE/DCD/CTS/FE/OVRN/PE/IRQ, CTS-high inhibits TDRE); baud via switch **S10** + clock divide (÷16 → 75–9600 standard; ÷64 → 5 extra rates at 4× selected); RS-232/TTL/20 mA-TTY serial levels; and both chips' **`IRQ`** (active-low → 6800 vector `FFF8/FFF9` → `0100`). Same 6820/6850 chips as the 88-4PIO / 2SIO, memory-mapped host wrapper. |
| `680 Operators Manual.pdf` | MITS *Operator's Manual — altair 680b*, © MITS Inc. 1976, 17 pp, ~0.8 MB. **No text layer — read as page images** (`Acrobat PDFWriter 3.02` scan; `pdftotext` returns nothing). Fetched 2026-08-08 from deramp.com (`.../altair/hardware/altair_680/680 Operators Manual.pdf`). | `reference/Altair 680b Operators Manual.md`. The operator-facing guide to the **Altair 680b machine itself** (Motorola **6800**), not a plug-in board — the three user-wired areas of the **main PC board** plus front-panel operation. Authoritative for the **baud-rate generator** jumper truth table (4 holes 0–3 → rate at IC Z pin 10: 50/75/134.5/200/600/2400/9600/4800/1800/1200/2400/300/150/110 — ⚠ **2400 is listed twice**, `HHL0` and `LLHH`; reproduce verbatim); the **1 K on-board RAM** starting-address scheme (jumpers 1–6 / pads 10–15 = NAND of A10–A15 or their complements, so the block never aliases above itself; worked bases `0000`, `2000`/8 K, `4000`/16 K, `5000`/20 K, `6000`/24 K, `8000`/32 K); the three **hardware-programmable bits** (hole 1 Baudot HIGH=yes/LOW=no; hole 2 stop bits HIGH=1/LOW=2; hole 5 HIGH=**no** terminal→front-panel programming / LOW=terminal; holes 3,4 reserved, **must tie LOW**); the three serial **interface builds** in the on-board I/O box (ASR33/KSR33 **20 mA current loop**; **RS-232**; **Baudot** driver, which alone is insufficient — needs an external reed-relay/opto isolation circuit); the optional **PTRC** paper-tape-reader-control block (only for a Teletype with a Call/Control Unit); the **front panel** (address = 4 hex digits on LEDs A0–A15, data = 2 hex digits on D0–D7, switch down=0/up=1, HALT mode continuously addresses the ADDRESS switches showing contents on the DATA LEDs, DEPOSIT in the upper-left, RESET before RUN, "AC" power-on LED); and the **power-up/deposit sequence** (HALT → rear POWER → RESET → set ADDRESS → set DATA → DEPOSIT, repeat; with the **No-Terminal bit** set the MPU PC starts at `0000` on RUN; with a terminal, defer to the *System Monitor* manual). |
| `680 Programming Manual.pdf` | MITS *Programming Manual — altair 680b*, © MITS Inc. 1976 ("Major portions … reprinted by permission of Motorola Semiconductor Products, Inc., copyright 1975"), 177 pp, ~5.9 MB. **No text layer — read as page images** (`Acrobat PDFWriter 3.02` scan of a Microsoft Word doc; `pdftotext` returns nothing). Fetched 2026-08-08 from deramp.com (`.../altair/hardware/altair_680/680 Programming Manual.pdf`). | `reference/Altair 680b Programming Manual.md`. The programmer's guide to the **Altair 680b** (Motorola **M6800**). **Most of the volume is the standard Motorola M6800 reprint** — CPU model, seven addressing modes, the 72-mnemonic/197-opcode instruction set with per-opcode cycle counts (§IV, Appendix A), arithmetic tutorials (§V), and sample programs (§VI) — **all deliberately out of scope** in the distilled reference (consult a Motorola databook / this project's CPU work for opcode detail). Distilled to what is **680b-specific or emulation-load-bearing**: the **programming model** (ACCA, ACCB, IX, PC, SP; 6-bit CCR `H I N Z V C`; memory-mapped I/O, no `IN`/`OUT`); the **addressing-mode summary** (Inherent/Accumulator 1 B, Immediate 2 B **but 3 B for CPX/LDS/LDX**, Direct/Indexed/Relative 2 B, Extended 3 B; **direct-vs-extended chosen by operand value** — ≤255 direct, >255 extended; relative branch range `(PC+2)−128 … (PC+2)+127` as an 8-bit two's-complement offset); the **interrupt latency** (12 machine cycles from the end of the instruction in progress, **4** immediately after `WAI`); the **680b assembler source language** (four fields; labels 1–6 alnum, never bare A/B/X; number forms — default decimal, `$`/`H` hex, `@`/`O`/`Q` octal, `%`/`B` binary, `#'C` 7-bit ASCII; ⚠ `$…B` reads as hex; one level of forward reference; strict left-to-right expressions, no precedence) and the assembler **directives** `ORG EQU FCB FCC FDB RMB END NAM OPT PAGE SPC` plus the 680b-specific **`MON`** ("Return to Monitor" — ends a source program by handing control back to the 680b PROM Monitor); and — the emulation payload, from **Appendix C, Input/Output Information** — the full **6850 ACIA** register model for the machine's serial port (same chip family as the 88-2SIO / 88-UIO / 680b UI/O): **TDR** write (clears TDRE), **RDR** read (non-destructive, clears RDRF), write-only **Control Register** (CR1,0 counter divide ÷1/÷16/÷64/**`11`=Master Reset**; CR4–2 word-select = 7/8 data + parity + 1/2 stop, changes unbuffered/immediate; CR6,5 transmitter control = RTS + Tx-interrupt-enable + Break; CR7 receive-interrupt-enable) and read-only **Status Register** (bit 0 RDRF, 1 TDRE, 2 DCD, 3 CTS, 4 FE, 5 OVRN, 6 PE, 7 IRQ) — with the load-bearing interlocks **⚠ CTS-high inhibits TDRE** and **⚠ DCD-high forces RDRF to read empty**, and **⚠ the ACIA is not auto-reset on power-on** (guest software must write Master-Reset then select a divide ratio); the **paper-tape-reader control** (ACIA **RTS high → reader ON**, which is the `CR6=1, CR5=0` combination; reader off for the other three); and the **interrupt vectors** at `FFF8`–`FFFF` inside the 680b PROM Monitor (IRQ `FFF8/9` → `0100` in the shipped monitor, matching the KCACR/UIO references). **⚠ The ACIA's memory address is *not* given in this manual** — it is a machine-wiring fact (KCACR cassette ACIA `F010`/`F011`; UI/O serial ACIA in its S9 window); do not invent one. |
| `680 Theory of Operation.pdf` | MITS *Theory of Operation — altair 680b*, © MITS Inc. 1976, 37 pp (mostly PC-board schematics, scope photos and a troubleshooting guide), ~1.6 MB. **No text layer — read as page images** (`Acrobat PDFWriter 3.02` scan of a Microsoft Word doc; `pdftotext` returns nothing). Fetched 2026-08-08 from deramp.com (`.../altair/hardware/altair_680/680 Theory of Operation.pdf`). | `reference/Altair 680b Theory of Operation.md`. The circuit-level companion to the 680b Operator's and Programming manuals — and the one manual that **names the on-board addresses the Programming Manual deliberately withheld**. Authoritative for the **680b clock** (500 KHz two-phase non-overlapping; 2 MHz crystal ÷4; Φ1 hi 600 ns/lo 1.4 µs, Φ2 hi 1 µs, period 2 µs; ⚠ **all data transfers on Φ2**, which also enables memory + the ACIA; the 6800 thus runs at **½ its usual 1 MHz**); the full **6800 bus-control signal set** (`RESET` positive-edge → PC from reset vector `FFFE/FFFF` = Monitor start; `HALT` hi=RUN / lo=halted → `BA` hi → address+data+`R/W` tri-stated → front-panel deposit enabled; `R/W` hi=READ/lo=WRITE, hi-Z when halted; `VMA` valid-address strobe; `DBE`=Φ2; `R/W-P`=`R/W`∧Φ2 = the valid-data-window R/W; `BA` lo=running/hi=halted; `TSC` = DMA tri-state, forces `BA`+`VMA` low ~500 ns after 2.4 V); the memory/PROM map (**1 K** 2102A RAM jumper-placed at any 1 K boundary; **four 256×8 PROMs**, **PROM 1 at `FF00`** holding the System Monitor + reset/interrupt vectors → the PROM window is `FC00`–`FFFF`); and — the emulation payload — the **on-board I/O address decode**: the **serial 6850 ACIA at `F000`/`F001`** (decode gated by VMA asserts both `CS1`+`CS2`; **A0 → Register Select**, so `F000` = Control(write)/Status(read) and `F001` = Tx Data(write)/Rx Data(read); MPU `R/W` → ACIA `R/W`; **ACIA Enable tied to Φ2**; ACIA `IRQ` → MPU `IRQ`; **`RTS` low = paper-tape reader ON** via the 20 mA loop; Tx/Rx = 20 mA TTY loop *or* RS-232, one at a time), the **34702 Programmable Bit Rate Generator** (IC Z, **2.4576 MHz** crystal, 4 jumper holes 0–3 select rate = same table as the Operator's Manual; ⚠ output is **16×** the indicated baud, feeding the ACIA Rx+Tx clocks), and the **Baudot interface at `F002`** (when built, **the ACIA is bypassed** — a 5-level code can't use the 7/8-bit Motorola ACIA — and the MPU bit-bangs serial↔parallel through D flip-flops on the **Buffered-Data-Zero `BD0`** line; `F002` on a Read also gates the 74367 tri-state buffer that returns the Operator's-Manual hardware-programmable config bits, read by the monitor to detect a non-front-panel terminal, the stop-bit count, and a Baudot terminal); the interrupt/reset vectors (`FFFE/FFFF` reset → Monitor; `FFF8/FFF9` IRQ → `0100`, matching KCACR/UIO); and the standard vendor **pinouts** (6800 MPU, 6850 ACIA, 2102A RAM, 1702A PROM, 34702). **⚠ This resolves the Programming Manual's "ACIA address not given" caveat** — the base 680b's *own* serial port is `F000`/`F001`; the KCACR's `F010`/`F011` and the UI/O's S9-selected window are the separate **plug-in** boards, not a contradiction. **⚠ Records the manual's own page-15 erratum** — the "READ/WRITE During WRITE Cycle" and "READ/WRITE PRIME During WRITE Cycle" scope photos are cross-labelled; trust the text/`R/W-P` definition, not the captions. |
| `680 Turnkey Front Panel Schematic.pdf` | MITS *6800 Turnkey Schematic*, drawing **SH 1-11** (drawn "ES"), © MITS Inc., **1 page**, ~3.7 MB. **A single schematic image sheet, no text of any kind** — read as a page image (photographed drawing, Adobe Photoshop origin). Fetched 2026-08-08 from deramp.com (`.../altair/hardware/altair_680/680 Turnkey Front Panel Schematic.pdf`). | `reference/Altair 680b Turnkey Front Panel Schematic.md`. The **turnkey** front panel of the Altair 680b — the *reduced* panel a turnkey machine ships with **instead of** the Operator's-Manual full hex panel (`680 Operators Manual.pdf`). Carries **only RESET (SW1, MOM SPDT) and RUN/HALT (SW2, MOM) switches plus three LEDs (PWR ON / RUN / HALT)** — **no address/data toggles, no DEPOSIT/EXAMINE, no A0–A15 / D0–D7 display**. Authoritative for the two bus-visible signals, both **debounced and Φ2-synchronized** (a 7474 D-flip-flop chain clocked by Φ2 on connector pin **57**): **`RES̄`** (connector pin **54**, **active-low** → 6800 RESET → PC from `FFFE/FFFF`, the main-board Monitor start) and **RUN/HALT** (pin **63** → 6800 HALT: hi = RUN, lo = halted → `BA` hi → address/data/`R/W` tri-stated, per `680 Theory of Operation.pdf`); the **reset-scope jumper** (**JA–JC** = reset honored in Run *or* Halt; **JB–JC** = reset in Halt *only*) as a power-on build constant; the switch/LED complement; and the build detail (two 7474 dual D flip-flops + one 7400 quad NAND; RESET debounce R5 20 K / R6 10 Ω / C1 0.1 µF; a cross-coupled-NAND RUN/HALT set-reset latch with R2/R3 4.7 K; LED droppers R1/R7/R9 560 Ω; VCC pins 1/51, GRD pins 50/100; test points TP1–TP4). **⚠ No boot PROM on this board** — unlike the 8800b Turnkey Module (`MITS Turn Key Board.md`), the 680b reset vector points into the **main-board** monitor PROM at `FF00`; and **⚠ no deposit path** — HALT here only stops the CPU (the full hex panel's HALT-enables-deposit behavior does not apply to the turnkey panel). |
| `3P+S Manual.pdf` | Processor Technology *3P+S Input/Output Module — Assembly and Operating Instructions*, © 1976, 75 pp. **No text layer — read as page images.** Fetched 2026-07-29 from s100computers.com (`.../Hardware Manuals/Processor Technology/3P+S Manual.pdf`). **⚠ Fetch the whole file — see the linearized-PDF trap below.** | `reference/Processor Technology 3P+S.md`. One **AMI S1883 / TMS6011** UART serial channel + two fixed-direction 8-bit parallel ports in a 4-address block (channels A/B parallel, C status/control, D UART data), placed anywhere by jumper. Its defining quirk: the **status/control byte has no fixed bit layout** — the builder wire-jumpers UART flags (PE/FE/OE/RDA/TBE), parallel flags (FA/FB/XA/XB) and EIA lines onto whichever bits software expects. TBE=D7 / RDA=D6 **active-high** confirmed from Appendix V's test-program code (`ANI 80H`/`ANI 40H`, loop-while-0). A 12-bit preset baud divider (35–9600), the CRL-strobe framing trap, and **no onboard interrupt logic** (the schematic shows only passive 7406 receivers on the VI bus; 88-VI is an off-board option). **SBS (stop-bit select) is genuinely undocumented** — no Channel-C bit is tied to it anywhere in the complete manual. |
| `SDS_VDB8024.pdf` | SD Systems *VDB-8024 Video Display Board* manual. **Scanned image, no text layer** — read as page images (rendered with `pdftoppm`). Fetched 2026-07-25 from deramp.com (`.../sd_systems/Manuals/SDS_VDB8024.pdf`); **no local copy** in the archive (only the onboard-firmware sources `VDB16.Z80` and `VDB.ASM`). | `reference/SD Systems VDB-8024.md`, and (planned) a `vdb8024` board. An **intelligent terminal on a board** (onboard Z80 + SMC CRT 5027 + PROM), **not memory-mapped** — the S-100 host sees only **two I/O ports**: status `IN 00H` (**D1** keyboard-ready, **D2** display-ready, active-high) and `IN`/`OUT 01H` (keyboard/display data). Authoritative for the host-visible terminal: 80×24 format, the `OUT 01H` control-word/enhancement set, cursor `ESC = Y X`, and the CGEN/cursor/interrupt jumpers. **⚠ Control-code semantics are firmware-dependent** — the shipped Section III set differs from the archived `VDB16.Z80`/`VDB.ASM` firmwares (which add CR/clear/home/insert-delete), so an emulation must pin the firmware it reproduces. The port map + D1 polarity are corroborated by the **SDMONV21** monitor's own `IN 00H` / `AND 02H` / `IN 01H` console routine — i.e. ports 00/01 are this board, the "VDM Console" that build targets. |
| `altair_680/` **software** directory (deramp.com) — *not a scan; the code, not a manual*. Fetched 2026-08-08 from `https://deramp.com/downloads/altair/software/altair_680/`. | `reference/Altair 680b Software.md`. The provenance anchor for the **680b software that ships as built-in ROMs** — Motorola **S-record (`.S19`)** images, distinct from the hardware manuals above. Names the four `altair_680/` sub-directories: `PROM Monitor/` (**MON680**, **SWIMON** → `builtin:mon680`/`swimon`, `FF00`–`FFFF`), `PROM for KCACR/` (**KCACR** loader/punch → `builtin:kcacr`, `FD00`–`FDFF`), and the runtime-only `Cassette Binary Loader/` and `BASIC/` (real 680b BASIC, loaded via `.J FD00`, not embedded). The three PROM images are tracked verbatim under `roms/<NAME>/` (with `.ASM`/`.LST`) and CRC-checked at build time — per-image size/range/CRC in `docs/roms.md`. Runs on the boards documented by the `680-*` / `680 *` rows above (`machines/altair680.toml`, `examples/altair680/`). |
| `EDOS-I Manual.pdf` | Motorola *M68FD3601–M68FD3604 EXORdisk Floppy Disk System User's Guide* (resident-driver listing stamped **VERSION 2.0, 4/9/75**), ~50 pp. **Scanned image, no text layer** — read as page images. Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/EDOS-I Manual.pdf`). | `reference/EDOS-I Manual.md`. **⚠ Not iCOM's 8080 "EDOS-I" the filename implies** — it is **Motorola's EDOS** (internally "FDOS" v2.0), the EXORciser Floppy Disk Operating System for the **M6800** under EXbug, driving the EXORdisk. The iCOM tie is the drive hardware: the bundled diagnostic is headed *ICOM, INC. FD360-X-68*, an iCOM FD360 rebadged with a Motorola 6800 (`-68`) interface. For an 8080 emulator the value is the shared iCOM **FD360 format** (77 tracks / 26 sectors / 128 bytes) and the logical→physical sector skew; the CPU-side software (EXbug boot `E800;G`, directives, zero-page driver, `E800`–`E824` entry vectors) is 6800, not 8080. |
| `FDOS-II Manual.pdf` | iCOM Microperipherals *FDOS-II Operator's Manual* (for SBC/8800/ALTAIR/IMSAI/POLY88), Rev. **Oct 15 1976**, © Sept 1976, 77 pp. **Scanned image, no text layer** — read as page images. Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/FDOS-II Manual.pdf`). | `reference/FDOS-II Manual.md`. iCOM's second-generation **8080 floppy disk operating system** for the FD3712/FD3812 (FD360) hardware. Authoritative for the module/memory map (Altair `C000H` and MDS `E800H` builds), the resident entry points and user-supplied device-vector `JMP` table, the mini-monitor commands, the disk/file format (77 × 26 × 128, track-0 directory, 11-byte FCB, ≤253 files), the `RI`/`WRT` byte-I/O syscalls with the 3:1 logical→physical sector interleave, the 22 directives (incl. `RUNGO`/`RUN`), and the error codes. Sits between EDOS and FDOS-III. |
| `FDOS-III Manual.pdf` | iCOM Microperipherals / Pertec (Microsystems Div.) *FDOS-III Operator's Manual*, **September 1977** (drawing 505-345-9084), 91 pp. **Scanned image, no text layer** — read as page images. Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/FDOS-III Manual.pdf`). | `reference/FDOS-III Manual.md`. The most advanced iCOM **8080 disk OS**, succeeding EDOS and FDOS-II. Authoritative for the per-host boot/load table, the `-58` I/O-vector init, the RAM map and entry vectors, the disk/file format (26 × 128 sectors, track-0 directory, 11-byte FCB, 253-file limit), the full command set (24 commands + `RUNGO`), the SYSGN field table, the `RI`/`WRT` byte-I/O plus RAM-resident **Executive**/`DKHN` syscall interface, the logical→physical sector-skew table, and all error codes (Executive, resident, DKHN, diagnostic). Appendices C/D (source/object listings) noted but not transcribed. |
| `iCOM Macro Assembler.pdf` | iCOM *Macro Assembler Operator's Manual*, © **August 1976**, ~81 pp, plus a one-page cover errata sheet. **Has an OCR text layer.** Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/iCOM Macro Assembler.pdf`). | `reference/iCOM Macro Assembler.md`. iCOM's two-pass **8080 Macro Assembler** under FDOS-II. Authoritative for the `ASMB,source,object,PASS` invocation (with the PASS-value table), statement/field/label rules, the nine operand data types and built-in register names, the expression operator set with precedence, the `DB`/`DW`/`DS` + `ORG`/`EQU`/`SET`/`END`/`IF`/`MACRO` directives, the macro facility (local/global labels, parameter substitution, delayed evaluation), the listing/object output format, and the single-letter error-code table. Cover-sheet errata folded in. |
| `FDOS-III Relocating Assembler.pdf` | iCOM (Pertec Computer Corp.) *FDOS-III Relocating Assembler* manual, **September 1977**, ~158 pp. **Has an OCR text layer.** Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/FDOS-III Relocating Assembler.pdf`). | `reference/FDOS-III Relocating Assembler.md`. iCOM's **relocatable** 8080/Z80 assembly system under FDOS-III — `ASMB`/`LINK`/`LIB`/`RDBFL`. Authoritative for the option switches, the relocation/structuring/data/conditional/macro/listing directives, the external/global symbol model, the binary-relocatable + library + Intel-Hex-ASCII object formats (record types, relocation-mode bits, checksums), the linker/library directive set, and the 0–43 error-code table. The Section V opcode reference and Appendices C/D noted but not transcribed. |
| `iCOM Text Editor.pdf` | iCOM *Text Editor* manual, **Revision A, January 1977**, ~29 pp. **Has an OCR text layer.** Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/iCOM Text Editor.pdf`). | `reference/iCOM Text Editor.md`. The iCOM 8080 **Text Editor**, Rev A — a TECO-family character/line editor run under FDOS-II as **`T,INPUT,OUTPUT`**. Authoritative for the in-memory workspace and buffer-pointer model, the single-letter command set (`A`/`I`/`B`/`L`/`M`/`Z`/`S`/`C`/`D`/`K`/`P`/`T`/`E`) with the `$$` ESCAPE terminator and `n`-argument rules, command strings/iteration/tabs, and the three error signals (`CANNOT FIND`, BELL, `ITERATION STACK FAULT`). Superseded by Rev B (next row). |
| `iCOM Text Editor Rev.B.pdf` | iCOM *8080 Text Editor* manual, **Revision B, June 1977**, ~20 pp of content. **Scanned image, no text layer** — read as page images. Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/iCOM Text Editor Rev.B.pdf`). | `reference/iCOM Text Editor Rev.B.md`. The later revision of the same editor — now invoked as **`EDIT,INPUT,OUTPUT`** (vs Rev A's `T,…`). Same `@`-prompt buffer model and 13 single-letter commands, command chaining/iteration/tabs, and the three status messages. **⚠ The manual carries no changelog**, so the A→B delta beyond the invocation keyword is not stated in the source and is not inferred here. |
| `iCOM BASIC-M (DEBBI).pdf` | iCOM *BASIC-M Operator's Manual*, © **December 1976**, 19 numbered pp + appendices. **Scanned image, no text layer** — read as page images. Fetched 2026-08-11 from deramp.com (`.../altair/software/icom_floppy/FDOS/iCOM BASIC-M (DEBBI).pdf`; the sibling `DEBBI.txt` note fetched alongside). | `reference/iCOM BASIC-M (DEBBI).md`. iCOM's disk-resident **BASIC-M** interpreter (informally "DEBBI", Disk Extended BASIC by iCOM) for FDOS-M/FDOS-II. Authoritative for the `RUNGO,BASIC[,"in"][,"out"]` invocation (and the `DEBBI,PROGA,PROGB` shorthand), the `DLOAD`/`DSAVE` disk-file mechanism, the BCD 6-digit numeric model, and the command/statement/function/operator/error-code sets. **⚠ Two internal discrepancies flagged in the distillation** — GOSUB nest limit reads 6 (§IV) vs 5 (the `NE` error text); `TAN` appears only in the Appendix A index, not the p.17 function table. |

## `disks/` — the CP/M images, and the listings that came with them

`disks/` follows the same rule as `reference/`, and `.gitignore` enforces it as an **allowlist**:
the disk images and the vendor ReadMes are **not in git** (20 MB), while the `.ASM` listings the
boards were *built from* **are** — they are first-hand period artifacts, they are cited by path in
`docs/boards/*.md`, and a citation to a file that is not in the tree is worthless.

Every directory carries a `README.md` with the exact download URL and how to run it, so a fresh
clone knows what it is missing and why. All of it is Mike Douglas's work, from **deramp.com**
(verified 2026-07-13):

| Directory | Source | Runs as |
|---|---|---|
| `mits-88dcdd/cpm22/buffered/` | `…/8_inch_floppy/CPM/CPM 2.2/CPM 2.2B/` | `examples/cpm/cpm22-buffered.toml` → `56K CP/M 2.2b v2.3`. **The machine file and the 56K image moved to `examples/cpm/` on 2026-07-19** — they ship; what stays here is source. **`BOOT.ASM` + `BIOS.ASM` here are the 88-DCDD's authoritative source.** |
| `mits-88dcdd/cpm22/8mb/` | `…/8_inch_floppy/CPM/CPM 2.2/FDC+ 8Mb CPM 2.2/` | `cpm22-8mb.toml` → `A0>`. Authoritative for the **8 MB medium** — which is the *same* hard-sector format, just 2048 tracks (see `docs/boards/mits-dcdd.md`). |
| `mits-88dcdd/cpm22/burcon/` | `…/8_inch_floppy/CPM/CPM 2.2/Burcon CPM/` | `cpm22-burcon.toml` → `56K CP/M Version 2.2mits`, © 1980 Burcon. |
| `mits-88dcdd/cpm22/lifeboat/` | `…/8_inch_floppy/CPM/CPM 2.2/Lifeboat CPM/` | `cpm22-lifeboat.toml` → Lifeboat `CONFIG` v4.8. A **48K** build, so it re-fits the memory card. |
| `mits-88dcdd/cpm22/pcgetput/` | `…/8_inch_floppy/CPM/CPM 2.2/PCGET and PCPUT/` | No machine file — CP/M-side XMODEM tools. Already on both `.dsk` images above. |
| `mits-88mds/cpm22/` | `…/minidisk/CPM 2.2/` | `cpm22-mini.toml` → `56K CP/M 2.2b v2.3 / For Altair Mini Disk / A>`. **A DIFFERENT CONTROLLER** — the 88-MDS, not the 88-DCDD. **`BOOT.ASM` + `BIOS.ASM` here are authoritative** for the minidisk's geometry (`MINIDSK`: 35 tracks, 16 sectors, `DATATRK` 4) and for the card's own account of itself. Ships as **two disks** because a minidisk holds a fourteenth of an 8″ floppy. |
| `tarbell-sd/cpm22/buffered/` | `…/tarbell_floppy_controllers/single_density_controller/CPM 2.2B (track buffered)/` | **Nothing.** The Tarbell is not built. `BIOS (1).ASM` is kept only because `docs/boards/tarbell-sd.md` cites it for the grounded-E30 finding. |

### The four images that ARE in git

An image that has to be downloaded before a test can run is an image that makes the test **skip** —
and three acceptance tests were skipping on every fresh clone and in all of CI. 480 KB buys them
back, so `.gitignore` names these, one line each (2026-07-18):

| Tracked file | Upstream SHA-256 | Why this one |
|---|---|---|
| `examples/cpm/cpm22b23-56k.dsk` | `3147946a…57a2cc` | The bootable 8″ system disk, and the only CP/M a fresh clone gets. `acceptance-examples`, `acceptance-dcdd-readonly`, `acceptance-ddt`, `acceptance-hostbridge`. |
| `mits-88mds/cpm22/CPM56K-1.DSK` | `41c87b01…32d3d` | The minidisk system disk (A:). `acceptance-minidisk`. |
| `mits-88mds/cpm22/CPM56K-2.DSK` | `0f6480b1…586db` | Its tools disk (B:) — `cpm22-mini.toml` mounts both, so one without the other does not boot. |
| `examples/diskbasic/Disk BASIC 4.1.dsk` | `e1fb7255…36b69` | Altair BASIC Rev 4.1 [Disk Extended Version], MITS 1977. `acceptance-diskbasic`. Added 2026-07-19. |

**The Disk BASIC image came from deramp.com** (Patrick, 2026-07-19), like everything else above,
but it arrived by hand rather than through `fetch-disk-images.sh` — so the SHA recorded for it is
of the file as it sits in the tree, not of a download that has been re-fetched and compared. It is
the one tracked image without that round trip. `disks/mits-88dcdd/diskbasic/Basic Versions.pdf` —
MITS's own table of what each BASIC release does — is kept beside the sources as the documentation
for it, and is where the C/O/Q answer set comes from.

**`cpm22b23-56k.dsk` is not byte-identical to the download**, and that is deliberate: it ships with
our `R.COM`, `W.COM` and `HDIR.COM` installed (26K free → 18K), so the host-bridge utilities exist
somewhere other than as `.HEX` in `cpm/hostbridge/`. The SHA above is the **pristine upstream** file;
the recipe that turns it into the tracked one is `tools/install-hostbridge-utils.sh`, which is what
makes the blob auditable rather than mysterious. Everything else on the disk is untouched.

Everything not tracked is one command away — `tools/fetch-disk-images.sh` downloads the 8.6 MB FDC+
image and the 24K floppy and checks both against pinned SHA-256s. It **never writes to what it
fetched**, so those hashes keep matching; anything needing a prepared disk copies to scratch first.
Since 2026-07-19 **no test needs either**: the 8 MB image is wanted only by `hostbridge.cmake`'s
`MODE=build`, which is run by hand when a `.ASM` changes.

CP/M 2.2 itself is redistributable: Bryan Sparks, president of DRDOS, Inc. and successor in interest
to Digital Research, granted "a right to use, distribute, modify, enhance, and otherwise make
available in a nonexclusive manner CP/M and its derivatives" (July 2022, superseding the 2001 Lineo
grant that had been tied to one website) — <http://www.cpm.z80.de/license.html>.

`tapes/` is **fully tracked** — a `.tap` is small, and the BASIC and `PS2-MON.TAP` images are what
the acceptance tests boot. The rule was never "media is untracked"; it is "*huge* media that is not
ours is untracked."

## `examples/` — Sol-20 games, off real cassettes

`examples/sol/` ships a Processor Technology **Sol-20** and four 1977 cassette games: **TREK80**
(© 1977 Processor Technology Corp.), **ATC** (Air Traffic Controller, © 1978 Creative Computing
Software / David Mannering), **PAC-MAN**, and **RAIDERS**. The four tapes are Sol-20 cassette
digitizations that deramp.com's `tape_wav_files` directory credits to **Philip Lord** ("P. Lord")
— deramp hosts them, it did not make them — all fetched 2026-07-19. TREK80's `ENTER`-script image
and its manual came from the deramp.com MFE archive; the ATC manual came from sol20.org.

<https://deramp.com/downloads/processor_technology/sol-20/software/tape_wav_files/> (the tapes)
<https://deramp.com/downloads/mfe_archive/010-S100%20Computers%20and%20Boards/00-Processor%20Technology/40-PTC%20Software/63-TREK80/> (TREK80's image + manual)

| File | Provenance |
|---|---|
| `TRK80.WAV` | `…/tape_wav_files/TRK80.WAV` on deramp.com — a **Sol-20 cassette digitized by Philip Lord**, real 1200-baud CUTS audio. **The tape that ships**, and the recording `reference/Sol-20.md` measured the CUTS parameters from. |
| `ATC.WAV` · `PACMAN.WAV` · `RAIDERS.WAV` | `…/tape_wav_files/` on deramp.com — the same, Philip Lord's Sol-20 digitizations. They decode to SOLOS files `ATC` (2,623 B), `PACMA` (3,415 B) and `RAID` (4,218 B), each at 0 framing errors. |
| `TRK80.TAP` | The byte stream, `altair_tapetool decode`d from `TRK80.WAV` (`cuts1200`). 7,939 bytes. Committed so the fast-path example needs no audio decode. |
| `TREK80.ENT` | `…/63-TREK80/Hex Dump/TREK80.ENT` — a SOLOS `ENTER` script: 7,840 bytes loading at `0000`, entry `AF C3 5C 1D`. Kept as the source for the tape-writing demonstration below. |
| `Trek80 Manual.pdf` | `…/63-TREK80/` on deramp.com — Processor Technology's own manual for the game. |
| `ATC Manual.pdf` | `sol20.org/manuals/atc.pdf` — Creative Computing Software's *Air Traffic Controller* manual (David Mannering, CS-8001), 6 pp. |

**Reading Lord's real recordings was the whole point, and they now read clean** — all four decode
to valid SOLOS tapes at 0 framing errors. TREK80 is the one with an independent oracle, so it is the
one the story is told through. `TRK80.WAV`
decodes to **7,939 bytes with 0 framing errors** — a valid SOLOS tape: `01` sync, name `TRK80`,
`SIZE` field `1EA0`, header checksum `D9`, the payload matching the `.ENT` image. That was **not**
always true. Until the demodulator fix (`src/host/tapemodem.cpp`; TODO's *CUTS demod* entry), the
decoder classified each half-cycle interval and sliced it, and on a genuine 1200-baud CUTS dub —
whose 1200/600 Hz tones and half-cycle "0" put only one or two crossings in a bit — it
desynchronized: **27 framing errors, 6,778 of 7,840 payload bytes wrong**, the entry point reading
`00 00 00 00`. The tape was effectively invisible. The matched-filter decoder measures which
tone's energy fills each cell and reads it byte-for-byte. `acceptance-trek80-wav` is that
regression, run against this exact file.

(The MFE archive's separate **MP3** is a different, lossy dub of a different build — 8,285 bytes,
31 errors — and is not the tape here.)

**The tape used to be synthesized, and the machinery is kept as a demonstration.** Because that
recording could not be read, the example tape was once *written by SOLOS itself* — `TREK80.ENT`
loaded into a running Sol-20 and `SA`ved back out (`make-trek80-tape.sh`). That workaround is no
longer needed and no longer ships, but the script and the `.ENT` are kept, because they show the
one thing that has to be true for any SOLOS tape to load: a SOLOS cassette carries a header checksum
and one after every 256-byte block, and a tape whose checksums are wrong is **invisible** — `CA`
lists nothing, `GE` never finds the file, and nothing prints an error. The script now writes to a
**separate output** (`TRK80-solos.*`) so it never overwrites the shipped recording, and its own
cross-check is the proof that both are the genuine artifact: the header checksum SOLOS computes is
`D9`, the same byte Lord's recording carries in the same position, and the `SIZE` it writes
(`1EA0`) agrees with the `.ENT`. `acceptance-trek80` reads the tape back through SOLOS for both
media, which is the honest test either way.

## Traps, paid for once

**The WD177X sheet is the WRONG CHIP for the Tarbell.** It is the WD1770/1772/1773 —
28-pin, single-5V, MFM, motor control. The Tarbell is an **FD1771**. The step-rate table
differs (the BIOS's `STPRATE equ 2` is 10 ms on a 1771 *only*; the 177x reads that code as
20 ms) and the Type-II status bits differ (the 1771 carries a two-bit record type where the
177x lineage puts write-fault). Building from the sheet that was to hand would have produced
something plausible, clean, and **wrong** — a controller that seeks at the wrong speed and
mis-reports deleted records, while looking entirely finished. `tests/test_wd17xx.cpp` keeps
a tripwire on the step-rate table so nobody "fixes" it back. See `src/chips/wd17xx.h`.

**The Operator's Manual never says "sense switch".** Not once — grep it. It documents the DATA
switches (7–0) and the ADDRESS switches (15–0) and stops there, because to an *operator* there is
no such thing as a sense switch: it is the same row of toggles, and what it means depends on what
the program does with it. The fact that `IN 0FFH` returns `SA8`–`SA15` is in the **Theory of
Operation** and in **schematic 880-106**, and nowhere else. A plausible story about which eight
switches feed the port would have been very easy to write and would have had a 50% chance of being
backwards. See `docs/boards/mits-frontpanel.md`, which shows *why* it is the top eight (that buffer
bank already existed, to serve EXAMINE).

**A MANUAL CAN BE A FOSSIL OF THE MANUAL IT WAS EDITED FROM.** The 88-MDS manual was clearly written
by editing the 88-DCDD's, and three of its numbers did not get changed:

- **p45: *"a new byte of Write Data is requested every 32 microseconds."*** That is the **8″ card's**
  rate. Everywhere else — p4, p30, p31, p33, p54, p60, and Figures 4-7 and 4-10 — says **64 µs**, and
  125,000 bit/s ÷ 8 = 15,625 byte/s = 64 µs closes it arithmetically.
- **pp. 27–28: the sample driver sends `MVI A,8 ; UNLOAD HEAD / OUT 9`** — on the bit **p32 of the same
  manual calls "Not used"**, for a head **p31 of the same manual says is "always loaded when the Drive
  is enabled."** There is no head solenoid on a minidisk. (p26 does it again: *"ENABLE WRITE WITHOUT
  SPECIAL CURRENT"*, which is the 88-DCDD's D6, also "Not used" here.)
- **The motor timer is 6.4 s, 6 s, and 5 s**, depending on which page you open — p31/p32/p68 vs. p3/p73
  vs. the MITS marketing sheet. **6.4 wins because it is DERIVED**: p68 says the timer is a **4020**
  ripple counter *"clocked every 12.5ms by the START OF SECTOR CLR pulse"*, and 12.5 ms × 512 = 6.4 s
  exactly. A number you can compute beats two round numbers you cannot.

**Prefer the number you can derive, then the schematic, then the tables, then the prose — and the
sample code last of all.** The sample code is the *only* source here that is flatly, checkably wrong,
and it is wrong in the most seductive way: it is real software that really ran.

**AND THIS SIMULATOR MADE THE SAME MISTAKE, FOR THE SAME REASON.** The minidisk lived inside the
88-DCDD as one format row plus `if (d->fmt.sectors != 16)`, and nothing failed — because the two
controllers are register-compatible *by design*, so wrong-card code runs. That compatibility is real
(DBL, the 8″ boot PROM, boots a minidisk perfectly well — `docs/roms.md`), which is exactly what makes
it dangerous: **the thing that works is not evidence that the model is right.**

**A MANUAL CAN STATE ITS OWN NOTATION BACKWARDS.** The PMMI MM-103 manual marks its active-low
bits with overbars in Figure 7.0 — and then explains the notation inside out: *"A bar over the
bit description indicates that the bit is to be set to a 1 to activate the function."* Its own
worked example gives it away, because the bit it picks (DTR, OUT 3 bit 6) carries **no** bar and
does activate at 1. Every barred bit's own subsection says it activates at **0**. Believing the
sentence instead of the bars produces a card whose Self Test, Transmit Break, Break Release and
Space Disconnect are all inverted — and one that still passes a naive send-and-receive test,
because none of those four bits is on the data path. **The bars are the spec; the sentence is a
typo.** And the bars only exist as pixels: overbars do not survive OCR, so this is another case
where the table had to be read as a page image (see the FD1771 note above).

**TWO PERIOD PROGRAMS CAN CONTRADICT EACH OTHER.** `DESIGN.md` §0.1 ranks artifacts above prose,
which is right, but artifacts do not automatically agree. The MM-103 manual ships two pulse
dialers: §8.3 holds the line on-hook while the timer bit is **high**, and §8.6 holds it on-hook
while the timer bit is **low**. They cannot both be right. The tiebreaker was not seniority but
**arithmetic plus the physical requirement**: §7.3.3.1 and §7.4.4.8 both state the bit is 40%
high / 60% low, and telephone pulse dialing needs the *break* to be the 60% interval — so §8.6 is
the working dialer and §8.3 emits a 40% break. §8.3's own prose asserts the high phase is "60
percent", which is what a reader copying the demo would have believed. **When two artifacts
disagree, find the third thing that can be derived.**

**Check for a text layer before spending an hour on a scan:** `pdftotext file.pdf - | wc -c`.
Several of these are pure page images and yield nothing. When that happens, the Read tool
renders PDF pages directly — that is how the WD177x was identified as the wrong part.

**Read the TABLES as images, not the OCR.** The FD1771 sheet's own prose contradicts its
Table 6 on a bit number (it calls Record Not Found "status bit 3" where the table says S4).
The table is right. Bit numbers from OCR'd prose are not evidence.

**A scan may be incomplete and not say so.** `88-SIO Rev 0 & 1.pdf` is titled "Rev 0 & 1"
and contains no Rev 1 section at all, and its address-selection chart is missing two of its
five pages. Check before trusting a gap.

**A LINEARIZED PDF TRUNCATES SILENTLY — AND THE MISSING PART LOOKS LIKE A SHORT SCAN.**
`3P+S Manual.pdf` is a web-optimized (linearized) scan. A fetch cut short — a `curl` that
times out — leaves a file that still **opens** and still renders its first two-thirds of
pages, while `pdfinfo` merely mutters xref warnings and the tail pages are simply absent. A
first pass read 49 of the 75 pages and reasonably concluded the **scan** was truncated, when it
was the **download** that was, and marked the SBS bit and the status polarity *not recovered*.
The tell is cheap: after fetching, check the byte count against the server's `Content-Length`
(`curl -sIL "<url>" | grep -i content-length`) and confirm the file ends in `%%EOF` before
trusting a page count. The complete **5,664,944-byte** file has all 75 pages; re-reading the
back half filled every gap (the Appendix V polarity confirmation, and that SBS is genuinely
undocumented rather than merely off-scan). **Verify a download before concluding a source is
incomplete.**

## Related

- `reference/README.md` — the index of the distilled, tracked `.md` references, one per
  source in the manifest above.
- `docs/roms.md` — provenance of the ROM images that *are* committed (they are small, and
  the build embeds them).
- `docs/boards/*.md` — the transcribed spec for each board. These are the working documents;
  the PDFs are the cross-check, not the dependency.
